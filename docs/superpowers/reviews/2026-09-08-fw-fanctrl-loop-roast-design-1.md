super-roast verdict: Blocking (23 confirmed)
mode: design        iteration: 1 of 3
profile (assumed): Single-operator personal Rust daemon (fw-fan-quiet) running as root on one Framework 16 laptop (Ryzen AI 9 HX 370 + RTX 5070, Bazzite). It caps CPU and GPU power to steer fan RPM indirectly through fw-fanctrl, which remains the only process that commands the fans; there is no network surface, no external users, and no valuable persistent data beyond a small state.json, and every exit path already restores stock limits. Blast radius is the operator's own hardware and comfort — sustained high temperatures, an audible fan limit cycle, or a machine that must be power-cycled — so resilience/observability/cost findings are down-weighted, while anything that could leave the machine thermally unprotected, strand the fans under a stale cap, or defeat the design's core purpose (holding the fan target without a learned thermal model) is treated as genuinely serious.
inputs: docs/superpowers/specs/2026-09-07-fw-fanctrl-loop-design.md (root spec + 23-row task tree)
coverage: 8 scouts ran (0 dead) · 245 raw → 75 deduped → 57 panel / 18 spot-checked · judge completion 100% · remainder-capped: 34
independence: same-family (Claude) — seat-differentiated panel
seat-agreement: panels 56 · rr 0.63 · rg 0.77 · fg 0.57 · unanimous 0.48 · ground-loo 0.77 (n=35) · reproduce 24/32/0 · refute 11/45/0 · ground 29/27/0

## Confirmed findings
- [Blocking] §2.4 'observe_applied' / fwloop.12 / §3.1 CONSERVATIVE_START deletion — observe_applied back-calculates against the commanded post-clamp caps (cpu_w+gpu_w == u by construction), not measured consumption, so whenever demand is below the cap (idle, game lulls) the single integrator winds to cpu_max_w+gpu_max_w with no correction and every load onset runs uncapped through the dead time; the mitigation that used to cover this is deleted with no replacement.
  verdict: confirmed (reproduce ✓ / refute ✓ / ground ✓)
  evidence: All three seats traced that split_budget always commits the full u across the two axes (equal split at zero demand), so `observe_applied(cpu_w+gpu_w)` is an identity outside guard/slew cases; §2.4's own rationale names only GPU HOT and long ramps; RAPL is confirmed wired only to the stickiness watchdog; the §5 plant is driven by commanded watts so the acceptance suite structurally cannot detect it. Floor applied: this plausibly defeats the stated core purpose (±150 RPM over a fluctuating gaming session).
  fix-shape hint: Feed measured package/GPU power (RAPL `cpu_pkg_w`, NVML `gpu_w`, both already sampled) into `observe_applied`, or add an explicit "demand-starved" hold on the integrator when applied < commanded − margin.
- [Should-fix] §2.8 NVMe guard 'min(target + nvme_boost_rpm, FAN_TARGET_MAX_RPM)'; fwloop.12/17 acceptance — the NVMe guard's only remedy raises the fan target, which in this architecture raises T* and therefore the CPU/GPU budget (injecting more heat), has no escalation, no authority when the SoC is idle, and the plant scripts nvme_temp_c so no test can observe whether it helps.
  verdict: confirmed (reproduce ✓ / refute ✓ / ground ✓)
  evidence: fwloop.12's acceptance literally requires "raises T*, and the budget rises"; the dGPU guard in the same section cuts power at the source (opposite sign); a cap raise is inert with no compute demand (the exact heavy-I/O-idle-SoC case the guard exists for); fwloop.16 makes nvme_temp_c a scripted input.
  fix-shape hint: Document the airflow-wins assumption with a measurement, or have the guard act on the caps (reduce budget) and/or an independent duty path rather than the fan target; add a timeout/escalation.
- [Nit] §2.4 defaults 'kc_w_per_c = 0.4, ti_s = 35' vs §2.2 60-sample boxcar — the shipped defaults reproduce the §3.3 IMC formula on the raw §5 plant (Kc=35/(0.8·110)=0.398) while the regulated variable is the 60 s boxcar (≈30 s extra group delay), so they are derived against the wrong plant model whenever calibration is skipped or rejected.
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: Reproduce and ground verified the exact-match arithmetic and the (N−1)/2 group-delay fact; the calibration path fits on the EC average (boxcar-aware) but the defaults do not. Demoted from Should-fix to Nit: the refute seat simulated the literal loop (FOPDT + 60 s boxcar + velocity PI at Kc=0.4/Ti=35) and found 0.2 °C overshoot nominal and no relay even under simultaneous worst-case ±50 % perturbation; profile fact — worst outcome is a comfort-level fan cycle on one operator's laptop, and the sim says it does not occur.
  fix-shape hint: Re-derive defaults with θ_eff ≈ θ + N/2 (or state in §2.4 that the λ=90 floor is the intended margin for the boxcar).
- [Should-fix] §2.4 'kc_w_per_rpm = 0.4 / 55'; §2.3 seed; §2.7 cool16 steep; fwloop.17 cool16×RpmLoop — Mode B's fixed gain assumes ~55 RPM/duty and quiet16's flat 0.6 %/°C segment; on cool16's 3.87 %/°C segment the watts→RPM plant gain is ~4× higher, loop_gains is strategy-agnostic, gain scheduling is deferred, and fwloop.17 requires the cool16×RpmLoop run to pass with defaults.
  verdict: confirmed (reproduce ✓ / refute ✓ / ground ✗-survived)
  evidence: Reproduce independently recomputed K_rpm ≈172–224 RPM/W on cool16 vs ≈45 on quiet16 (loop gain ≈1.25–1.6); refute confirmed loop_gains has no strategy key and §3.1 deletes the old anti-relay nets; ground's correction (finding's 254 RPM/W overstated, true multiple ~3.9×) does not remove the risk to the stated acceptance run.
  fix-shape hint: Pick a conservative default derived from the steepest supported tread, or key/scale kc_w_per_rpm by the active curve's slope_at(T*) (a minimal form of the deferred schedule).
- [Should-fix] §3.3 step 4 rejection rule (K ≤ 0, τ < 5 s); fwloop.21 — Kc = τ/(K(λ+θ)) is unbounded as K→0⁺ and no sanity range or comparison-to-default exists, so a step that stays under quiet16's 55 °C knee (duty pinned at 15) fits a tiny positive K_rpm and persists a Mode B gain orders of magnitude too large.
  verdict: confirmed (reproduce ✓ / refute ✓ / ground ✓)
  evidence: quiet16 = (0,15)(55,15)… is flat below 55 °C; §Facts idle max is 47.85 °C; StepTest starts at the floors with fixed STEP_W=30; only two rejection rules exist anywhere; the bad gain persists to state.json and is loaded by Budget::new.
  fix-shape hint: Reject fits whose K is below a floor or whose derived Kc exceeds k× the default; require a minimum RPM response magnitude for the RPM fit.
- [Should-fix] §3.3 steps 1–2 'Step u by STEP_W = 30 W … burner on for CPU'; fwloop.21 'fit_fopdt(&[(t, y)], step_w)' — the fitted K is biased by construction: step_w is a single scalar never defined as the realised per-axis delta (split_budget divides the 30 W by demand), the burner starts with the step so the idle→load transition is folded into ΔT, and an argmax handover mid-step appears as extra dead time.
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: §3.3 explicitly wants CPU+GPU load during the one step, so the CPU axis receives < 30 W; fwloop.13's only gate is binary "power never rose"; a biased-low K yields a biased-high Kc via the spec's own formula; fwloop.17's plant maps commanded watts directly so the confound is untestable.
  fix-shape hint: Pass the measured pre/post applied-power delta per axis as step_w; start the burner before the settle hold; abort the fit on an argmax label change.
- [Should-fix] fwloop.17 'derived gains within 25 % of the plant's'; §3.3 fits 'the EC average' — the fit runs on the 60-sample boxcar of the plant's response, so recovered τ/θ (and Ti=τ) cannot land within 25 % of 35 s/20 s by construction; the acceptance clause is unsatisfiable as written.
  verdict: confirmed (reproduce ✓ / refute ✓ / ground ✓)
  evidence: Reproduce and refute independently simulated the pipeline (noise-free): τ̂ +28–30 %, θ̂ +110 %, Ti error +30 %, K within 2 %; no spec text redefines "the plant's" as the composite.
  fix-shape hint: Fit on the raw EC reading (not ec_ma), or define the reference gains as those the IMC formula gives on the boxcar-filtered plant, or restrict the 25 % clause to K and Kc.
- [Nit] §2.7 'Unreachable from below'; §2.4 'at_lower_bound_for()'; fwloop.10 ArbiterInput — no upper-bound counterpart exists: u pinned at cpu_max_w+gpu_max_w with the error still calling for more heat (target above the fan's reach, stalled-but-valid tach) raises no flag and is indistinguishable in telemetry from a converged loop.
  verdict: confirmed (reproduce ✓ / refute ✓ / ground ✓)
  evidence: All three seats grepped the spec: only at_lower_bound_for is owned/tested; fwloop.17 has a sub-floor run but no above-ceiling counterpart. Reproduce also found the SIGKILL/ExecStopPost sub-premise likely wrong (systemd runs ExecStopPost on abnormal exit). Demoted to Nit: profile fact — u at the ceiling is the same power stock operation already allows, so this is an observability parity gap, not a thermal or stale-cap risk.
  fix-shape hint: Add at_upper_bound_for() and a symmetric `high` TARGET UNREACHABLE reason (u at upper bound ≥ 60 s with error still positive).
- [Should-fix] §2.6 reconciliation compares replica max_c vs 'temperature' only; §2.1 ma_temperature used only for reseed — the regulated variable (the MA) is never checked against the socket's movingAverageTemperature, and upstream (verified in FanController.py) skips history appends while paused and injects 50 °C on framework_tool failure, so the divergence that matters is invisible in production.
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: Reproduce read the installed upstream source (run() appends only under `if self.active`; hardcoded `return 50` fallback); §2.2's replica is not gated on active; §5's MA-tracking assertion is steady-state-only against an emulator built from the same assumptions and fwloop.16's quirk list omits the active-gated freeze.
  fix-shape hint: Add a bounded |ec_ma − ma_temperature| check to §2.6 (or reseed on every resume/active-edge), and add the paused-buffer quirk to the emulator's fidelity list.
- [Should-fix] §2.6 'the poll and the sample are within one tick … EC temperatures move far slower' — the print-all reply is merged up to one 5 s tick (plus socket latency) after its capture, both sides are integer-rounded, and the spec's own plant (K 0.8, τ 35) gives ~0.9 °C/s on a 40 W step, so a sustained directional skew can trip three 1 °C strikes during exactly the load ramp fwloop.17 requires to "stay in TempLoop".
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: The ramp-rate figure is derived from the spec's own plant constants; the 3-strike debounce rejects noise, not a monotone bias; both Sample and FanctrlView already carry monotonic stamps that are not used to gate the comparison.
  fix-shape hint: Skip the comparison when |observed_at − sample stamp| exceeds a bound, or gate it on |dT/dt| of the replica below a threshold.
- [Should-fix] §2.2 'cleared only when the replica is re-seeded after EC MISMATCH clears'; §2.5 15 s entry hysteresis — EcAverage is never seeded on auto entry, socket recovery or daemon restart, the initial EC MISMATCH state is unspecified, and the entry hysteresis (15 s) is 4× shorter than the boxcar fill (60 s), so TempLoop can engage on a 15-sample mean used as a 60-sample mean.
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: fwloop.12 wires exactly two EcAverage operations (set_interval, reseed on Decision.reseed_ma); the boxcar push lives inside on_auto_sample so it starts empty on every manual→auto toggle; §2.4 has an explicit warm-start rule for u but none for the MA; fwloop.23's cold-start flow never asserts ec_ma correctness.
  fix-shape hint: Seed EcAverage from view.ma_temperature on every auto engagement/re-engagement and state the arbiter's initial mismatch state as "unreconciled".
- [Should-fix] §2.5 'RpmLoop | fan_valid' + '3 consecutive ticks' rule; §2.2 argmax tie rule — the entry hysteresis is vacuous for RpmLoop (fan_valid is continuously true), while the argmax-controllable precondition is evaluated on integer-rounded readings with no debounce, so a single-tick argmax flip exits TempLoop immediately and costs 15 s to re-enter.
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: EC MISMATCH gets a 3-view debounce and guards get a 5 °C band, but argmax has only a sysfs-order tie-break; §Facts show a 4 °C ambient/apu gap that closes under load. Ground corrected one sub-premise (T* feasibility does have a 5 °C margin and 60 s clear), but the argmax path alone sustains the finding.
  fix-shape hint: Debounce the argmax-controllable condition (N consecutive samples or a temperature margin) before it can fail the TempLoop row.
- [Should-fix] §5 'All timing decisions … monotonic timestamps'; no section mentions suspend/resume — Rust Instant is CLOCK_MONOTONIC on Linux (rust-lang/rust#87906, #88714 unmerged), which does not advance across suspend, so pre-suspend views look fresh, the boxcar holds cold samples, and steady windows can straddle sleep and write into warm_start/duty_rpm_table.
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: Reproduce fetched std's time.rs at the pinned toolchain (CLOCK_MONOTONIC, no BOOTTIME) and found the current codebase already has `Sample.resumed` with a comment fixing this exact class for the old adaptation tier — the new spec deletes that tier and introduces equivalent windows without mentioning `resumed`. Profile fact keeping it Should-fix: a laptop daemon suspends daily. The ryzenadj-reset-on-resume sub-claim is unsupported (FAQ text is about AC/power-mode changes).
  fix-shape hint: On `Sample.resumed`, clear EcAverage/fan windows/steady-window state and reset freshness stamps (mirroring the existing resume handler).
- [Should-fix] §2.3 'commanded duty constant over the window … the entry for that duty is updated'; fwloop.19 'refine(duty, mean_rpm)' — "commanded duty" resolves to target_duty (the controller commands watts, not duty), so any steady plateau where the physically achieved duty diverges (GPU HOT episode, floor-clamped target) writes measured RPM into the wrong duty's persisted entry; the 25 % band does not catch one-tread errors.
  verdict: confirmed (reproduce ✗-survived / refute ✓ / ground ✓)
  evidence: Refute traced a GPU HOT episode (integrator not frozen, target_duty unchanged, back-calc yields a steady low plateau) passing the gate and the 25 % band; ground showed the §2.7 sub-floor run (explicitly tested as a steady state) satisfies the refine gate with floor RPM; no acceptance test checks table integrity after either.
  fix-shape hint: Gate refinement on "not guard-overridden, not at a budget bound, and speed_pct == target_duty" (speed_pct is already polled).
- [Should-fix] §5 / fwloop.17 'no relay (no ≥ 3 consecutive excursions with 60–200 s period)' — the window is not derived from the loop's own constants; with Ti=τ the nominal loop is IPDT with θ_eff ≈ 50 s (θ 20 + boxcar 30), giving an ultimate period ≈ 4·θ_eff ≈ 200 s, so a marginal loop hunts at or beyond the window's upper edge while spending most of each cycle inside the band.
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: Reproduce derived Pu = 4·θ_eff from the spec's own IMC cancellation; the ±50 % perturbation runs erode margin further; the ≥90 % band and 40 s steady-window checks are both satisfiable by a slow cycle. Refute's crossover calculation (146–182 s) lands inside the window but only just, and the window is nowhere derived in the spec.
  fix-shape hint: Widen the relay window (e.g. 60–600 s) or replace it with a period-agnostic hunting detector (excursion count / autocorrelation) and state its derivation in §5.
- [Should-fix] §2.8 'dGPU (gpu_hot_c, default 83 …)', exit 78 — 83/78 sits inside a laptop RTX 5070's normal sustained gaming band (the spec's own research doc records an 87 °C internal target loop), so the guard latches for whole sessions and ratchets GPU power to the floor as a permanent clamp; no derivation supports the default and fwloop.17's criteria would pass a permanently latched guard.
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: Ground located "RTX 5070 has its own internal 87 °C target loop" in docs/research/05-fw-fanctrl-loop.md and confirmed the external citations; the guard is the one behaviour-gating threshold with no entry in §Facts. Refute's point (there is a separate 87 °C hard-trip watchdog) supports rather than refutes routine engagement.
  fix-shape hint: Log NVML temperature for one session and set gpu_hot_c above the observed steady value (or key the guard on a rate/excess over the vendor target), and note the rationale in §2.8.
- [Nit] §2.1 'Curve: piecewise-linear in file order (never sorted)'; 'tread(d): the maximal temperature interval' — non-monotone curves are admitted, on which a duty's preimage is a union of intervals, tread() is ill-posed, t_star can land on a descending segment, and the steepness check (slope > 2 %/°C) never fires on a negative slope.
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: grep shows the only monotonicity constraint anywhere is on DutyRpmTable; only quiet16/cool16 are exercised. Demoted to Nit: profile fact — one operator, two verified monotone curves; the failure needs a pathological user edit.
  fix-shape hint: Have Curve::from_points reject (or flag and fall to RpmLoop on) any descending segment and state monotonicity as a precondition in §2.1.
- [Should-fix] §2.9 'three consecutive mismatches ⇒ release to stock with the flag held'; fwloop.12 'a later Verified re-engages' — after release-to-stock nothing states whether the per-tick write+read-back continues, so a later Verified may be unproducible (silent permanent loss of auto) or the controller keeps commanding after "release"; §2.5's Released has an explicit re-entry rule, the actuator path has none.
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: §3.3 explicitly says writes continue under Freeze::Calibrating; no equivalent sentence exists for the 3-strike case; the arbiter never observes actuator health so no mode transition drives re-entry. Refute's reading (per-tick freeze argument implies writes continue) is plausible but unstated.
  fix-shape hint: One sentence in §2.9: after 3 strikes, keep issuing the computed write + read-back every tick (or a probe at a stated cadence); the first Verified clears the strikes and re-seeds.
- [Should-fix] §2.9 'set_sustained_mw writes … then runs ryzenadj --info'; 'Verified iff … within 0.5 W' — no settling delay, re-read or debounce exists, and the RyzenAdj FAQ's documented cause of lost limits is the platform reasserting vendor defaults on AC unplug/power-mode change/periodic resets, so normal platform behaviour can freeze the integrator and reach release-to-stock in 15 s; on_ac is sampled but never used to disambiguate.
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: FAQ text fetched and confirmed by two seats; the decision table's "mismatch = actuator failure" contract is contradicted by a platform reset; fwloop.7/12 test only a stale fixture. The refute seat's point that the RAPL watchdog (REASSERT_PERIOD_S=10) was built for this shows the concern is real; it does not gate the new §2.9 strike counter. The finding's Options-wiki citation does not contain the quoted text.
  fix-shape hint: Suppress/discount mismatches for N ticks after an on_ac edge or after any read-back showing vendor defaults, and re-read once before scoring a Mismatch.
- [Should-fix] §3.3 'StepTest (needs … argmax_controllable …)'; step 1 'Hold u at the floors' — the precondition is evaluated at the floors at idle, where the spec's own fixture (fwloop.20: ambient 47.85 > apu 43.85) makes the argmax uncontrollable, so the phase self-skips before the burner ever runs and unfitted defaults ship; no thermal abort exists either.
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: CalibContext is rebuilt each sample; the burner starts only in step 2; the fixture is a verbatim capture of the target machine; the Decisions table commits to the step-test phase.
  fix-shape hint: Check argmax_controllable after the burner is running (or gate only fanctrl_active/!ec_mismatch before the settle hold) and add an EC ceiling abort for the 30 W step.
- [Should-fix] §2.5 'active: false … the EC's own curve drives the fans — RpmLoop handles that'; §3.3 StepTest needs fanctrl_active; fwloop.16 — under active:false the EC firmware's unmodelled fan curve is inside Mode B's loop, Mode B's gain was identified only against the fw-fanctrl curve, and the plant has no EC-autofan model, so the fallback's stability in the regime it is designated for is never graded.
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: The research input states "EC takes over with its own unknown curve"; fwloop.16's plant has one Curve; the only active:false test asserts bumplessness at the switch instant. Ground correctly notes the "voids one-integrator" phrasing overstates (an unknown static curve adds no integrator) but the gain-mismatch risk stands.
  fix-shape hint: Run the finding's spike (pause fw-fanctrl, step SoC power, log EC max vs RPM) and either bound the EC curve's slope or add a second curve model to FanPlant for the active:false runs.
- [Should-fix] fwloop.8 acceptance 'no ModelSnapshot/trim_rpm/gain symbol remains outside the deletable modules' vs fwloop.22 owns 'the removal of the adaptation tier and its tests' — controller.rs is not a deletable module, and the current file has ~98 such references woven through the tier and its tests, so fwloop.8 cannot pass its own acceptance without doing fwloop.22's chartered work one round earlier.
  verdict: confirmed (reproduce ✗-survived / refute ✓ / ground ✓)
  evidence: Both confirming seats grepped controller.rs (trim_rpm ×69, gain ×83, a ~700-line adaptation-tier test block) and cited fwloop.8's "types only / compile-only" framing as incompatible with the symbol sweep.
  fix-shape hint: Narrow fwloop.8's acceptance to "no such symbol in the new type surface" and move the repo-wide symbol sweep to fwloop.22 (or fwloop.14).
- [Should-fix] fwloop.14 deps 'fwloop.13, fwloop.15, fwloop.22' vs fwloop.5 'trim.rs becomes unused and is deleted by fwloop.14' — fwloop.5 is trim.rs's only consumer-remover and the only source of the CONSERVATIVE_START/overshoot_settle/contour symbols fwloop.14's sweep must not find, yet fwloop.14 lists no dep on fwloop.5; a readiness-based scheduler can run 14 before 5 and break the build.
  verdict: confirmed (reproduce ✓ / refute ✓ / ground ✓)
  evidence: All three seats walked the transitive closure (13→4/11/21, 15→8/9, 22→8 — never 5) and verified allocator.rs:1345 `use crate::control::trim::Trim` inside the sims fwloop.5 deletes; the preamble's "every dep is paired with a blocked-by line" is violated.
  fix-shape hint: Add fwloop.5 to fwloop.14's deps with a blocked-by line.

## Not verified (beyond panel cap)
- none

## Beyond remainder cap (count only)
- 34 candidates dropped by the remainder cap — raise config.remainderCap and re-run to see them

## Rejected (with reason)
- §1/§2.4 Mode A regulates T*−MA while fw-fanctrl uses min(MA, current) — reject 2/3: the branch is emulated in §5 and tested by fwloop.16/17, passive table refinement absorbs delivered-duty bias cause-agnostically, and §6 explicitly reserves the outer RPM trim on T* pending field validation (ground's cheaper fix — regulate min(ma_temperature, temperature) — is worth noting).
- §2.1/§2.7 tread()/t_star() on the flat clamps — reject 2/3: the quietest tread's infeasibility routes to the in-scope RpmLoop fallback, the 60 s low-clamp rule fires independently of T*, and duty 100's tread is never selected (table tops at 85); the finding's own T* arithmetic is internally inconsistent.
- §2.7/§2.8 no CPU/APU over-temperature guard — reject 3/3: T* is bounded by the user's own curve (≤95 °C on quiet16), AMD's cHTC/PROCHOT ceiling is firmware-enforced independent of ryzenadj limits, and §6 explicitly defers the --tctl-temp CPU leg.
- §Facts/§2.2 replica equivalence rests on one idle coincidence — reject 3/3: §2.6's continuous three-strike reconciliation with tested A→B→A fallback is exactly the mechanism for sensor-set divergence; the cited EXAMPLES.md does not contain the mapping claimed.
- §2.2/§2.7 dGPU-powered gaming makes quiet T* infeasible — reject 3/3: §2.7's second rule (u at lower bound ≥60 s ⇒ TARGET UNREACHABLE low) covers it, and §5's dGPU-hot run deliberately grades only "stay in TempLoop", an accepted scoped-down criterion.
- §5 chained plant has no fan→temperature path — reject 3/3: §6 explicitly bounds in-tree evidence to the perturbed chained plant with field validation post-merge, names gain scheduling as deferred, and the STEEP CURVE mechanism (curve slope) is modelled.
- §Goal 'converged window' undefined — reject 3/3: the executable criterion (§5/fwloop.17: 30 min post-step, ≥90 % in band) is fully concrete; the settling-time estimate assumes an unstated step magnitude.
- §3.3 single-point FOPDT identification — reject 3/3: gain scheduling is named and deferred in §6, IMC λ-tuning plus ±50 % perturbation runs are the stated mitigation, and StepTest requires GPU load too.
- §2.5 arbiter mode undefined during soft-condition lapse / Released cap step — reject 3/3: §2.6/§2.7 say "→ RpmLoop" explicitly, fwloop.10 states "3-tick in, immediate out", and the dual-failure Released cap step is a recorded decision with its own fwloop.17 test.
- §2.7 infeasible targets hold the floors indefinitely — reject 3/3: holding the floor with a self-clearing explanatory flag is the specified, tested behaviour; a dead socket with a valid tach lands in RpmLoop, not the floors.
- §2.3 duty_for_rpm has no snap hysteresis under refinement — reject 2/3: refinement needs a ≥40 s constant-duty window (a natural dwell), each resnap is bumpless via resync_error, and a one-tread flip (~55–95 RPM) sits inside the ±150 band.
- §2.3 passive refinement accepts a 24 % saturated error — reject 2/3: converging to the machine's real curve is the mechanism's purpose (fwloop.17's −8 % bias run), max(fan1,fan2) resists a single dead fan, and §6 reserves the outer trim if refinement proves insufficient.
- Mode B setpoint with strategy=None / no cached curve — reject 3/3: duty_for_rpm is table-only, nearest_tread is a conditional correction, and a warm-start miss falling to the floors is the documented rule; fwloop.17's Released run presupposes prior engagement.
- cool16 1 °C integer step ⇒ ~200 RPM jump the emulator cannot show — reject 2/3: the plant emits EcReading (integer max_c by §2.2), fwloop.16 mandates a current-branch test, §5's load-release run has a 90 s recovery bar, and the 90 % criterion budgets excursions.
- Deadband deletion ⇒ resolution limit cycle — reject 3/3: the PI's measurement is a 60-sample average (~1/60 °C resolution) and its actuator is continuous watts, not integer duty; the spec's relay test covers the residual risk.
- STEEP CURVE warning-only as the entire defence — reject 3/3: the research the spec cites endorses warn-or-refuse, gain scheduling targets low-RPM plant gain (a different concern), and fwloop.17's cool16 no-relay runs gate it.
- gpu_* stay controllable during GPU HOT — reject 3/3: §2.4's observe_applied is explicitly justified by the GPU HOT windup case and fwloop.12 tests "u does not grow"; the guard's ramp is itself the actuator.
- GPU HOT guard vs back-calculation "chase downward" — reject 2/3: the two statements are within-tick vs cross-tick views of one mechanism, u is floor-clamped, and EC max stays GPU-dominated while the GPU is hot so fans do not slow prematurely.
- Guards fail open on sensor loss with no flag — reject 3/3: the None rule is stated twice and tested (fwloop.6), the primary loop still sees gpu_* via the EC replica, and hardware/firmware thermal cutoffs remain independent.
- Released removes caps at the least-known thermal state — reject 3/3: "release to stock if the RPM loop is invalid too" is a recorded user decision; a live EC reading with a usable socket keeps TempLoop; EC-only capping would reintroduce the thermal model the goal excludes.
- Curve from the 0777 socket unvalidated — reject 2/3: upstream's set_config is schema-validated (speed/temp 0–100, minItems 1), u is hard-clamped to [floors, maxes] regardless of T*, and §6 names the socket-permissions upstream note.
- fanSpeedUpdateFrequency/effectiveTemperature never modelled — reject 3/3: fw-fan-quiet's live strategies are freq=1 (research doc), the StepTest fits real dead time empirically, and effectiveTemperature's omission is the documented control-the-MA choice.
- GRID_STEP_W unstated and possibly larger than the band — reject 3/3: it is the existing 0.5 W allocator constant (≈82 RPM even at the finding's own gain), and it quantises the split, not the total.
- resync_error on every re-derivation makes retargets integral-only — reject 3/3: no-proportional-kick bumplessness is the Goal's stated invariant, tested in fwloop.4/17; the suppression lasts one tick, not the whole approach.
- Empty warm_start seeds at the floors (~3.4 W/min climb) — reject 2/3: the Goal's metric is scoped to the converged window, IMC closed-loop settling is ~3λ not a linear extrapolation of the first tick, and fwloop.17's load-step runs exercise a floor-adjacent start.
- GPU lock verification unsound (transient false Mismatch) — reject 2/3: NVML clock transitions settle in ~100 ms, far under the 3-consecutive-1 Hz-sample rule, and the shared three-strike release is an explicit, tested reuse of the existing watchdog pattern.
- No argmax-switch event handling — reject 3/3: max() is continuous across a crossover, uncontrollable↔controllable crossings already trigger mode-entry resync, and the ±50 % robustness runs plus deferred gain scheduling cover per-sensor gain variance.
- Seven allocator mitigations deleted, field-replay sims gone until round 6 — reject 3/3: §5 states the acceptance sims are deliberately the last leaf, fwloop.4/5 carry unit coverage from round 1, and the old sims cannot compile against the new AllocInput.
- fwloop.17 sits after the deletion sweep with no kill gate — reject 2/3: §5's "last leaf" sentence is about grading the fully-wired controller, §6 bounds in-tree evidence explicitly, deletion is on a branch (revertable), and fwloop.4/16/21 catch a broken approach earlier.
- Mode A adopted with no recorded rationale over Mode B — reject 2/3: fwloop.17 already runs RpmLoop alone against the goal criterion (the finding's own spike), Mode B is required in-scope regardless, and the Decisions table records the choice as taken with the user.
- controller.rs co-edited by 5/7/8/11/13/22 with no ordering — reject 2/3: fwloop.12 lists 5/7/8/11/22 as deps, 13 touches a distinct call site merged by fwloop.19, and 8→22 and 11→13 edges exist; no incompatible pair was demonstrated.
- fwloop.20's dGPU-on fixture is an unmakeable acceptance — reject 3/3: the same-second capture was already done for the unpowered case (47.85 vs 48.0), the dGPU is powerable on demand, the pairing is consumed only by fwloop.3, and the finding's own spike calls it a five-minute test.
- fwloop.17 'Released … re-engages from the warm-start without a cap step' unsatisfiable — reject 3/3: the entry-side stock-caps step is explicitly expected ("shows stock caps"), and "without a step" matches fwloop.12/fwloop.4's no-proportional-kick idiom for the same event.

## Unverified nits (spot-checked)
- [Nit] §Decisions/§3.2 warm-start key — omits ambient and CPU/GPU workload split; spot-check rejected (bumpless promise is scoped to outages/strategy edits; per-component keying would need the excluded thermal model).
- [Nit] §3.4 on_ac from hard-coded /sys/class/power_supply/ACAD/online — spot-check rejected (single-machine scope; on_ac only keys warm-start with a floors fallback).
- [Nit] §2.8 nvme_composite_c() has no device selector — spot-check rejected (target machine has one NVMe; multi-drive is out of stated scope).
- [Nit] §3.3 'u is unchanged from calibration start to exit' vs SetBudget seeding — spot-check rejected (clause is LUT-sweep-scoped; fwloop.19 restates it precisely; seed() is not gated by freeze).
- [Nit] §2.4 LoopGains lacks tau_rpm/theta_rpm and persists unread intermediates — spot-check rejected (Ti = τ under IMC so tau_rpm is ti_rpm_s; only theta_rpm is absent and nothing consumes it).
- [Nit] §2.9 read-back state machine: Unreadable/Unverifiable vs freeze/strike reset, shared counters — spot-check rejected (freeze is a per-call step() argument; "N consecutive" idiom is used consistently; GPU routes "its" mismatch).
- [Nit] §2.9 --fast-limit derivation unspecified — spot-check rejected (fast_limit_mw is an existing fixed 53 W constant, never derived from budget).
- [Nit] §2.8/fwloop.6 FAN_TARGET_MAX_RPM never defined — spot-check rejected (existing controller.rs const = 7000.0, untouched by the spec).
- [Nit] §3.1 AllocInput lists lut vs fwloop.5 lists gpu_floor_w — spot-check rejected (integrator bounds come from Budget::set_bounds in fwloop.12; fwloop.5 owns the shape). Worth a copy-edit.
- [Nit] §2.2/§2.6 EC replica + reconciliation vs using the socket's own MA — spot-check rejected (print all is capped at 30 s with two forks; the PI ticks every 5 s against a τ 35 s plant).
- [Nit] §2.1 speed_pct routed only to telemetry — spot-check rejected (enhancement, not a violated requirement; the design deliberately deletes trust/mismatch machinery).
- [Nit] §2.8/§3.1 DOWN_RATE_W doubles as GPU HOT pace with no response-time requirement — spot-check rejected (both roles are explicit; the Safety model promises no SLA; unchanged constants are not restated elsewhere either).
- [Nit] §1 'exactly one integrator' vs the 1 Hz GPU PI — spot-check rejected (claim is scoped to the two controllers on one measurement; the GPU PI closes on NVML watts, unchanged).
- [Nit] §2.2 replica boxcar sized in 1 Hz samples vs fw-fanctrl's ≈1.02–1.06 s tick — spot-check rejected (both boxcars are sample-count based by design; a 2–6 % window skew is negligible at the converged steady state the criterion measures).
- [Nit] §2.2 hwmon chip discovery has no re-discovery rule — spot-check rejected (reuses existing hwmon.rs behaviour shared by fan_valid; degrades to the designed RpmLoop fallback; cited LWN article does not discuss renumbering).
- [Nit] §2.1 1 s connect timeout unavailable on std UnixStream — spot-check rejected (std gap confirmed via rust-lang/rust#53615, but freshness is stamp-driven so a hung connect degrades to Stale; libc is already a dependency).
- [Nit] §2.2 ec_valid needs one surviving reading, no plausibility bound — spot-check rejected (mirrors fw-fanctrl's own algorithm by design; dGPU/NVMe guards are independent of the replica).
- [Nit] Controller process death leaves caps applied — spot-check rejected (RestoreGuard/FinalRestore/startup_reset in guard.rs and main.rs already cover crash, panic and SIGKILL for the same actuators).

## Escalations (need human)
- none
