super-roast verdict: Blocking (9 confirmed)
mode: design        iteration: 3 of 3
profile (assumed): Single-operator personal Rust daemon (bazerame-fans) running as root on one Framework 16 laptop (Ryzen AI 9 HX 370 + RTX 5070, Bazzite). It caps CPU and GPU power in order to steer fan RPM indirectly through fw-fanctrl, which remains the only process that commands the fans. There is no network surface, no external users, and no valuable persistent data beyond a small state.json; rollback is stopping the daemon, and every exit path already restores stock limits. Blast radius is the operator's own hardware and comfort: the realistic worst cases are sustained high temperatures, an audible fan limit cycle, or a machine that must be power-cycled — not data loss or third-party impact. Weigh resilience/observability/cost findings accordingly, but treat anything that could leave the machine thermally unprotected, strand the fans under a stale cap, or defeat the design's stated core purpose (holding the fan target without a learned thermal model) as genuinely serious.
inputs: docs/superpowers/specs/2026-09-07-fw-fanctrl-loop-design.md (root spec + 23-row task tree), after the iteration-2 fix pass
delta vs prior: 7 new confirmed (2 Blocking) · 0 carried (0 Blocking) · 11 resolved · 2 regressed (0 Blocking)
coverage: 9 scouts ran (0 dead) · 38 raw → 15 deduped → 12 panel / 3 spot-checked · judge completion 100% · remainder-capped: 0
independence: same-family (Claude) — seat-differentiated panel
seat-agreement: panels 12 · rr 0.83 · rg 0.67 · fg 0.67 · unanimous 0.58 · ground-loo 0.70 (n=10) · reproduce 8/4/0 · refute 8/4/0 · ground 12/0/0

Prior-report disposition (iteration 2, 12 confirmed): 11 resolved in the current text — both iteration-2 Blocking findings (ungated back-calculation tracker; combined-sum undrawn-share collapse) are closed by the mechanism replacement in §2.4, and the two Blocking items below are new failure modes of that third revision, evidenced in the current §2.4/§2.8 text, not restatements. 1 iteration-2 finding fixed incompletely (θ_eff double-count, §3.3 step 3 left verbatim) and 1 iteration-1 finding fixed incompletely (dGPU guard 83/78 → 90/85 still latches below the card's own 87 °C setpoint) are listed as regressed. 0 still-open. No escalations: every panel seated 3/3 and no external finding drew an UNVERIFIED vote.

## Confirmed findings
- [Blocking] §2.4 'Demand-limited freeze (per axis)' lines 261-284 (rule 267-271); §2.5 ~299-308; §2.7 377-384; §5 603-606; fwloop.4 `demand_limited(&[(draw, cap)])` / `step(err, freeze)` + acceptance; fwloop.12, fwloop.17, fwloop.19 warm-start gate — `Freeze::DemandLimited` is direction-blind: it holds `u` whenever every axis draws >3 W below its cap regardless of the error's sign, so it blocks the downward integration that is the loop's only route back to a binding cap. Once `u` sits above the workload's free-running draw with the EC above T* (warm-start seed from a heavier same-key session, mid-session RPM-target drop, approach overshoot after a lull), the state is self-latching — freeze keeps `u` above draw, unconstrained draw keeps the freeze engaged — and the fans sit above target for the session. Neither §2.7 rule fires (both need a bound), no StatusFlag exists for a freeze, every graded demand-limited run exercises only the positive-error/lull direction, fwloop.4's acceptance ('u never decays toward the draw', no sign qualifier) forbids the directional repair, and it defeats §2.5's own `active: false` surfacing (descent to the floor is frozen, so `TARGET UNREACHABLE (low)` never fires). (new; third revision of the §2.4 mechanism, mirror image of the iteration-2 Blocking tracker, not a restatement)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: Rule text 267-271 has no sign term and its rationale (261-265) is one-signed ('at idle or in a game lull the temperature error stays positive'); the freeze is a true hold ('a freeze, not a pull-down'), so with `split_budget` deriving next tick's caps from the frozen `u`, both axes read demand-limited again every tick — a fixed point. `warm_start` keys on (strategy, duty, on_ac), not workload; `resync_error` on a target drop resets only e_{k−1}; §2.7 low/high need `at_lower_bound_for`/`at_upper_bound_for`; ArbiterInput carries no freeze field; fwloop.19's steady-window gate lacks 'not frozen'. Ground fetched erdosmiller / MathWorks anti-windup references: conditional integration halts accumulation only in the saturation-deepening direction — exactly the asymmetry the rule omits. Floor applies: this defeats the stated core purpose (±150 RPM for ≥90 % of the converged window) silently and indefinitely.
  fix-shape hint: Freeze only while every axis is demand-limited AND the PI's Δu would be positive (error calls for more heat); let negative-error descent integrate normally; add the fwloop.4/fwloop.17 seed-high-then-lighter-load case; exclude frozen ticks from warm_start recording; and while a DemandLimited hold is active surface the reason in the §3.5 header (the rejected observability item below becomes moot once the latch is gone).

- [Blocking] §2.8 dGPU bullet line 398; §2.5 line 306; §2.4 lines 267-271 and 274-279; §3.1 pipeline order; fwloop.4, fwloop.12 acceptance ('a GPU HOT override leaves the GPU axis demand-limited and u does not grow'), fwloop.17 ('5 min GPU HOT episode ... no post-episode overshoot > 150 RPM') — §2.8 and §2.5 still justify not freezing the integrator during GPU HOT / `active:false` by citing 'the applied-power back-calculation (§2.4)', a mechanism §2.4 274-279 and fwloop.4 explicitly deleted, and the replacement does not cover the gap: the override sets the GPU cap to `max(gpu_floor_w, current gpu_w − DOWN_RATE_W)` (8 W/tick, allocator.rs:62), at or below the present draw, so a hot loaded GPU tracks its overridden cap and is NOT demand-limited under fwloop.4's predicate; with the CPU at its own cap (the sustained-gaming case §2.8 derives the 90 °C threshold from) the 'any axis at cap ⇒ integrate' branch fires and `u` winds against a share the guard discards for the whole episode, snapping onto the GPU at exit. fwloop.12's GPU HOT clause is unsatisfiable under fwloop.4's definition; fwloop.17 grades only a 5 min episode (≈10 W wind-up, inside the band). (new; iteration-1 rejected two GPU HOT wind-up items on the strength of observe_applied, which no longer exists — current text is the evidence)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: grep for 'back-calculat|applied-power' returns 212 (bounds), 274-279 ('the only back-calculation this design performs' = against the bounds), and 306/398 attributing anti-windup to the deleted variant. fwloop.12 fixes the comparison as 'evaluated per axis against the previous tick's caps' in a pipeline where guard overrides run after split_budget, so 'cap' is the post-override commanded value the 1 Hz GPU PI converges draw onto within the 5 s tick. §Facts 52-57: gpu_* read −150 even powered, so throttling the GPU barely moves the EC max, e stays positive, u integrates at ≈0.031·e W/tick toward cpu_max + gpu_floor_w and beyond. Even under the pre-override-share reading the OR-rule integrates whenever the CPU is at cap. Kept at Blocking: it contradicts three stated acceptance criteria in the guard's primary case and produces a GPU-HOT re-fire cycle at exit — the audible limit cycle the profile names as a realistic worst case.
  fix-shape hint: Decide explicitly whether GPU HOT freezes the integrator or hands the removed GPU watts to the CPU axis inside split_budget; define which value is the per-axis 'cap' in §2.4; sweep lines 306 and 398; rewrite fwloop.12's GPU HOT clause naming the CPU-axis state; extend fwloop.17's episode and assert u ≤ cpu_max + gpu_floor_w throughout.

- [Should-fix] §2.4 line 269 (`DEMAND_MARGIN_W = 3`, single threshold) and 218-222 ('on leaving any freeze'); fwloop.4 ('leaving any freeze ... call resync_error implicitly'); fwloop.16 demand model; fwloop.17 — the demand-limited condition is the only latching decision with no hysteresis, dwell or debounce, and leaving it calls `resync_error`. The loop's own equilibrium is draw ≈ cap − margin, so ordinary load fluctuation toggles `Freeze::DemandLimited` at the 5 s tick; each unfreeze sets e_{k−1} := e_k and discards the velocity form's proportional increment, leaving an integral-only loop (≈0.031·e W per unfrozen tick) that either degrades transient response or walks `u` upward at a load-determined duty cycle. fwloop.16's plant draw carries no noise, so no graded run can see it. (new)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: Every other boolean is protected — mode entry 3 ticks (317-320), argmax 3 samples/1 °C with the rationale that an undebounced flip is 'a self-inflicted mode oscillation' (322-327), EC MISMATCH three views (§2.6), guards exit = enter − 5 (391), feasibility 60 s (§2.7), actuator mismatch re-read + three strikes (§2.9) — while demand_limited is one comparison of a 5-sample tail-mean of RAPL/NVML against 3 W. Kc·Ts/Ti = 0.22·5/35 = 0.031 checks against line 233; the ±90 RPM plant noise is on FanPlant output only; fwloop.17's demand-limited runs are all far-below-cap / at-cap scripts.
  fix-shape hint: Enter at cap − 3 W, exit at cap − 1 W (or an N-tick dwell); state whether a DemandLimited unfreeze should call resync_error at all or hold e_{k−1} across the freeze; add a ±4 W-around-cap plant script asserting bounded toggles, no 30 min drift, 90 s onset recovery.

- [Should-fix] §2.4 lines 266-271 (`DEMAND_MARGIN_W = 3`, RAPL vs slow-limit on the CPU, NVML watts vs a clock-lock-realised setpoint on the GPU); §2.9 CPU/GPU verification; §Facts 36-86 ('we control locked clocks, not power'); fwloop.4, fwloop.12, fwloop.16; src/control/allocator.rs:167,170,205; src/control/controller.rs:51-52,101,105 — `DEMAND_MARGIN_W = 3` is stated with no derivation and no §Facts measurement of the steady-state offset between the compared quantities, applied flat to both axes although the repo's own surviving pinned-predicate uses axis-specific margins (`CPU_PINNED_MARGIN_W = 1.5`, `GPU_PINNED_MARGIN_MHZ = 30` on clock, `STICKINESS_MARGIN_W = 5.0` described as 'RAPL vs STAPM accounting slack') and §2.9 itself verifies the GPU by clock, not watts. A clock-pinned GPU drawing 3-5 W under its LUT-derived watt figure with a light CPU — the headline GPU-bound game — reads demand-limited on both axes and freezes the shared budget under the load it exists to regulate, with no flag; fwloop.16's plant models no accounting offset. (new; distinct from iteration-2's rejected multiplicative scale item on the deleted back-calculation — that rejection itself invoked a 5 W additive slack, which the new 3 W threshold now sits below, and never touched the GPU watts-vs-clock leg)
  verdict: confirmed (reproduce ✓ / refute ✗-rejected / ground ✓)
  evidence: Reproduce: `gpu_hot_c = 90` is derived from a §Facts measurement in the same section while the margin gets no rationale sentence; controller.rs:105 keeps `ACHIEVED_GPU_MARGIN_W = 5.0` 'wider than the CPU's: GPU draw telemetry is noisier and the PI dithers the clock around the target by a few watts'; §2.9 concedes STAPM 'reported to fail silently', so a stale sustained limit parks CPU draw below the fresh slow-limit with no Mismatch. Ground verified all cited constants live by grep and confirmed no StatusFlag accompanies DemandLimited. Refute (rejected) correctly notes ACHIEVED_* belong to the deleted tier and that DEMAND_MARGIN_W equals the CPU's 3 W precedent — which is why this lands at Should-fix (per-axis constant/predicate fix), not Blocking; its 'a clock-pinned GPU cannot draw more anyway' objection fails once the freeze also blocks descent (finding 1).
  fix-shape hint: Measure smoothed RAPL vs commanded slow-limit and NVML vs locked-clock setpoint on the machine, record both in §Facts, set the margin per axis above them (or reuse the §2.9/allocator clock-pin rule as the GPU binding test); add a fwloop.12 case with the GPU pinned at its lock 6 W under its share asserting the loop keeps integrating.

- [Should-fix] §3.4 lines 540-545 ('polled on the existing FanctrlPoller thread at 30 s alongside print all'); §2.1 freshness (~131-133: 'no successful print speed for 15 s ⇒ stale'); §2.5 arbiter table; fwloop.9 line 690; fwloop.17 fault list — moving the NVMe SMART read onto the `FanctrlPoller` thread relocated the stall: that one thread also owns the 5 s `print speed` poll whose staleness rule is 15 s, the read's only bound is the 60 s kernel `admin_timeout` §3.4 itself cites, and no timeout, own thread or cadence budget is given. A read blocking >15 s starves `print speed`, marks the socket stale, drops TempLoop to RpmLoop and raises `FANCTRL LOST` while fw-fanctrl is healthy — during heavy I/O, the regime the reading exists to report on — and re-entry costs the 3-tick hysteresis. fwloop.9's 'delays no Sample' criterion passes (Samples read a cached view) while the mode flip occurs; no §5/fwloop.17 run scripts a slow read against freshness; whether the shared mutex is held across the read is unstated. (new; second-order consequence of iteration-2 R11, which closed the Sample-cadence stall it targeted)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: §2.1 gives every socket op explicit connect 1 s / read 3 s timeouts precisely so the poller's cadence is bounded; the NVMe read on the same thread gets a 30 s cadence and its own staleness rule but no timeout (grep for 'timeout' finds only the socket client's). Serialised, a 60 s read skips twelve `print speed` polls; §2.5's stale-exit is immediate (only the argmax condition is debounced). Ground's spec-wide grep for 'block' returns only fwloop.9's own acceptance. Should-fix, not Blocking: RpmLoop is a designed, functioning fallback and the caps are not left applied.
  fix-shape hint: Give the NVMe read its own thread or a bounded-timeout wrapper with a cached last-good value; state the mutex is never held across it; add a fwloop.9 criterion and fwloop.17 fault run where the read blocks 60 s asserting Freshness stays Fresh, Speed cadence holds, mode stays TempLoop, FANCTRL LOST not raised.

- [Should-fix] §3.3 step 3 lines 520-521 ('θ_eff = θ + ma_interval/2 for the EC fit ... Kc = τ/(K·(λ + θ_eff))') vs §2.4 lines 234-237 and fwloop.21 body ('θ taken as fitted'); fwloop.21 acceptance ('derived gains match the IMC formulas (with θ_eff)'); fwloop.13; fwloop.17 calibration run — the boxcar double-count iteration 2 confirmed is fixed in §2.4 and fwloop.21's body but left verbatim in §3.3 step 3 — the prose fwloop.13's StepTest implements — and fwloop.21's own acceptance still demands the discarded formula. Two rows contradict each other by name, and fwloop.17's ±25 % calibration criterion resolves differently depending on which is implemented. (regressed — iteration-2 finding #6 / R6 fixed incompletely; the shortfall is in the current text at lines 520 and 694)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: grep for `ma_interval/2` returns exactly two live sites: line 236 (§2.4 warning that adding it again 'would count the boxcar twice and roughly halve the fitted Kc') and line 520 (§3.3 instructing exactly that addition). Refute checked `git show 9bce3d4` (iteration-2 apply commit, message 'theta_eff no longer double-counted on the fit path'): the diff touches §2.4 and fwloop.21's row but has zero hits on the §3.3 step-3 text. On the spec's plant (θ̂ ≈ 42 s, τ̂ ≈ 44.5 s) the two rules give Kc ≈ 0.19 vs ≈ 0.33 — a ~1.7× gap outside fwloop.17's 25 % band.
  fix-shape hint: Delete `+ ma_interval/2` from §3.3 step 3 (keep it only in §2.4's defaults derivation) and change fwloop.21's acceptance from 'with θ_eff' to 'with the fitted θ̂ used as-is'.

- [Should-fix] §2.8 dGPU guard ('gpu_hot_c, default 90, exit 85 ... derived, not guessed'); §Facts 'dGPU thermals (NVML)'; fwloop.6 `exit = enter − 5`; fwloop.17 '5 min GPU HOT episode at the 90 °C threshold' — the 90/85 pair was derived from one of the four thermal specifications NVML publishes for this card. `GPU Current T.Limit Temp` + `GPU Current Temp` = 87 °C invariantly, and the published offsets (Slowdown −2, Shutdown −5) place software slowdown at 87 °C, hardware slowdown at 89 °C and hardware shutdown at 92 °C. Entry at 90 is above the card's hardware slowdown, so the guard engages only after both hardware protections already have, with authority limited to the 90-92 °C residue; exit at 85 sits 2 °C below the card's own sustained-load setpoint, so once fired it cannot clear until it has ratcheted the GPU below the point the card itself targets — the 'latches and ratchets to the floor' failure the 83/78 → 90/85 revision was written to remove. (regressed — iteration-1 finding #15 fixed incompletely on the exit side; evidence is the current §2.8 text against the full NVML spec set)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: Reproduce and refute both ran `nvidia-smi -q -d TEMPERATURE` live on the machine (2026-09-09): Current 41/40 + T.Limit 46/47 = 87, Shutdown T.Limit Spec −5, Slowdown T.Limit Spec −2, Max Operating 0, Target 87 — the same single command whose output §Facts recorded only one line of. Ground corroborated T.Limit semantics via NVIDIA developer-forum threads (T.Limit 0 = SW slowdown, +2 HW slowdown, +5 HW shutdown), matching this card's −2/−5 exactly. §2.8's own rationale sentence ('90 fires only when the card is hotter than its own loop intends') is false relative to the 89 °C hardware slowdown. Should-fix: NVIDIA's own slowdown/shutdown protections remain independent, so this is guard effectiveness and latch/hunt risk, not unmitigated hardware risk.
  fix-shape hint: Record all four NVML specs in §Facts; key the guard on the published T.Limit margin (or set entry below 87 with exit above where the card's loop parks); log clocks_event_reasons through one sustained gaming session to confirm when SW/HW slowdown first assert.

- [Should-fix] §2.4 'Mode B's gain is scheduled on the live curve'; §2.5 'RpmLoop under active: false ... checks that the loop does not hunt'; §Facts ('Either regime is far from the plant Mode B's gain was identified against'); fwloop.4 `scale_rpm_gain(slope: Option<f64>)`, fwloop.10 `Decision.slope`, fwloop.17 steep-segment run — under `active: false` with a fresh socket the Mode B gain schedule keys on fw-fanctrl's curve while the EC's own staircase is the plant, two objects with no causal relation in that regime. On quiet16 slope_at(T*) ≈ 0.86 %/°C < slope_ref = 1.0, so `scale_rpm_gain` returns its LARGEST multiplier (1×) for the plant §Facts measures as the most sensitive (140-420 RPM/°C ≈ 112-336 RPM/W against the 62.4 RPM/W the default was derived for — up to 5.4× loop gain); the schedule can only reduce gain and its floor needs a curve four times steeper than the reference, which has no bearing on the EC plant. (new; the iteration-2 `None ⇒ 0.25×` fallback covers only no-curve entry paths — active:false with a resolving socket is not among §2.4's three enumerated None cases)
  verdict: confirmed (reproduce ✗-rejected / refute ✓ / ground ✓)
  evidence: Refute and ground both re-derived the arithmetic from the spec's own numbers: (37−31)/(82−75) = 0.857, max(0.857, 1.0) = 1.0 ⇒ 1×; K_rpm = 78·0.8 = 62.4 (§2.4); §Facts' T* ≈ 75 on quiet16 is the machine's real operating point, not a contrived case; RpmLoop's only §2.5 gate is fan_valid, not active. Reproduce (rejected) argued this restates iteration-1 R21's 'do not fight it' resolution — that holds for the flat 67-73 °C band (floor + `TARGET UNREACHABLE (low)`, an explicit accepted tradeoff), but the steep band below 64 °C is defended only by a pass/fail 'does not hunt' run graded against the plant of the next finding, and nothing in §2.4/§2.5 conditions the schedule's input on `active`. Panel arithmetic (2/3) stands; the flat-band half is not re-litigated.
  fix-shape hint: When the socket reports active: false (or Absent), treat the scheduling input as unknown and apply the conservative 0.25× clamp exactly as the None path does; keep the fwloop.17 hunt run as the check, not the mechanism.

- [Should-fix] §Facts 'Measured 2026-09-08, the EC's own autofan curve'; fwloop.16 ('an EC-autofan mode ... with the measured staircase of §Facts'); fwloop.17 'with the EC on its steep segment below 64 °C the loop does not hunt'; docs/research/2026-09-08-ec-autofan-probe.csv — the non-flat part of the EC-autofan staircase baked into the `active: false` / `absent` plant (4096 RPM at 61-62 °C, 4520 at 63, 4658 at 64) comes entirely from the probe's 148 s `ec_decay` phase — a single falling transient with n = 25/28/16/4 samples and no rising-branch data below 67 °C. A static RPM(T) map from one monotone cool-down cannot separate the EC's curve from fan spin-down, ramp limiting or hysteresis, so the only acceptance run defending Mode B stability in that regime is graded against a model that omits the standard cause of hunting on a thermostat-like actuator. (new; iteration-1 R21's probe was the entire resolution of the 'never graded' finding — its provenance was never examined)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: All three seats regrouped the 300-row CSV by phase × integer ec_max_c independently: every 61-64 °C sample is ec_decay (t 450.3-598.3 s); ec_idle (t 60-148) and ec_load (t 150-448) never visit below 67 °C; the decay branch reads 4681 RPM at 67 and 71 °C vs the rising branch's 4726-4748 — the two directions already disagree; 62 °C spans 4028-4658 RPM (a 630 RPM spread at one nominal temperature). Spec grep for hysteresis/rising/falling finds only the mode-arbiter dwell timer; fwloop.16's EC-autofan mode is 'driving RPM straight from EC temperature with the measured staircase' with no directional term. Ground corroborated via bang-bang/hysteresis control references that hysteresis is the standard anti-hunting mechanism such actuators carry. The flat 67-73 °C band (n≈180) carrying §2.5's no-authority conclusion is unaffected.
  fix-shape hint: With fw-fanctrl stopped, ramp CPU load slowly 55→75 °C logging EC max and RPM at 1 Hz, then decay; overlay the branches to get the rising curve and hysteresis width; give fwloop.16's EC-autofan mode a rising/falling branch (or a measured hysteresis band) before the fwloop.17 hunt run is graded.

## Not verified (beyond panel cap)
- none

## Beyond remainder cap (count only)
- none

## Rejected (with reason)
- fwloop.10 vs fwloop.12 ownership collision on the EC MISMATCH counters (ArbiterInput lacks replica slope / stamp gap) — reject 2/3: iteration-2 R3's split is explicit in both rows — fwloop.10's ArbiterInput entry reads 'the reconciliation counters the controller scores at 1 Hz (§2.6 — this row owns their meaning, not their sampling rate)' and fwloop.12 owns 'the 1 Hz reconciliation scoring (§2.6) whose counters the arbiter consumes'; the controller performs the skip-rule/MA arithmetic from Sample fields it already has, so the slope and gap never need to cross ArbiterInput, and the two acceptances test different layers (transition logic on mocked counters vs cadence-correct scoring). Ground's point stands as a copy-edit: fwloop.10's opening prose ('implementing ... EC MISMATCH counters ... including the skip rule ... and the moving-average check') predates R3 and should be reworded to 'consuming'.
- fwloop.16 never states ThermalPlant integrates DRAWN watts rather than the commanded cap — reject 2/3: fwloop.16's own clause 'without this the plant cannot exercise the demand-starved wind-up at all' is only true if the thermal path consumes the demand model's output, and fwloop.4/fwloop.12 unit-test `Freeze::DemandLimited` on synthetic (draw, cap) tuples with no plant at all, so the mechanism is not solely dependent on fwloop.17's fidelity. One explicit clause ('ThermalPlant consumes the demand model's drawn watts, not budget_w') is a worthwhile copy-edit given this mechanism's history.
- `Freeze::DemandLimited` surfaces only in decision telemetry, no StatusFlag / §3.5 header segment — reject 2/3: §2.4 line 227 names decision telemetry as Freeze's reporting channel by design, `Calibrating` is likewise flag-less (surfaced via CalibProgressLite), §2.5's 'must not silently sit' principle is textually bound to the floor case that `TARGET UNREACHABLE (low)` already covers, and the held `budget_w` is visible in the header every tick. The one case where a DemandLimited hold IS a silent stuck loop is the confirmed Blocking direction-blind latch above; its fix removes the latch, and the fix-shape hint there already asks for the freeze reason in the header while a hold is active.

## Unverified nits (spot-checked)
- [Nit] fwloop.17 has grown to ~33-35 graded scenarios and its '2 strategies × 3 modes, dGPU on/off, default vs fitted' cross-product names 8 vacuous Released cells — spot-check rejected (the row's body enumerates Released exactly once with no strategy/dGPU/gains parameterisation; 'each spec-enumerated configuration' reads as a coverage-axis checklist over the runs listed, not a fresh Cartesian expansion; no implementer would construct the vacuous cells).
- [Nit] NVMe reporting-only guard keeps the full guard apparatus and three tiers of tests (fwloop.6/12/17) for a mechanism that takes no action — spot-check rejected (threshold, hysteresis and TUI/telemetry surfaces are the reporting mechanism itself; Guards::step is shared with the GPU guard; the 30 s poll/stamp is justified by the admin_timeout hardware fact; the same unit/controller/closed-loop tiering is applied uniformly to the active GPU guard).
- [Nit] No criterion asserts fitted gains outperform the shipped defaults; every rejection path falls back to defaults — spot-check rejected (§6 explicitly bounds in-tree evidence to the perturbed chained plant and defers differentiating evidence to field validation; fwloop.21's acceptance verifies the fit mechanism itself; the Goal never commits to comparative superiority; fwloop.21 lists four rejection rules, not five — the fifth is a §3.3 runner skip gate).

## Escalations (need human)
- none
