---
super-roast verdict: Blocking (18 confirmed)
mode: PR        iteration: 2 of 3
profile (assumed): Single-operator personal tooling with hardware side-effects: a Rust daemon on one Framework 16 laptop that writes CPU power limits via ryzenadj (sudo) and dGPU clock locks via NVML, and reads the EC through cros_ec/ectool. No network surface, no other users, no external data. Blast radius is the operator's own machine: a wrong cap or a stuck emergency release degrades performance or acoustics and, at worst, lets the hardware's own thermal protection (card slowdown 89 C / shutdown 92 C; EC trip points) take over — the hardware backstops exist and are documented in the design. Rollback is a git revert and a daemon restart. Thermal-safety inversions, silent loss of control (NaN/latched states), and anything that defeats the hardware backstops are the material class; resilience/observability polish is low-value here.
inputs: epic-fw-fanctrl-loop-6ma-integration@737b26a vs master@10a730f
delta vs prior: 4 new confirmed (1 Blocking) · 11 carried (0 Blocking) · 7 resolved · 3 regressed (0 Blocking)
coverage: triage live, 13 scouts ran (0 dead), dedupe live · 7 raw → 7 deduped → 7 panel / 0 spot-checked · judge completion 100% · remainder-capped: 0
independence: same-family (Claude) — seat-differentiated panel
seat-agreement: panels 7 · rr 0.86 · rg 1.00 · fg 0.86 · unanimous 0.86 · ground-loo 1.00 (n=6) · reproduce 7/0/0 · refute 6/1/0 · ground 7/0/0

Prior-report disposition (iteration 1, 21 confirmed): fix merges 566d92b / 7b7ff1f / fb09a12 / 737b26a (source commits 4449a2e, 1ff98d1, c388aaa, 619bf8c) answered findings 1–10. **Resolved (7):** #1 fanctrl mutex held across socket I/O (poller now owns the source, publishes a `FanctrlSnapshot`; `hung_fanctrl_socket_does_not_stall_the_sampler`), #2 `send` without total deadline/size cap (`Read::take` + `Instant` deadline), #3 poisoned-mutex fail-stop (`sync_util::lock`, `thread::Builder::spawn`), #4 untimed ryzenadj + unbounded join (`run_with_timeout`/`RUN_TIMEOUT`, `join_with_timeout`), #5 `signum` on zero error (`sign3`), #6 hysteresis at the wrong cadence (seconds + `ticks_for`), #9 strict `print all` strategies (`serde_json::Value`, only the active entry typed). No scout re-surfaced any of them. **Regressed / fixed incompletely (3):** #7 state validation (two of four fields), #8 `gpu_hot_c` sanitizer (floor does not exclude the latching range), #10 GPU leg in acceptance sims (`Unverifiable` still never reached) — each carried below as its own entry. **Still-open (11):** the iteration-1 Nits (#11–#21) — the fix diff `c5fa02c..737b26a` touches none of README.md, curve.rs, selftest.rs, the `freeze: None` emitter, the slope divisor, or `Decision.reasons` (verified by grep on 737b26a); they were not re-scouted this round and are carried verbatim below. Three of the four new findings are side-effects of the finding-4 and finding-6 fixes themselves.

## Confirmed findings

- [Blocking] src/control/controller.rs:1503-1511; src/control/controller.rs:2245-2253; src/control/budget.rs:276-281,413 — The Auto/Calibrating budget bounds `lo = cpu_floor_w + lut.watts_for_clock(gpu_floor_mhz)` and `hi = cpu_max_w + gpu_max_w` are never ordered against each other, so an operator config with a lowered `gpu_max_w` plus a raised GPU clock floor (settable live from the TUI) makes `Budget::set_bounds`'s `self.u.clamp(lo, hi)` panic the controller thread in release builds. (new)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: controller.rs:1503-1511 computes lo/hi and calls `auto.budget.set_bounds(lo, hi)` with no ordering check (duplicated in `calib_bounds` 2245-2253 → `w.clamp(lo, hi)` at 2362); budget.rs:276-281 guards only with `debug_assert!(lo <= hi)` then `self.u.clamp(lo, hi)`; `step` at 413 likewise. All three seats compiled `50.0f64.clamp(105.0, 74.0)` with `rustc -O`: `panicked … min > max, or either was NaN. min = 105.0, max = 74.0`, exit 101. `Config::sanitized` (config.rs:179-268, extended by 4449a2e) clamps `gpu_floor_mhz` to [1000, 3090] and `gpu_max_w` to [1.0, 100.0] independently and never cross-checks, despite its own doc "must degrade to a sane floor, never panic the control loop downstream (`f64::clamp` with min > max panics)". `SWEEP_CLOCKS[0] = 3090` and the LUT unit test give `watts_for_clock(3090)` ≈ 103 W, so default `cpu_floor_w` 15 + ~95 = ~110 > `cpu_max_w` 54 + `gpu_max_w` 40 = 94. `Command::SetFloors` (controller.rs:1061-1070) applies TUI floor keys through the same `sanitized()`; model.rs:209-213 clamps them only to the MHz range. gpu_pid.rs:117-119 already defends the identical panic class ("Cap it here so no caller can crash the control loop") — `Budget` has no equivalent. `budget.rs` is absent on master (`git cat-file -e master:src/control/budget.rs`), so entirely new. `RestoreGuard` unwinds and restores stock, but the daemon dies on an in-range operator config — a core-purpose failure, Blocking under this profile.
  fix-shape hint: make `Budget::set_bounds`/`seed`/`step` release-safe (`if lo > hi { warn; hi = lo }` or `lo = lo.min(hi)`) and add a cross-field clamp in `Config::sanitized`/`SetFloors` that lowers `gpu_floor_mhz` (via the LUT) or raises `gpu_max_w` until `lo <= hi`; one controller test that raises the floor live against a soft-capped `gpu_max_w`.

- [Should-fix] src/main.rs:381-388; src/control/controller.rs:2728-2734; src/actuators/guard.rs:161-196 — After the new bounded controller join expires, the abandoned controller thread has no stop fence: if it unwedges (NVML is the only unbounded call left) it can service a still-queued `Sample` — running a fresh `on_sample` including `cpu.set_sustained_mw` — after `FinalRestore::drop` has restored stock, so the last write before process exit can be a CPU cap that `ryzenadj` leaves in place. (new; side-effect of the finding-4 fix)
  verdict: confirmed (reproduce ✓ / refute ✗ (rejected: in-flight ryzenadj write is 5 s-bounded) / ground ✓)
  evidence: 4449a2e replaced master's unbounded `ctl.join()` with `join_with_timeout(ctl, CONTROLLER_JOIN_TIMEOUT)` (20 s); on `TimedOut` main logs and continues (main.rs:381-388), the `JoinHandle` is consumed and nothing ever stops the thread. `controller::spawn` (controller.rs:2728-2734) receives no `shutdown` flag — the only `AtomicBool` it touches is `restored`; the `select!` loop (2744-2785) services whichever of `sample_rx`/`cmd_rx` is ready and only exits on `Quit`/disconnect. `gpu.rs` `set_max_clock`/`release` call `nvml_wrapper` directly with no timeout (the "wedged somewhere with no timeout of its own" case main.rs:327-331 names). Sampler exits within one period of `shutdown`, leaving up to a few queued `Sample`s. `FinalRestore::drop` (guard.rs:161-196) then restores; main returns and kills the thread (ground seat: matklad "Join Your Threads"). guard.rs:7-17 scopes the double-restore analysis to the panic path and only to two restores racing, not to a cap landing after the final restore. **Dissent resolved:** the refute seat correctly shows the *in-flight* `ryzenadj` write cannot be the straggler (it is `RUN_TIMEOUT`-bounded and runs before the NVML call, cmd.rs:35, controller.rs:1598/1601) — but the reproduce seat's mechanism is the *next* queued `on_sample` after the NVML wedge clears, which the refute evidence does not address; the claim is confirmed in that narrowed form. Self-heals only on the next launch via `guard.startup_reset()` (main.rs:180).
  fix-shape hint: pass the `shutdown` `Arc<AtomicBool>` into `controller::spawn` and check it at the top of the sample arm (and before each actuator write) so an orphaned thread never issues a cap once shutdown began; alternatively drain `sample_rx` before sending `Quit`.

- [Should-fix] src/main.rs:332; src/control/controller.rs:1977-1980; src/actuators/cpu.rs:118-138; src/actuators/cmd.rs:35 — `CONTROLLER_JOIN_TIMEOUT` (20 s) is not larger than one fully-bounded `on_sample` CPU write path (2 × `set_sustained_mw` × 2 runner calls × `RUN_TIMEOUT` 5 s = 20 s) plus the restore that follows (`modprobe ryzen_smu`, another 5 s), so the join can expire with every external command honouring its deadline — contradicting the constant's own doc ("only bites when the controller thread is wedged somewhere with no timeout of its own") and opening the finding above on a merely slow SMU mailbox. (new; side-effect of the finding-4 fix)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: controller.rs:1977-1980 calls `cpu.set_sustained_mw` twice on `Mismatch` when not suppressed; cpu.rs:118/129→138 each call is a write plus `verify_write` → `run("ryzenadj", ["--info"])`; cmd.rs:35 `RUN_TIMEOUT = 5 s`; the `select!` loop (2753-2791) only sees `Quit` after `on_sample` returns; `restore_all` (guard.rs:88-103) → `smu.restore` → `runner.run("modprobe", …)` (smu_module.rs:75) up to 5 s more plus the ~400 ms profile toggle and untimed `gpu.release()`. Worst case ≈ 25.4 s > 20 s. Both constants were introduced by 4449a2e and never reconciled; the Mismatch retry predates that commit. Consequence bounded by `FinalRestore`'s Drop, so Should-fix not Blocking; kept at Should-fix rather than demoted because it is the realistic trigger for the abandoned-thread window above.
  fix-shape hint: size `CONTROLLER_JOIN_TIMEOUT` from the bounded worst case (e.g. `5 × RUN_TIMEOUT + margin`) and correct the doc comment, or skip the Mismatch re-write and `verify_write` once `Quit` is pending.

- [Should-fix] src/state.rs:93-110; src/control/controller.rs:895,899,1503-1509,1856-1864; src/control/budget.rs:280,288,485 — `PersistedState::validated` (the finding-7 fix) checks only `duty_rpm_table` and `loop_gains`; `lut` and `warm_start` are loaded verbatim and reach the same sinks: a non-finite LUT watts entry makes `Budget::set_bounds` panic in `f64::clamp`, and a non-finite `warm_start` value seeds `u`/`v` to NaN permanently. (regressed — iteration-1 finding #7 fixed incompletely)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: state.rs:93-110 tests exactly two fields; struct has four (state.rs:30-45); the seven new tests target the same two; `ClockWattsLut` has no `is_valid` and `watts_for_clock` (lut.rs:88-107) returns stored watts unchecked → controller.rs:1503-1509 → budget.rs:280 `self.u.clamp(lo, hi)`. Seats compiled `50.0f64.clamp(f64::INFINITY, 145.0)` → panics (exit 101, release build). serde_json parses `1e400` as `inf` — the branch's own test `non_finite_duty_rpm_table_entry_falls_back_to_the_seed` (state.rs:296-311) uses that exact trick, so `{"lut":{"points":[[1000,1e400]]}}` is an input shape the authors already treat as reachable. `WarmStart::lookup` (budget.rs:485, plain `map.get().copied()`) → `seed` (budget.rs:288 `u.clamp(lo,hi)`; NaN survives) → controller.rs:1962 saturates to 0 mW and 1969 `!= Some(NaN)` re-writes every tick. Module doc state.rs:5 "Loading NEVER crashes"; `validated()`'s own doc names "hand-edited or schema-skewed state.json" as the threat. NaN loss-of-control is the profile's named material class; not Blocking because the writer is this daemon (root-owned file, edit/corruption required).
  fix-shape hint: add `ClockWattsLut::is_valid` (non-empty, finite, monotone) and a finiteness filter on `warm_start` values inside `validated()`, dropping the bad field to "not calibrated"/empty with a warn; one negative test per field mirroring the existing seven.

- [Should-fix] src/config.rs:22-28; docs/superpowers/specs/2026-09-07-fw-fanctrl-loop-design.md §2.8; src/control/guards.rs:48 — The finding-8 fix's `GPU_HOT_C_FLOOR = 60.0` and its rationale ("cruises in the 60s–70s under load") contradict the spec's and guards.rs's own measured numbers (die settles at 82–83 °C under sustained load, parks at 87), so any operator `gpu_hot_c` in ~60–84 passes `sanitized()` unchanged and latches `GPU HOT` for the whole session, ratcheting the GPU share to its floor — the failure the floor was added to exclude. (regressed — iteration-1 finding #8 fixed incompletely)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: config.rs:22-26 constant + comment; config.rs:241 `gpu_hot_c.clamp(GPU_HOT_C_FLOOR, GPU_HOT_C_CEIL)`; test `in_range_guard_thresholds_are_left_alone_by_sanitized` (config.rs:564-576) pins 84.0 passing untouched; the floor test exercises only {−10, 0, 45, 59.9}. Design doc lines 64/75/645-647 and guards.rs:10-18: "under a 100 W gpu-burn the die settles at 82–83 °C on `quiet16` with the fans free", park point 87; no spec text supports "60s–70s under load". guards.rs `hysteresis()` (102-112) enters at `t >= enter`, clears only at `t <= enter − 2` (GPU_HYSTERESIS_C, :48); `gpu_share_override` (118-120) ratchets `(current − DOWN_RATE_W).max(floor)` each tick. Commit 4449a2e's own message: "Below the card's cruising range the guard would latch hot forever". Distinct from iteration-1 #8 (keys bypassing `sanitized()`), which is otherwise fixed. Latched state is the profile's material class; default 88 unaffected, so Should-fix.
  fix-shape hint: raise the floor to just above the measured sustained-load band (e.g. `GPU_HOT_C_FLOOR = 85.0`, i.e. park − 2) and rewrite the comment to cite §2.8's 82–83/87 figures; add a sanitizer test at a value inside 60–84.

- [Nit] src/control/controller.rs:64,1558,2334; src/sensors/sampler.rs:31; src/control/mode.rs:128 — The finding-6 fix re-creates the cadence coupling one layer up: the sampler's real 1 Hz period (`sampler::SAMPLE_PERIOD`, private) and the controller's `SAMPLE_PERIOD_S` (private) that §2.5/§2.7's 15 s / 60 s hysteresis are derived from via `ticks_for` are two unlinked constants with no test asserting agreement, while `ArbiterInput` already carries real `Duration` fields (`at_lower_bound_for`/`at_upper_bound_for`) for its other spec-in-seconds timer. (new; side-effect of the finding-6 fix)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: controller.rs:64 `const SAMPLE_PERIOD_S: f64 = 1.0` (new in 619bf8c) → `ArbiterInput.sample_period_s` at 1558/2334; sampler.rs:31 `const SAMPLE_PERIOD: Duration = Duration::from_secs(1)` used only for the sleep at :281; neither `pub`, no import, `main.rs:229` threads no period through. The only test (controller.rs:5332-5335) checks `SAMPLE_PERIOD_S` against itself. `budget.rs:54 pub const PI_PERIOD_S` is the project's own correct pattern, reused by mode.rs:590. Changing the sampler cadence silently rescales both spec timers with the suite green; call-counting also makes the timers not wall-clock under a gapped stream.
  demoted: profile states a single-operator tool with git-revert rollback and both constants agree today (1.0 s), so a latent drift hazard with no present behavioural effect is a Nit here.
  fix-shape hint: make `sensors::sampler::SAMPLE_PERIOD` `pub(crate)` and derive `SAMPLE_PERIOD_S` from it (`.as_secs_f64()`), or measure the two timers from `Sample.t_mono` the way `BOUND_HOLD` already is.

- [Should-fix] src/control/sim_tests.rs:2059,2240; src/actuators/gpu.rs:76; docs/superpowers/specs/2026-09-07-fw-fanctrl-loop-design.md:958 — Commit 1ff98d1 closes only the Verified/Mismatch legs of `verify_lock`: both GPU-attached closed-loop runs script `gpu_util_pct = 95.0`, and the two low-utilisation runs are `with_gpu: false`, so the sub-90 % `WriteVerdict::Unverifiable` branch — the verdict fwloop.17's acceptance names for the dGPU-unpowered run — is still never reached from any plant-closed simulation. (regressed — iteration-1 finding #10 fixed incompletely)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: `grep -c Unverifiable src/control/sim_tests.rs` = 0 on 737b26a, unchanged; sim_tests.rs:2059/2240 "above verify_lock's 90 % floor" inside the only two `build_controller(…, true)` runs (2079 via `run_gpu_heavy`, 2225); sim_tests.rs:1838/2639 script 20 % but pass `with_gpu: false`, and `run_gpu_pi` (controller.rs:2032-2036) gates write+verify on `Some(gpu)`; gpu.rs:71-75 returns `Unverifiable` only at `gpu_util <= VERIFY_UTIL_FLOOR_PCT`. The test named `a_dgpu_unpowered_run_…` (1997) asserts `GpuSet` never fires, so it cannot produce the verdict by construction. Design doc:958: "a dGPU-unpowered run (no `GPU HOT`, floor honoured, `verify_lock` `Unverifiable`)"; the iteration-1 fix-shape hint named the unpowered scenario with `with_gpu: true` explicitly; 1ff98d1's message claims only the Verified→Mismatch walk. Test-only, so Should-fix not Blocking.
  fix-shape hint: add one `with_gpu: true` + `gpu_lut` run with `gpu_util_pct` ≤ 90 asserting `verify_lock` returns `Unverifiable`, no `GPU HOT`, and the floor honoured.

Carried from iteration 1 (still-open, not re-scouted this round; the fix diff c5fa02c..737b26a touches none of these sites — verified by grep on 737b26a):

- [Nit] src/fanctrl/curve.rs:63 — `Curve::from_points` validates duty order only, not temperature order (still-open, see iteration 1). fix-shape hint: second `CurveError` variant for `w[1].0 < w[0].0`.
- [Nit] src/control/controller.rs:1528 (was 1497); :2300 — §2.6 reconciliation slope divides by `len − 1` samples, not seconds, while the window is pushed only when `s.ec` is `Some` (over-skips on EC dropout) (still-open, see iteration 1). fix-shape hint: store `(t_mono, max_c)` and divide by the time span; share with the calib copy.
- [Nit] README.md:71,168,176,204; src/config.rs:55; src/control/controller.rs:248 — docs still carry pre-a78 thresholds 90/85/87 vs shipped 88/86/91 (still-open, see iteration 1; README.md:71/168/176 re-checked on 737b26a). fix-shape hint: search-and-replace to 88/86/91/"− 2", including design doc §2.8 header.
- [Nit] src/sensors/poller.rs:104,109; src/fanctrl/client.rs — fw-fanctrl socket failures discarded without any log line (`let _ = self.source.poll(...)` survives the finding-1 rewrite; `grep tracing src/fanctrl/*.rs` still empty) (still-open, see iteration 1). fix-shape hint: edge-triggered `tracing::warn!` on transition into failure.
- [Nit] src/control/controller.rs:2871 (was 2835) — `decision` telemetry `freeze` column hardcoded `None` (still-open, see iteration 1). fix-shape hint: destructure `freeze` from `Effect::AutoAllocated` into the record.
- [Nit] src/control/controller.rs (decision emitter) — `mode` column carries controller `Mode`, never `LoopMode` (still-open, see iteration 1). fix-shape hint: add a `loop_mode` column.
- [Nit] src/control/mode.rs:261,487-512; src/control/controller.rs — `Decision.reasons` has no production consumer (`grep '\.reasons' src/` outside mode.rs still empty) despite design doc:276 (still-open, see iteration 1). fix-shape hint: `reason` column or StatusFlags; reconcile the spec.
- [Nit] src/selftest.rs — no fw-fanctrl socket / `cros_ec` preflight (still-open, see iteration 1). fix-shape hint: two informational selftest steps.
- [Nit] src/control/controller.rs (save_persisted_state); src/state.rs — one-way state.json migration, no `.bak` (still-open, see iteration 1). fix-shape hint: write `state.json.bak` once; document.
- [Nit] README.md:248-255 — Status paragraph says the closed loop is not wired (still-open, see iteration 1). fix-shape hint: replace with the shipped state.
- [Nit] README.md:15-24 — Requirements never state fw-fanctrl must be running (still-open, see iteration 1). fix-shape hint: one prerequisite bullet.

## Not verified (beyond panel cap)
- none

## Beyond remainder cap (count only)
- none

## Rejected (with reason)
- No panel finding was rejected this iteration. The iteration-1 rejections stand unchanged and were not re-surfaced: poller `+= PERIOD` catch-up burst (clock semantics — see Escalations); leaked connect thread per timeout (documented std-only tradeoff, client.rs:320-330); released actuator re-commanded every tick (design §2.9 mandates it); hard-freeze→DemandLimited skipping `resync_error` (bespoke resync path); guard fails open on `None` / CPU-only sensor-lost (spec'd / pre-existing); unversioned socket JSON (degrades to designed `FANCTRL LOST`); band residency graded against `snapped_rpm` (design doc:17); `GPU_TRIP_C` unpinned (sim_tests.rs 88 °C run pins it). None of the fix commits changed the code or spec text those rejections rested on, so no reconsideration applies.

## Unverified nits (spot-checked)
- No spot-tier packets this iteration (7 raw → 7 panel). The 23 iteration-1 spot-survived nits (allocator `.min(max)` after quantise; committed `sudo.txt` path; unpinned `bd` hooks; agent tooling in the functional branch; duplicated EC-replica pipeline; spike-harness private constant copies; flag severity encoded three times; 39 `self.auto.as_*().expect` re-borrows; `Coverage<T>` self-asserting checklist; dGPU-unpowered test holds by construction; four `Xorshift32` copies; `run_gpu_pi` LUT clone; double-parsed `print all` body; duplicated NVMe field in the v2 sample line; indistinguishable `TargetUnreachable` causes; unlogged missing `cros_ec`; silently-ignored misspelled `fanctrl_socket`; real-clock read-timeout test race; demand-starved test tolerance; stale `float_roundtrip` justification; §2.8 header 5 °C/90/85; stale TODO.md; README Development section) remain as recorded there — note that 1ff98d1 replaced the single scripted `dgpu_powered` tick with a plant-closed run, and c388aaa's `parse_print_all` rewrite may have changed the double-parse; neither was re-checked this round.

## Escalations (need human)
- Instant/suspend clock semantics (carried from iteration 1, untouched by the fix commits): design doc:270-271 still says "a monotonic clock does not advance across suspend" while Cargo.toml:5 and sampler.rs:104 say Linux `Instant` is CLOCK_BOOTTIME-backed since 1.87 and advances during suspend; `is_resume_gap` and the resume-reassert path depend on which is true — a human should verify against the pinned rustc and correct whichever side is wrong (and re-open the poller catch-up-burst finding if BOOTTIME is real).
---
