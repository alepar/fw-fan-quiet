---
super-roast verdict: Blocking (20 confirmed)
mode: PR        iteration: 3 of 3
profile (assumed): Single-operator personal tooling with hardware side-effects: a Rust daemon on one Framework 16 laptop that writes CPU power limits via ryzenadj (sudo) and dGPU clock locks via NVML, and reads the EC through cros_ec/ectool. No network surface, no other users, no external data. Blast radius is the operator's own machine: a wrong cap or a stuck emergency release degrades performance or acoustics and, at worst, lets the hardware's own thermal protection (card slowdown 89 C / shutdown 92 C; EC trip points) take over — the hardware backstops exist and are documented in the design. Rollback is a git revert and a daemon restart. Thermal-safety inversions, silent loss of control (NaN/latched states), and anything that defeats the hardware backstops are the material class; resilience/observability polish is low-value here.
inputs: epic-fw-fanctrl-loop-6ma-integration@4aeceea vs master@10a730f
delta vs prior: 7 new confirmed (0 Blocking) · 11 carried (0 Blocking) · 5 resolved · 2 regressed (1 Blocking)
coverage: triage live, 12 scouts ran (0 dead), dedupe live · 14 raw → 10 deduped → 10 panel / 0 spot-checked · judge completion 100% · remainder-capped: 0
independence: same-family (Claude) — seat-differentiated panel
seat-agreement: panels 10 · rr 0.90 · rg 0.90 · fg 0.80 · unanimous 0.80 · ground-loo 0.89 (n=9) · reproduce 9/1/0 · refute 8/2/0 · ground 10/0/0

Prior-report disposition (iteration 2, 18 confirmed): fix merges 8c9cf0b / 112a966 / 7b14c83 / 4aeceea (source commits e80fb20, 70ef858, 8a7323c, 15bc98f) on top of 28904cf answered findings 1–7. **Resolved (5):** #3 `CONTROLLER_JOIN_TIMEOUT` sizing (now derived, `5 × RUN_TIMEOUT + margin`, main.rs:327-346), #4 `lut`/`warm_start` validation (`ClockWattsLut::is_valid` lut.rs:57 with three negative tests; `WarmStart::drop_non_finite` state.rs:126 — premise correction carried: this crate's serde_json `float_roundtrip` rejects `1e400` as out of range rather than parsing it as inf, so the iteration-2 reproduction trick was wrong; the finiteness guards were kept as defence in depth, which is the right call), #5 `GPU_HOT_C_FLOOR` (60 → 85, config.rs:32, park − 2 as the hint asked), #6 sampler/controller cadence coupling (`sampler::SAMPLE_PERIOD` is `pub(crate)` and `SAMPLE_PERIOD_S` is derived from it, sampler.rs:35 / controller.rs:66), #7 `verify_lock` `Unverifiable` leg (`grep -c Unverifiable src/control/sim_tests.rs` = 8 on 4aeceea, from 0). No scout re-surfaced any of them. **Regressed / fixed incompletely (2):** #1 budget-bound inversion — the release `f64::clamp` panic is closed by `ordered_bounds` + `budget_bounds`, but the cross-field invariant the fix introduced (`with_lut_floor_clamp`) is not re-run on the one path where construction had no LUT to clamp against, so the first calibration re-creates the inversion and the new fallback pins the budget at the cap for the session (carried below as the Blocking entry); #2 orphaned-thread stop fence — added, but the primary Auto CPU write it was built to fence is the one write it misses (carried below). **Still-open (11):** the iteration-1 Nits — the fix diff 28904cf..4aeceea touches only config.rs, budget.rs, controller.rs, lut.rs, sim_tests.rs, integration_tests.rs, main.rs, sampler.rs and state.rs; README.md, curve.rs, selftest.rs, poller.rs and mode.rs are untouched, and the controller.rs sites were re-grepped on 4aeceea (`freeze: None` at 2994, `len() - 1` slope at 1586/2392, no `.bak`; poller.rs's two new `tracing` lines are shutdown `debug!`s, not the failure warn). Five of the seven new findings are side-effects of the finding-1 and finding-2/3 fixes themselves; the remaining two (stale `gpu_*` fact in spec, fixture and tests) are unrelated to any prior entry.

## Confirmed findings

- [Blocking] src/control/controller.rs:2587 (clamp sites controller.rs:885-887, :1116-1123; config.rs:301-320) — `with_lut_floor_clamp` is never re-run when `self.lut` is replaced at `RunnerEffect::SaveState`, so on the first-calibration path (no state.json → construction clamps against `None`) an unaffordable `gpu_floor_mhz` survives, `budget_bounds` collapses to `(hi, hi)` the moment the LUT lands, and the budget PI is pinned at `cpu_max_w + gpu_max_w` for the rest of the session instead of regulating. (regressed — iteration-2 finding #1 fixed incompletely)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: controller.rs:884-887 `let config = config.sanitized().with_lut_floor_clamp(persisted.lut.as_ref());` is the only construction-time clamp and the comment there names this exact failure ("a GPU clock floor whose watts do not fit under cpu_max_w + gpu_max_w inverts the budget bounds"); config.rs:302 returns `self` unchanged on `None`; `sanitized()` (config.rs:189-224) range-clamps `gpu_floor_mhz` and `gpu_max_w` independently with no cross-check. `grep shutting_down`-style audit by all three seats: the only two `with_lut_floor_clamp` call sites are construction and `Command::SetFloors` (:1122-1123); the `SaveState` arm (:2586-2601) assigns `self.lut = state.lut.clone()`, clears `NotCalibrated` on that same LUT, and never re-clamps — the doc at :2316-2318 itself says `self.lut` "only updates at `RunnerEffect::SaveState`". `budget_bounds` (:2325-2346) then takes `lo > hi` → `return (hi, hi)`; `Budget::set_bounds`/`clamp_to_bounds` (budget.rs:308-321) pin `u == hi` every tick, under a caller comment at :1566 literally claiming "ordered pair guaranteed". Downstream: unclamped `gpu_floor_w` reaches `AllocInput` and trips `allocator::step`'s `debug_assert!` (allocator.rs:250-255) in debug/test builds; `lower_bound_dwell` and `upper_bound_dwell` accrue simultaneously (budget.rs:456-457). Refute seat confirmed master@10a730f has neither `with_lut_floor_clamp` nor `budget_bounds` — entirely this PR — and that every SaveState/calibration test constructs with `Config::default()` (floor 1000), so no test covers a LUT landing against a pre-set floor. Self-heals only on next launch. Silent loss of control on a reachable operator config is the profile's material class; Blocking.
  fix-shape hint: after `self.lut = state.lut.clone()` in the SaveState arm (and in `SetAuto(true)` for symmetry), re-run `self.config = self.config.clone().with_lut_floor_clamp(self.lut.as_ref())` and persist if it changed; one controller test that constructs with `gpu_floor_mhz: 2800, gpu_max_w: 20.0` and no persisted LUT, drives a calibration to `SaveState`, and asserts `budget_bounds()` returns `lo < hi`.

- [Should-fix] src/config.rs:312 — `with_lut_floor_clamp` only ever lowers `gpu_floor_mhz` and silently falls through when the inverse lookup lands at or above the current floor — which happens whenever the LUT's watts column dips across the floor — so it returns a config that still violates the `gpu_floor_w <= gpu_max_w` invariant its own doc claims to establish, with no warning. (new; side-effect of the finding-1 fix)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: config.rs:309-320 `if lowered < self.gpu_floor_mhz { warn; assign }` else unchanged and unlogged, after :306 has already established `gpu_floor_w > gpu_max_w`. Dips are explicitly valid data: lut.rs:8-11 / `is_valid` :44-56 deliberately skip watts monotonicity, and the repo's own test `a_measured_dip_in_the_watts_column_is_not_treated_as_corruption` (state.rs:468-478) pins `[[1200,60],[2000,50],[2800,100]]` and asserts `clock_for_watts(55.0) == Some(2080)`. All three seats hand-traced `gpu_floor_mhz = 1500, gpu_max_w = 55`: `watts_for_clock(1500) = 56.25 > 55` engages the clamp, `clock_for_watts(55) = 2080 >= 1500`, floor stays at 1500 costing 56.25 W against a 55 W cap. Refute seat verified the propagation is not defused: `run_budget_and_allocate` (controller.rs:1884-1991) computes `gpu_floor_w` unclamped and calls raw `allocator::split_budget` (:1986), bypassing `Allocator::step`'s per-axis clamp; `split_budget`'s `.min(gpu_max_w).max(gpu_floor_w)` (allocator.rs:201) yields 56.25 → `auto.last_gpu_cap_w` (:1990) → `gpu_share_override` and the demand-limited judgement; `AllocInput` also feeds the `debug_assert!` at allocator.rs:250-255. Both new unit tests use the strictly-increasing `test_lut()`. Narrow LUT shape and sub-watt overshoot keep it below Blocking.
  fix-shape hint: when `lowered >= self.gpu_floor_mhz`, fall back to the largest LUT clock whose `watts_for_clock` fits under `gpu_max_w` (a forward scan, not the inverse lookup), or raise `gpu_max_w` to `gpu_floor_w` with a warn — and never return silently; add a dipping-LUT test mirroring state.rs:468.

- [Should-fix] src/control/controller.rs:2333-2344; src/control/budget.rs:262-264, :308-321 — When the bounds do invert, both new fallbacks collapse the pair to `(hi, hi)`, which pins the PI integrator at the top of the power envelope with zero authority to reduce power — the opposite of the "giving up performance is always the safe direction" principle the same commit's `with_lut_floor_clamp` doc argues for and applies one function away; the pre-fix panic was loud, the post-fix state is an inert loop with a warn. (new; side-effect of the finding-1 fix)
  verdict: confirmed (reproduce ✓ / refute ✗ (rejected: documented last-resort fallback; independent `ThermalWatchdog` still trips) / ground ✓)
  evidence: controller.rs:2343 `return (hi, hi)`; budget.rs:262-264 `(lo.min(hi), hi)`; `clamp_to_bounds` (:319-321) makes every `step` return `hi`. The branch's own test `disordered_bounds_collapse_instead_of_panicking` (budget.rs:733-752) pins it: `set_bounds(110, 94)` → `(94, 94)`, then `step` with `e_c: -1000.0` (maximal cooling error) still returns 94. Ground seat traced `split_budget` with `budget_w = hi = cpu_max_w + gpu_max_w`: both axes necessarily land at their maxima (`cpu_w = 54`, `gpu_w = 40`) regardless of demand split (the reproduce seat's intermediate `gpu_raw 100` mislabels the pre-clamp value, but its bottom line holds). config.rs:291-296 states the principle and resolves the identical inversion by lowering the floor. **Dissent noted, not resolved in the refute seat's favour:** the refute seat is right that the fallback is documented as "degrade instead of panicking" (controller.rs:2320-2323, config.rs:296-297), matches the iteration-2 fix-shape hint's `lo = lo.min(hi)` option, and that `ThermalWatchdog` → `emergency_release` (watchdog.rs GPU_TRIP_C 91, controller.rs:2628-2652) remains as the hard backstop; but neither CONFIRM concedes the point — the reproduce seat weighs the watchdog explicitly and still confirms, and the iteration-2 hint was a "never panic" shape, not an endorsement of pinning at the cap. Arithmetic 2/3 stands. Held at Should-fix, not Blocking: `hi` is the operator's own configured envelope and the watchdog/hardware backstops are untouched, so this is a regulation loss inside a safe envelope, reached only via the Blocking entry above or the dipping-LUT entry.
  fix-shape hint: on `lo > hi`, drop the unaffordable GPU floor watts rather than raising the floor to the cap — e.g. `lo = cpu_floor_w.min(hi)` (or `lo = (hi - GPU_MIN_SHARE).max(cpu_floor_w)`), keeping downward authority; apply the same shape in `ordered_bounds`, and change the budget.rs test to assert `step` with a large negative error moves `u` below `hi`.

- [Nit] src/control/controller.rs:2325-2346 — The `budget_bounds` degraded path is signalled only by an unthrottled `tracing::warn!` re-emitted every 5 s in Auto and every 1 Hz sample in Calibrating for the whole session, raises no `StatusFlag` and no telemetry field, so the TUI shows nothing while the file-only log fills with one identical line per tick. (new; side-effect of the finding-1 fix)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: controller.rs:2334 warn with no `Once`/rate limit; callers at :1566 (inside `if due`, `ALLOC_PERIOD_S = 5.0` at :57), :2269 (`on_calib_sample`, every sample; `sampler::SAMPLE_PERIOD` 1 s), :2453 (`SetBudget`). `grep -n 'log\|tracing' src/ui/view.rs` is empty; the 13 `StatusFlag`s (controller.rs:3028-3044) include `NotCalibrated`/`TargetUnreachable` for this class and none is raised; `Record::Decision` (telemetry.rs:78-113) carries `budget_w` but no bounds field. The same commit's `ordered_bounds` (budget.rs:249-265) wraps the identical message in `static WARNED: Once` — the last-resort layer is throttled, the per-tick layer is not. Refute seat confirmed `budget_bounds` is absent on master. All three seats rated Should-fix.
  demoted: profile states observability polish is low-value for a single-operator tool with file-only logs, and the condition is only reachable via the two entries above (whose fixes make this path dead) — so an unthrottled warn plus a missing flag is a Nit here.
  fix-shape hint: gate the warn on a transition (`self.bounds_degraded` bool, warn on false→true, info on true→false) and raise/clear a `StatusFlag::BudgetBoundsCollapsed` in the same place so the TUI renders it.

- [Should-fix] src/control/controller.rs:2035 (stall site :2786-2788; call chain :1314 → :1364 → :1656) — The finding-2 stop fence is missing on the primary Auto CPU write: `run_budget_and_allocate`'s first `cpu.set_sustained_mw(cpu_mw)` has no `shutting_down()` check, and the untimed NVML `set_max_clock` in `reassert_actuators` runs earlier in the same `on_sample`, so a thread that unwedges after main's bounded join and `FinalRestore` can still land a CPU cap that nothing undoes — the exact window the fence was added to close. (regressed — iteration-2 finding #2 fixed incompletely)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: `grep -n 'shutting_down()' src/control/controller.rs` on 4aeceea → 1033, 1062, 1261 (top of `on_sample`), 2045, 2099, 2481, 2534, 2767; 2045 guards only the Mismatch re-write (`&& !self.shutting_down()`), the primary write at :2029-2035 is bare. The field's own doc at :786-792 states the contract: "Checked at the top of `on_sample` and again immediately before every actuator WRITE" — every other write site complies (run_gpu_pi :2099 "this covers the flag being raised mid-sample", reassert :2767, calib :2481/:2534), so this is an asymmetry against the code's own invariant. Resumed branch calls `reassert_actuators` (:1314) → `gpu.set_max_clock` (:2786) → `set_gpu_locked_clocks` with no timeout (gpu.rs:148-157), before `on_auto_sample` (:1359-1361) reaches the write; main.rs:327-346's join accounting covers the fast `release`, not the `set_max_clock` lock path. `a_sample_after_shutdown_began_issues_no_actuator_write` (:3396-3417) stores the flag before `on_sample` — only the top fence is tested (refute seat: 3 fence tests exist, not 4 as the finding said; immaterial). Whole `shutdown` mechanism absent on master. Narrow timing window and next-launch `startup_reset` self-heal keep it at Should-fix.
  fix-shape hint: add `if self.shutting_down() { return; }` immediately before the `cpu.set_sustained_mw(cpu_mw)` at :2029 (mirroring run_gpu_pi :2099), and one test with a fake GPU actuator whose `set_max_clock` flips the shutdown flag mid-call, asserting the CPU actuator log stays empty.

- [Should-fix] src/actuators/guard.rs:161-194 (main.rs:177, :398-401; gpu.rs:132-134, :161-166) — The bounded controller join does not bound shutdown: on `TimedOut` the `restored` flag is still false, so `FinalRestore::drop` re-issues the very NVML calls the wedge is attributed to (`GpuActuator::new()` + `release()`), untimed, on main's stack — a driver wedge relocates into main's `Drop`, the process never exits, the caps stay applied, `_log_guard` never flushes, and the old process being alive defeats next-run `startup_reset`. (new; side-effect of the finding-2/3 fix)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: main.rs:398-401 `TimedOut` arm only logs "continuing shutdown so the terminal is restored"; `restored` is set only by the controller thread after its own `restore_all` (controller.rs:2912-2915). `_final_restore` (main.rs:177) is a plain local declared after `_log_guard` (comment at :174-175), so it drops first at end of `main`; guard.rs:162 sees `false`, grace-polls 2 s (:165-171), then builds `GpuActuator::new()` (gpu.rs:132-134 `Nvml::init`, `device_by_index`) and calls `restore_all` → `gpu.release()` (guard.rs:88-92 → gpu.rs:161-166 `reset_gpu_locked_clocks`) — direct `nvml_wrapper` calls with no deadline, unlike every `ryzenadj`/`modprobe` call (cmd.rs:35). main.rs:342-343's own derivation says the bound "only bites when the controller thread is wedged somewhere with no timeout of its own", and main.rs:242-248's shutdown ORDER comment assumes "_final_restore drops as a no-op (restored is true)" — false on the very `TimedOut` branch this commit adds. Refute seat: master's join was unbounded, so a wedge hung the process there too, but earlier and without the PR's new "FinalRestore's Drop … still there to restore the hardware" claim (70ef858); guard.rs is byte-identical to master, so the claim is new and unkept. Distinct from iteration-1's spot-refuted "poller joins without timeout", which rested on "hardware restore already ran" — here it has not. Requires a genuine driver wedge; Should-fix.
  fix-shape hint: run the fallback restore on a helper thread joined with `join_with_timeout` (log and exit on expiry), or wrap the NVML init/release pair in a deadline; either way `main` must return with the tty restored and the log flushed even if NVML never does.

- [Should-fix] src/main.rs:299-317 — `quit_and_join_controller`'s documented fallback ("On expiry we log and continue; `FinalRestore`'s `Drop` … still there to restore the hardware") is unreachable under the wedge the bound exists for: after the timed-out controller join main still passes `sampler.join()` and the poller joins, all unbounded, and the sampler makes the same untimed NVML calls, so one driver hang blocks main forever before `_final_restore` drops; even the benign NVMe case adds up to ~60 s of post-restore hang. (new; side-effect of the finding-2/3 fix; same root as the entry above — fix together)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: main.rs:305 `sampler.join()`, :309 `fanctrl_poller_thread.join()`, :312 `nvme_poller_thread.join()`, :317 `led.join()` — all plain `JoinHandle::join`, all before `main` returns (ground seat: terminal restore at :283-289 runs before these, so the tty is safe; the hardware restore is not). sampler.rs:194 `self.gpu.as_ref().map(|g| g.read())` → `GpuSensor::read` (sensors/gpu.rs:44-63) untimed `device_by_index`/`power_usage`/…; `Sampler::spawn`'s loop (:267-289) checks `shutdown` only between iterations. The actuator and sensor open independent `Nvml` handles on the same device, so one driver-level hang stalls both threads — not an independent conjunction. `CONTROLLER_JOIN_TIMEOUT` = 5 × 5 s + 5 s = 30 s confirmed (cmd.rs:35). poller.rs:159-166 module doc: the NVMe `read` "may block far longer than `NVME_PERIOD` … delays this thread noticing `shutdown`" (kernel `admin_timeout`); the input thread at :322-326 is already detached with "it owns nothing needing cleanup", which the sampler and pollers also satisfy at that point. Refute seat: the sampler join was unbounded on master too, but the fallback promise is new in 70ef858 and its commit message reasons as if `FinalRestore::drop` reliably fires. No design-doc text accepts a post-join hang.
  fix-shape hint: after a `TimedOut` controller join, detach (or `join_with_timeout`) the sampler and poller handles instead of blocking on them — they own nothing needing cleanup once shutdown began — so `_final_restore` is reached; keep the unbounded joins only on the normal path.

- [Should-fix] docs/superpowers/specs/2026-09-07-fw-fanctrl-loop-design.md:265 (also :938, :941 vs the correction at :54); docs/research/05-fw-fanctrl-loop.md:54-56 — The §Facts correction of 2026-09-09 ("the dGPU DOES feed the EC fan curve"; all four `gpu_*` cros_ec sensors report live and `gpu_vr` is the steady-state argmax under GPU load) was never propagated into §2.2's EC-replica rule or the fwloop.3 / fwloop.20 task rows, which still state the refuted "the `gpu_*` sensors never report" as measured fact and as a binding acceptance criterion; the stale research-doc entry sits unannotated ten lines above its own refutation. (new; fix together with the next entry)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: design doc:54 "**CORRECTED 2026-09-09 — the dGPU DOES feed the EC fan curve** … all four report live values … `gpu_vr` at steady state (mean 83.2, max 84)"; :265 unchanged "on this machine the `gpu_*` sensors never report (measured with the dGPU powered, §Facts)" — citing §Facts as its own support; :938 (fwloop.20) "this fixture pins the measured fact that they never report", acceptance "the dGPU-on tree contains no positive `gpu_*` reading"; :941 (fwloop.3) "measured: they never report even with the dGPU powered". Refute seat: `git show --stat cb4a2ab` (the §Facts rewrite) touched only the two §Facts bullets and §2.8 prose, never §2.2 or the task rows; 3bf65d8 touched only the research doc (1 file, 46 insertions) and left 05:54-56 "**the dGPU does not feed the fan curve directly**" in place. Both docs are wholly new on this branch. Ground seat: the stale claim is also baked into shipped code — ec.rs:6-11 doc comment and the test named `cros_ec_dgpu_on_gpu_sensors_still_never_report` (ec.rs:281); §2.5/§2.6 gate Mode A on whether the EC argmax is a sensor this app can steer, which is exactly the corrected fact. Not polish: a spec asserting a hardware fact and its negation, with the negation as acceptance text, is a real propagation gap.
  fix-shape hint: rewrite design doc:265 and the fwloop.3/fwloop.20 rows to the corrected fact (gpu_* live, `gpu_vr` argmax under load, classified controllable), strike-through or annotate research 05:54-56 with a pointer to the 2026-09-09 section, and update the ec.rs/plant.rs comments in the same commit.

- [Should-fix] tests/fixtures/hwmon/cros_ec_dgpu_on/temp5_input (temp6/temp7_input, temp8_label); src/sensors/ec.rs:281; src/test_support/fixtures.rs:137 — The branch's only dGPU-powered cros_ec fixture and the two tests built on it still encode the refuted "`gpu_*` never report" fact and actively forbid the corrected shape, so no captured fixture anywhere presents a `gpu_*` sensor as the EC argmax — the regime the branch's own measurement says dominates within 13 s of GPU-only load — and the §2.2 controllability branch Mode A depends on is exercised only by a synthetic single-value unit case. (new)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓)
  evidence: fixture `temp5/6/7_input = -150` (`gpu_amb`/`gpu_vr`/`gpu_vram_f75303@4d`), `temp8_label: gpu_temp@40` with no `_input` — verified by direct read; added in 469fb88 (2026-09-09 05:39), ~14 h before the §Facts correction cb4a2ab. ec.rs:280-301 `cros_ec_dgpu_on_gpu_sensors_still_never_report` asserts `!argmax.starts_with("gpu")` ("they never report on this machine") and pins `argmax == "ambient_f75303@4d"`; fixtures.rs:136-152 `cros_ec_dgpu_on_has_no_positive_gpu_reading` asserts every `gpu_*` value ≤ 0 and its comment says a positive value "would mean the fixture silently reverted to the round-2 'they come alive' assumption this task replaces" — forbidding the shape research 05:60-81 measured (`gpu_vr` mean 83.2 °C argmax by t=13 s in two 240 s runs). `grep -rn cros_ec_dgpu_on src/ tests/` → only these two tests plus the generic loop at fixtures.rs:67. Refute seat searched for coverage under another name: plant.rs:495-497 repeats the stale claim; `grep set_gpu_ec src/control/sim_tests.rs` is empty; the one 30-min dGPU-hot sim (sim_tests.rs:1815) scripts only NVML `gpu_temp_c`, explicitly "(an unrelated sensor)"; the only positive-`gpu_*` test is the synthetic `a_synthetic_positive_gpu_reading_joins_the_max_and_is_controllable` (ec.rs:330-341), commented as "a future firmware waking up". Post-Implementation Notes never mention it. Fixing the spec entry alone would leave the suite pinning the retracted claim.
  fix-shape hint: re-capture `cros_ec_dgpu_on` (or add a `cros_ec_dgpu_load` tree) under GPU load with the live `gpu_*` values, flip the two tests to assert a controllable `gpu_*` label takes the argmax at ~83 °C, and drive one plant-closed sim through `set_gpu_ec` so Mode A's controllable-argmax path is exercised from a captured tree.

Carried from iteration 1 (still-open, not re-scouted this round; the fix diff 28904cf..4aeceea touches none of these sites — verified by grep on 4aeceea):

- [Nit] src/fanctrl/curve.rs:63 — `Curve::from_points` validates duty order only, not temperature order (`CurveError` still has only `Empty`/`DescendingSegment`, curve.rs:22-35) (still-open, see iteration 1). fix-shape hint: second `CurveError` variant for `w[1].0 < w[0].0`.
- [Nit] src/control/controller.rs:1586 (was 1528); :2392 (was 2300) — §2.6 reconciliation slope divides by `len − 1` samples, not seconds, while the window is pushed only when `s.ec` is `Some` (still-open, see iteration 1). fix-shape hint: store `(t_mono, max_c)` and divide by the time span; share with the calib copy.
- [Nit] README.md:71,168,176,204; src/config.rs:55; src/control/controller.rs:248 — docs still carry pre-a78 thresholds 90/85/87 vs shipped 88/86/91 (README.md:71 "`90.0` … exit is this − 5" and :168 "GPU ≥ 87 °C" re-checked on 4aeceea) (still-open, see iteration 1). fix-shape hint: search-and-replace to 88/86/91/"− 2", including design doc §2.8 header.
- [Nit] src/sensors/poller.rs:104,109; src/fanctrl/client.rs — fw-fanctrl socket failures discarded without any log line (`grep tracing src/fanctrl/*.rs` still 0; poller.rs's two hits are shutdown `debug!`s) (still-open, see iteration 1). fix-shape hint: edge-triggered `tracing::warn!` on transition into failure.
- [Nit] src/control/controller.rs:2994 (was 2871) — `decision` telemetry `freeze` column hardcoded `None` (still-open, see iteration 1). fix-shape hint: destructure `freeze` from `Effect::AutoAllocated` into the record.
- [Nit] src/control/controller.rs (decision emitter) — `mode` column carries controller `Mode`, never `LoopMode` (still-open, see iteration 1). fix-shape hint: add a `loop_mode` column.
- [Nit] src/control/mode.rs:261,487-512; src/control/controller.rs — `Decision.reasons` has no production consumer despite design doc:276 (still-open, see iteration 1). fix-shape hint: `reason` column or StatusFlags; reconcile the spec.
- [Nit] src/selftest.rs — no fw-fanctrl socket / `cros_ec` preflight (still-open, see iteration 1). fix-shape hint: two informational selftest steps.
- [Nit] src/control/controller.rs (save_persisted_state); src/state.rs — one-way state.json migration, no `.bak` (`grep '\.bak'` still empty) (still-open, see iteration 1). fix-shape hint: write `state.json.bak` once; document.
- [Nit] README.md:248-255 — Status paragraph says the closed loop is not wired (still-open, see iteration 1). fix-shape hint: replace with the shipped state.
- [Nit] README.md:15-24 — Requirements never state fw-fanctrl must be running (still-open, see iteration 1). fix-shape hint: one prerequisite bullet.

## Not verified (beyond panel cap)
- none

## Beyond remainder cap (count only)
- none

## Rejected (with reason)
- src/config.rs:301-320 — `with_lut_floor_clamp` is structurally a no-op for any `gpu_floor_mhz` at or below the LUT's lowest swept clock (1200 MHz), the default 1000 included, so the invariant its doc promises is never established there. Rejected 1/3 (reproduce REJECT, refute REJECT, ground CONFIRM). The structural claim is true — `clock_for_watts` clamps low to `points[0].0` (lut.rs:69-101), `SWEEP_CLOCKS` bottoms at 1200, and both new tests use 2800/1200 — but the refute seat showed the method's own doc (config.rs:295-297) names this exact residual: "`clamp_gpu_clock`'s hardware floor (1000 MHz) still applies, so an envelope below the cheapest lockable clock stays infeasible; the allocator's own `.clamp` and the `Budget`'s bound ordering are what make that case degrade instead of panicking" — a documented tradeoff with a named, implemented fallback (`budget_bounds` :2322-2346 and `ordered_bounds` budget.rs:250-264, both fed to every `set_bounds` call site), and the reproduce seat found the branch's own test at controller.rs:6791-6795 treating the 1000→1200 clamp as expected. The ground seat's remaining point — that the per-axis `gpu_floor_w > gpu_max_w` pair still reaches `Allocator::step`'s `debug_assert!` unclamped — is the same propagation the confirmed dipping-LUT entry above covers, and is fixed by the same change to `with_lut_floor_clamp`'s fall-through. The doc overclaim ("implies `lo <= hi`") is a documentation nit; correct it when fixing that entry.
- The iteration-1 and iteration-2 rejections stand unchanged and were not re-surfaced: poller `+= PERIOD` catch-up burst (clock semantics — see Escalations); leaked connect thread per timeout; released actuator re-commanded every tick (§2.9); hard-freeze→DemandLimited skipping `resync_error`; guard fails open on `None` / CPU-only sensor-lost; unversioned socket JSON; band residency vs `snapped_rpm`; `GPU_TRIP_C` unpinned. None of the fix commits changed the code or spec text those rejections rested on. Note for the record: iteration-1's *spot-refuted* "shutdown joins poller threads without timeout" was refuted on the premise "hardware restore already ran"; the finding-2/3 fix's bounded join created a path where it has not, and the two confirmed shutdown entries above were panel-verified on that new evidence (main.rs:398-401, 70ef858's fallback claim) — they do not re-litigate the spot refutation, they supersede its premise.

## Unverified nits (spot-checked)
- No spot-tier packets this iteration (14 raw → 10 deduped → 10 panel). The 23 iteration-1 spot-survived nits remain as recorded there; none was re-checked this round.

## Escalations (need human)
- Instant/suspend clock semantics (carried from iterations 1 and 2, still untouched by any fix commit: the 28904cf..4aeceea diff's only sampler.rs change is the `SAMPLE_PERIOD` `pub(crate)` link): design doc:270-271 still says "a monotonic clock does not advance across suspend" while Cargo.toml:5 and sampler.rs say Linux `Instant` is CLOCK_BOOTTIME-backed since 1.87; `is_resume_gap` and the resume-reassert path depend on which is true — a human should verify against the pinned rustc and correct whichever side is wrong (and re-open the iteration-1 poller catch-up-burst finding if BOOTTIME is real).
---
