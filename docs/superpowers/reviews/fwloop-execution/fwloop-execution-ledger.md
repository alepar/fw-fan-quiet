# SDD ledger — plan: fw-fanctrl-loop-6ma-plan.md
Launch: args {"epicId":"fw-fanctrl-loop-6ma","integrationBranch":"epic-fw-fanctrl-loop-6ma-integration","integrationWorktree":"/var/home/alepar/AleCode/bazerame-fans/.claude/worktrees/fw-fanctrl-loop/.worktrees/epic-fw-fanctrl-loop-6ma-integration","config":{"concurrency":4,"hotFileCap":3,"topUpQueryCap":40,"edgeAuditCap":3,"gate":"cargo test","sweep":"cargo clippy --all-targets -- -D warnings","testPaths":["src/**/*.rs"],"models":{"planner":"opus","implementer":"sonnet","reviewer":"sonnet","mechanical":"sonnet","triage":"opus","finalReview":"opus","fixEscalation":"opus"}},"dryRun":false}
Launch: args {"epicId":"fw-fanctrl-loop-6ma","integrationBranch":"epic-fw-fanctrl-loop-6ma-integration","integrationWorktree":"/var/home/alepar/AleCode/bazerame-fans/.claude/worktrees/fw-fanctrl-loop/.worktrees/epic-fw-fanctrl-loop-6ma-integration","config":{"concurrency":4,"hotFileCap":3,"topUpQueryCap":40,"edgeAuditCap":3,"gate":"cargo test","sweep":"cargo clippy --all-targets -- -D warnings","testPaths":["src/**/*.rs","tests/**"],"models":{"planner":"opus","implementer":"sonnet","reviewer":"sonnet","mechanical":"sonnet","triage":"opus","finalReview":"opus","fixEscalation":"opus"}},"dryRun":false}
Merge: fw-fanctrl-loop-blm — rebase clean · seam-review none · gate pass
Task 2 (fw-fanctrl-loop-blm): minor (deferred): src/test_support/fixtures.rs: resolves_an_existing_fixture_to_an_absolute_path asserts p.is_absolute(), but path() always joins onto CARGO_MANIFEST_DIR (Cargo-guaranteed absolute) so this assertion cannot fail regardless of fixture content — assertion-discipline nit, not a defect.
Task 2 (fw-fanctrl-loop-blm): complete (commits 15b6dbf..469fb88, review clean)
Merge: fw-fanctrl-loop-834 — rebase clean · seam-review none · gate pass
Task 3 (fw-fanctrl-loop-834): minor (deferred): cargo clippy --all-targets -- -D warnings fails with 12 dead_code errors (verified by running it) because nothing outside #[cfg(test)] calls into budget.rs yet. Self-disclosed in the report's Concerns #1; structurally forced by the deliverable's own 'no controller wiring in this task' scoping plus the plan's mod.rs hot-file ownership table (task 12, which wires Budget into controller.rs, is not among the tasks permitted to touch mod.rs, so a #[cfg(test)] gate now would block task 12 later). Matches the SDD launch config (progress.md), which runs cargo clippy as an end-of-epic 'sweep', not the per-task 'gate' (cargo test is the gate) — expected to clear once fwloop.12 wires the module in.
Task 3 (fw-fanctrl-loop-834): minor (deferred): LoopGains (src/control/budget.rs:83-92) carries only the four PI-gain fields, not the full persisted struct §2.4 also lists (tau_s, theta_s, k_c_per_w, k_rpm_per_w, fitted_at). Self-disclosed as Concern #2 in the report. The brief's own 'API this task owns' list and every acceptance-criteria clause exercise only the four gains, so this reads as correct scoping per the brief (which the plan says wins on brief/spec conflicts when disclosed) — but fwloop.21 (FOPDT fit) and fwloop.11 (persisted-state) should confirm they're meant to extend this struct rather than find it pre-populated.
Task 3 (fw-fanctrl-loop-834): minor (deferred): at_lower_bound_for/at_upper_bound_for (budget.rs:327-338) compare v_new against lo/hi with <= and >= independently, so if lo==hi (the pre-set_bounds() state from Budget::new) both counters could increment on the same tick. Never exercised by any test since step() is only called after set_bounds(lo<hi) throughout this suite and in the design's real call pattern (controller always calls set_bounds before stepping).
Task 3 (fw-fanctrl-loop-834): minor (deferred): step() (budget.rs:292-300) only implicitly resync_errors on a kind switch or on leaving a freeze entirely (frozen -> None). Transitioning directly between two different Freeze reasons (e.g. ActuatorMismatch -> DemandLimited without an intervening unfrozen tick) skips resync_error, so DemandLimited's first tick after such a transition could apply a proportional kick off a stale e_prev. Not covered by any acceptance criterion, and §2.4 explicitly leaves inter-freeze-reason transitions unspecified (open items are for the fwloop.24/9it spike).
Task 3 (fw-fanctrl-loop-834): complete (commits ec602e6..d9f691f, review clean)
Merge: fw-fanctrl-loop-9dv — rebase clean · seam-review cleared · gate pass
Task 1 (fw-fanctrl-loop-9dv): minor (deferred): src/fanctrl/table.rs refine(): upper-neighbor lookup via `duty.saturating_add(1)..` self-includes the entry at duty==255 (u8::MAX), an untested edge case outside the documented 0-100 percent domain — harmless today since no real caller produces duty>100, but worth a guard/comment if the type is ever widened.
Task 1 (fw-fanctrl-loop-9dv): minor (deferred): tread()'s NEG_INFINITY/INFINITY bounds at the curve's floor/ceiling duty are a literal-but-unverified reading of §2.1, already flagged by the implementer as needing the design author's sign-off before a downstream setpoint/PI consumer relies on it.
Task 1 (fw-fanctrl-loop-9dv): complete (commits f1b4da4..24ac6cd, review clean)
Merge: fw-fanctrl-loop-fo1 — rebase clean · seam-review none · gate pass
Task 4 (fw-fanctrl-loop-fo1): minor (deferred): src/control/trim.rs:117 and src/control/trust.rs:7 doc comments still say the trust monitor is surfaced "via `StatusFlag::ModelDistrust`" — that variant is removed by this diff, so the comments are now stale (out-of-scope adaptation-tier files per the brief's scope fence; not a functional bug).
Task 4 (fw-fanctrl-loop-fo1): complete (commits c11170e..7c0b9d4, review clean)
Merge: fw-fanctrl-loop-mm2 — rebase clean · seam-review cleared · gate pass
Task 5 (fw-fanctrl-loop-mm2): complete (commits dbf6dca..8e22a55, review clean)
Merge: fw-fanctrl-loop-52c — rebase clean · seam-review none · gate pass
Task 8 (fw-fanctrl-loop-52c): complete (commits 7a7ed28..8fa333a, review clean)
Merge: fw-fanctrl-loop-58u — rebase clean · seam-review cleared · gate pass
Task 7 (fw-fanctrl-loop-58u): minor (deferred): src/fanctrl/client.rs:264 — FanctrlView.all_observed_at is typed Option<Instant> but both producers (UnixFanctrlClient::poll and FakeFanctrl::poll, client.rs:574 and fakes.rs:1097) only ever construct a view with Some(now); a view can only be created by a successful All, so the None arm of compute_freshness's `.is_some_and` (client.rs:314-316) is structurally unreachable once a view exists — Instant (not Option<Instant>) would express the same invariant with less ceremony.
Task 7 (fw-fanctrl-loop-58u): minor (deferred): src/test_support/fakes.rs:1001-1008 — FakeFanctrl does not assert that the scripted outcome's implied command matches the PrintCommand actually passed to poll() (e.g. a Speed outcome can be dequeued for an All call); documented as an intentional non-goal, but a debug_assert would catch a whole class of test-authoring bugs for free.
Task 7 (fw-fanctrl-loop-58u): minor (deferred): src/fanctrl/client.rs:473-484 — connect_with_timeout leaks its spawned thread if the underlying UnixStream::connect syscall never returns (no way to cancel a blocked syscall from std alone); documented in the function's doc comment and practically unreachable for AF_UNIX (connect is effectively instant), but worth a tracking note if this pattern is reused for a socket type where a hang is plausible.
Task 7 (fw-fanctrl-loop-58u): complete (commits 6b464c0..802de7c, review clean)
Merge: fw-fanctrl-loop-jpg — rebase clean · seam-review none · gate pass
Task 9 (fw-fanctrl-loop-jpg): minor (deferred): GPU's GpuLockVerifier::verify_lock is deliberately left unwired from src/control/controller.rs (no non-test callers) -- deferred to Task 19 (fw-fanctrl-loop-j6s, real bead, 'Controller loop integration', which owns controller.rs's body next). A defensible reading of the brief's 'the two actuator call sites' as 'call sites for the two actuator types' rather than a literal count, self-disclosed at length in the report's 'Scope decision' section.
Task 9 (fw-fanctrl-loop-jpg): minor (deferred): cargo clippy --all-targets -- -D warnings leaves 6 new dead_code errors (GpuLockVerifier struct/new/verify_lock, its 3 VERIFY_* constants, WriteVerdict::Unverifiable), all from the deliberately-unwired GPU verifier. Verified via progress.md (the SDD ledger, not just the report) that the identical pattern on Task 3/budget.rs was already reviewed and logged 'minor (deferred)', and the launch config makes cargo test the per-task gate with clippy as the end-of-epic sweep -- same disposition applies here.
Task 9 (fw-fanctrl-loop-jpg): minor (deferred): The GPU Mismatch's field name ("gpu_sm_mhz") is the implementer's own choice -- the design doesn't name one verbatim the way it does for the CPU's PPT LIMIT SLOW/FAST rows -- self-disclosed in the report's 'Self-review findings'.
Task 9 (fw-fanctrl-loop-jpg): minor (deferred): A failed ryzenadj write now returns WriteVerdict::Unreadable with the original stderr/error text only reaching a tracing::warn! log, not the return value -- a minor loss of caller-visible diagnostic detail versus the old io::Error-based signature, though this is an inherent, reasonable consequence of the new richer verdict type.
Task 9 (fw-fanctrl-loop-jpg): complete (commits 4fcb11f..b4d6a16, review clean)
Task 6 (fw-fanctrl-loop-zct): fix round 1/5 (1 addressed, 0 open — src/control/allocator.rs:260-269 (Allocator::step) — quantize() is applied AFTER the `.max(floor)` re-clamp, so when cpu_floor/gpu_floor is not itself a multiple of GRID_STEP_W (0.5 W), the final quantized output can round DOWN below the configured floor by up to 0.25 W, violating the bead's verbatim acceptance criterion "floors always met" and the module's own stated invariant ("Both floors always win: split_budget returns at least the floor on each axis..."). Concrete repro: cpu_floor_w = 15.2 is a legal config value — src/config.rs's Config::validate only clamps cpu_floor_w to [0, cpu_max_w], it never grid-aligns it (see config.rs:179-190). With prev/raw at or near that floor, step() computes `raw_cpu.clamp(...).max(15.2)` = 15.2, then `quantize(15.2)` = `(15.2/0.5).round()*0.5` = `30.0*0.5` = 15.0 — 0.2 W below the floor. The same applies to gpu_floor_w, which controller.rs derives via `lut.watts_for_clock(self.config.gpu_floor_mhz)` (interpolated, no grid-alignment guarantee). The pre-existing allocator avoided this by applying `.max(cpu_floor)` LAST, with no further rounding after it — this task's reordering (quantize wraps the floor-clamp) is a regression. Fix: quantize the rate-clamped raw value first, then apply the floor last without re-quantizing, e.g. `let cpu_w = quantize(raw_cpu.clamp(prev.0 - DOWN_RATE_W, prev.0 + UP_RATE_W)).max(cpu_floor);` (and symmetrically for gpu_w/gpu_floor). Add a regression test with a non-grid-aligned floor (e.g. cpu_floor_w = 15.2) asserting the floor is still met exactly through Allocator::step, not just through split_budget (which has no quantization and is not affected).; commits ec602e6..7232067)
Merge: fw-fanctrl-loop-zct — rebase clean · seam-review fixed · gate fail → blocker
Task 6 (fw-fanctrl-loop-zct): BLOCKED — SUMMARY. fw-fanctrl-loop-zct's implementation is correct and complete within its charter, but it cannot satisfy the epic's declared per-task gate, and the gap is a plan defect rather than something the plan settles. Evidence: I ran `cargo test` at the merge-base itself (integration tip 1ee0ffa, /var/home/alepar/AleCode/bazerame-fans/.claude/worktrees/fw-fanctrl-loop/.worktrees/epic-fw-fanctrl-loop-6ma-integration) and it is fully green — 553 passed, 0 failed, 2 ignored. So the blocker bead's central claim that the 24 `control::controller::tests::*` failures are "pre-existing" is factually wrong: they are introduced by zct. The diff 1ee0ffa..271d8ff shows why and shows it is unavoidable — landing the scalar budget split required deleting the `contour` closure, the KF `bias`/`gain` feed into `AllocInput`, `FAN_SMOOTH_N`/`tail_mean` smoothing, and the `overshoot_settle` `Noted` effect from the auto path in `controller.rs`, which is exactly the production behavior the KF/trust/cooldown/`fitted_model` test block asserts on. Plan Task 6 (§"What this task owns") mandates those deletions; plan Task 12 (`fw-fanctrl-loop-24s`) owns deleting the tests that die with them; and the plan sequences 6 before 12 (Task 12: "the auto path compiles with the budget stubbed at the floors and the existing `AllocInput` call site (from Task 6)"). Meanwhile Global Constraints demand `cargo test` green for every task and Task 6 step 9 says "Run the test suite and the linter; both clean". Those two requirements are mutually unsatisfiable for zct given its scope fence ("`src/control/controller.rs` — the `allocator.step` call site only. Nothing else in that file."), which zct already had to breach out of necessity. This is not isolated: `fw-fanctrl-loop-bwt` is an open blocker with the identical shape (dsh's PersistedState change breaks ~50 Auto-mode tests "that fw-fanctrl-loop-24s owns fixing"). A per-task re-dispatch of zct fixes neither — the implementer would re-run and file the same blocker — so RESOLVE would burn a dispatch without changing the outcome. Note that `fw-fanctrl-loop-24s` is currently in the ready queue with no active blockers (`bd ready` lists it; its only recorded dependency, fwloop.8 = fo1, merged at dbf6dca), and branch `task-fw-fanctrl-loop-24s` is still sitting unworked at the integration tip. DECISION NEEDED (pick one; all three change the declared plan or gate, which is why this is yours and not triage's): (a) Accept a knowingly-red integration branch for the zct→24s window: merge zct as-is with the 24 failures confined to the adaptation-tier block, immediately dispatch 24s as the next task, and re-run the full `cargo test` gate only after 24s lands. Preserves the plan's 6→12 order and every scope fence; cost is a temporarily red integration branch and a gate policy exception. This also unblocks `fw-fanctrl-loop-bwt` by the same rule. (b) Authorize zct to widen into 24s's territory: re-dispatch zct with permission to delete the 24 dead adaptation-tier tests from `controller.rs` as part of its own commit. Keeps the branch green at every merge, but reassigns chartered ownership and risks the "most expensive merge conflict available in this epic" that the plan's hot-files section warns about for `controller.rs`. (c) Reorder: run 24s first against the current green tip, then rebase and merge zct. Cleanest gate-wise, but 24s's implementation steps (step 4) are written against the Task 6 `AllocInput` call site that would not yet exist, so its implementer must stub against the old shape and zct's rebase then collides on that same call site. My read, offered but not acted on: (a) is the lowest-risk and the only option that does not rewrite ownership or ordering, and it generalizes to the bwt blocker. But it requires you to relax the "cargo test green at every task" constraint into "green at every task except where the failing tests are explicitly owned by a later, named task", which is a gate-policy change the run cannot make for itself. Nothing was written or modified; zct's work and the beads are untouched.
Merge: fw-fanctrl-loop-4aj — rebase clean · seam-review none · gate pass
Task 10 (fw-fanctrl-loop-4aj): minor (deferred): fit_fopdt takes an added `min_response: f64` third parameter beyond the brief's literal 2-arg signature; well-justified (neither Fopdt nor derive_gains carries step_w to do the magnitude check elsewhere) but is a public-API deviation from the design doc's pseudocode that task 18 (fw-fanctrl-loop-0nv) must account for when wiring.
Task 10 (fw-fanctrl-loop-4aj): minor (deferred): search_tau_theta's grid bounds (tau_hi = 3*span, theta_hi = 0.5*span) are heuristic, validated against only one synthetic (K,tau,theta) scenario; implementer's own report flags this as an open risk for task 18's real fixture data.
Task 10 (fw-fanctrl-loop-4aj): complete (commits 1ee0ffa..2bbf4f6, review clean)
Task 13 (fw-fanctrl-loop-dsh): BLOCKED — Task fw-fanctrl-loop-dsh (plan Task 13, "Persisted state migration") cannot proceed while its sibling fw-fanctrl-loop-24s (plan Task 12, "Remove the adaptation tier") is still open and in flight, and the fix is a beads-graph change that triage is not permitted to make. Verified in the dsh worktree (HEAD 4fcb11f, untouched): dsh's acceptance criteria removes `model`/`adapt_bias`/`adapt_gain` from `PersistedState`, but `src/control/controller.rs` still gates Auto entry on `self.model.is_none() || self.lut.is_none()` (lines 821 and 1124) and `self.model` is populated only from `PersistedState.model`; the `calibrated()`/`auto_controller()` test fixtures inject `PersistedState { model: Some(fitted_model()), .. }` (7 `PersistedState {` literals in controller.rs), so the field deletion breaks that block at compile time and, if patched to compile, leaves Auto permanently unreachable at runtime — the same ~24-50 adaptation-tier tests already recorded red on the integration branch by blocker bead fw-fanctrl-loop-6ma.1. `src/calib/runner.rs:489` (SaveState construction) and its test near line 783 are a second compile break outside dsh's filesTouched. The plan settles the ordering — Task 12 "owns `src/control/controller.rs` **as a whole** for the duration — it is the file's demolition pass", while Task 13 is confined to "the persist call sites only (`save_persisted_state`, `exit_auto_and_persist`, `apply_calib_effects`). Nothing else", and the plan's Hot files note says these controller.rs edits are "almost entirely sequenced by dependency, not parallel" — but no such edge exists in beads: `bd show fw-fanctrl-loop-24s` depends only on fw-fanctrl-loop-fo1 (closed), dsh depends on neither, and both were dispatched as simultaneously-ready siblings (24s currently has uncommitted controller.rs edits in its own worktree). Re-dispatching dsh now with any clarification re-blocks deterministically, because the dependency it needs is not expressible as task context. DECISION NEEDED FROM THE USER: pick one — (a) add a `blocks` edge fw-fanctrl-loop-24s → fw-fanctrl-loop-dsh so 24s lands first (matches the plan's stated ordering and is the lowest-risk option; dsh then re-runs unchanged after re-invocation), (b) widen dsh's charter to also perform 24s's controller.rs gate/test demolition and drop its "nothing else in that file" restriction (risks the exact merge conflict the plan warns is "the most expensive merge conflict available in this epic", since 24s is already mid-implementation), or (c) merge dsh into 24s as one task. Whichever lands the `PersistedState` schema change must also be authorized to fix `src/calib/runner.rs` (~line 489 and its test ~775-788), which is in no current task's filesTouched.
Task 11 (fw-fanctrl-loop-9it): pending retry — RESOLVE: The CPU-axis measurement is NOT unavailable on this machine — you hit a documented precondition and then undid the fix. The governing spec's §Facts already records it, at docs/superpowers/specs/2026-09-07-fw-fanctrl-loop-design.md line 93: > `ryzenadj --info` fails while `ryzen_smu` is loaded — the same precondition the existing `smu_module.rs` already enforces for writes, so read-back shares it. And src/actuators/smu_module.rs's own header comment states the mechanism verbatim: > the installed ryzen_smu 0.1.7 lacks Strix Point PM-table support. While [loaded, ryzenadj fails with "Unable to get os_access Obj"]; with it unloaded, ryzenadj falls back to libpci SMN access, which works. So before CPU actuation we unload the module, and restore it on exit iff we were the ones who unloaded it. The "Unknown PM table version: 0x005D000B" dmesg line is not a blocker, it is exactly the condition this codebase already works around. Proof it works on this hardware: the committed fixture tests/fixtures/ryzenadj_info.txt was captured on this machine and opens with `no compatible ryzen_smu kernel module found, fallback to /dev/mem`, then reports `PM Table Version: 5d000b` (the same version) and a live table containing precisely the two rows your measurement needs — `PPT LIMIT SLOW 48.000` (commanded) against `PPT VALUE SLOW 37.282` (drawn), plus STAPM LIMIT/VALUE. Your attempt #4 (`modprobe -r ryzen_smu && modprobe ryzen_smu`) unloaded the module and then immediately reloaded it, restoring the broken precondition before running ryzenadj. The module must be unloaded and LEFT unloaded for the duration of the measurement. I verified the current state: `/sys/kernel/ryzen_smu_drv/` exists with no `pm_table` entry — which is literally `SmuModule::needs_unload()` returning true in smu_module.rs. Procedure for the re-dispatch (per the project's sudo pattern: the password is in `sudo.txt` at /home/alepar/AleCode/bazerame-fans/sudo.txt, gitignored; each sudo call waits ~30 s on a fingerprint-reader timeout first, so use Bash timeouts >= 120 s and batch root commands into one `sudo -S bash -c '...'` session): 1. `sudo -S bash -c 'modprobe -r ryzen_smu' < sudo.txt` — and do NOT reload it until the measurement is finished. 2. Confirm `ryzenadj --info` now prints the table with the `fallback to /dev/mem` banner. 3. Command a sustained cap with `ryzenadj --slow-limit=<mw> --stapm-limit=<mw> --fast-limit=<mw>`, drive a CPU-bound load, and sample `PPT LIMIT SLOW` vs `PPT VALUE SLOW` (and/or RAPL `energy_uj` differentiated over the 5 s allocator cadence) at several cap levels to get the commanded-versus-drawn steady-state spread. That spread is CPU `DEMAND_MARGIN_W`. Record numbers, method and date in §Facts. 4. When done, restore the module (`modprobe ryzen_smu`) since you were the one who unloaded it, matching smu_module.rs's restore-iff-we-unloaded contract. On the GPU axis: do not treat your existing number as blocked either. The spec's own clock-lock pin criterion (§2.9 / fwloop.7 `verify_lock`: util > 90 %, sm <= locked + 30, 3 samples) is the standard that measurement is held to, and your run met it — 1492 MHz against a 1500 MHz lock at 95–98 % util for 7 ticks, spread ~0.11 W. Record it in §Facts with an explicit limitation sentence naming glxgears as a fill/geometry-bound proxy and the heavier generators that were unavailable (the spec already uses that "Limitation, stated because the numbers are used as a plant" pattern for the EC autofan curve at lines 85–91 — follow it). Do not install new tooling and do not block on it. Nothing else about the task changes: the four fixed invariants of §2.4 stand, no value may be assumed, and the deliverable is still the full candidate x scenario sweep tabulated in the spec plus the §2.4 rewrite replacing the "Open for the spike to decide and record" list with the decided rule and its per-axis constants.
Task 11 (fw-fanctrl-loop-9it): complete (already merged into epic-fw-fanctrl-loop-6ma-integration before this re-entry — bead closed, no new review)
Merge: fw-fanctrl-loop-iym — rebase clean · seam-review none · gate pass
Task 16 (fw-fanctrl-loop-iym): minor (deferred): src/control/mode.rs:646 — `warmed_up()`'s sanity-check assertion `d.reasons.contains(&"unreconciled") || d.mode != LoopMode::TempLoop` has an unfailable right-hand side: `d.mode` can never be `TempLoop` after a single `decide()` call because entry hysteresis requires 3 consecutive ticks (entry_streak starts at 0), so this assertion passes unconditionally regardless of the `unreconciled` reason's correctness. Per assertion-discipline: harmless (not counted toward any of the 14 acceptance tests, per the implementer's own note) but should either be removed or rewritten to actually gate on the reason text.
Task 16 (fw-fanctrl-loop-iym): minor (deferred): src/control/mode.rs:537 — `flags.dedup()` only removes *consecutive* duplicates. It happens to work today because all `StatusFlag::TargetUnreachable` push sites (sub-floor at ~404, feasibility at ~435, low/high bound-hold at ~444/451) are mutually exclusive or contiguous, but that's an implicit invariant a future edit could silently break (e.g. inserting an unrelated flag push between two TargetUnreachable pushes would leave a real duplicate in the output). Consider `if !flags.contains(&StatusFlag::TargetUnreachable)` guards, or dedup at the end regardless of adjacency (e.g. sort_unstable+dedup if flag ordering isn't meaningful, or a small ordered-set helper).
Task 16 (fw-fanctrl-loop-iym): complete (commits 36de70b..8c95e95, review clean)
Merge: fw-fanctrl-loop-sov — rebase clean · seam-review none · gate pass
Task 14 (fw-fanctrl-loop-sov): minor (deferred): src/sensors/sampler.rs:56-66 `find_chip_dir` duplicates `Hwmon::discover`'s directory-scan logic (src/sensors/hwmon.rs:20-38), rescanning the same `hwmon_root` a second time at construction just to find the `cros_ec` chip dir; understandable given hwmon.rs is outside this task's filesTouched, but worth collapsing into a `Hwmon` accessor next time that file is touched.
Task 14 (fw-fanctrl-loop-sov): minor (deferred): task-14-report.md's self-review claims `#[allow(clippy::too_many_arguments)]` on `Sampler::with_paths` 'match[es] the existing pattern elsewhere in this codebase' -- verified false: `grep -rn 'too_many_arguments' src/` shows this is the only occurrence in the crate (the other two `#[allow(clippy::...)]` sites are for unrelated lints). Doesn't affect the code; the rationale in the report is just inaccurate.
Task 14 (fw-fanctrl-loop-sov): minor (deferred): A panic while any thread holds the `fanctrl`/`nvme_cache` mutex (e.g. inside `FanctrlSource::poll`) poisons it via the `.expect("... mutex poisoned")` pattern (src/sensors/poller.rs:226,232,308; src/sensors/sampler.rs:765), so every later access -- including the sampler's own `merge_fanctrl` -- panics too. This mirrors an existing codebase convention (test-only `lock().unwrap()` elsewhere), so it isn't new, but it sits somewhat against this task's own stated goal of isolating a hung/failing dependency from the control loop.
Task 14 (fw-fanctrl-loop-sov): complete (commits 96cf290..c8373fc, review clean)
Task 12 (fw-fanctrl-loop-24s): fix round 1/5 (1 addressed, 0 open — Test changes: ran `git diff --stat 1ee0ffa0ae6026a0758491ac631000f11661e875..HEAD -- 'src/**/*.rs' 'tests/**'` (1 file, controller.rs, 193 insertions/2658 deletions) and the matching full diff restricted to those pathspecs — identical to the review package's diff (already fully read, single file, 3575 diff lines). Spec compliance: the tier deletion itself is correct — grep in the final controller.rs for thermal_model/kalman/trust/cooldown/adapt_bias/adapt_gain/persisted_bias returns only explanatory prose (matches report's step-5 evidence), Auto entry now gates on `lut` alone (§3.2), and the degenerate-contour stub is a sound, well-reasoned call given Task 6 (fw-fanctrl-loop-zct) is unmerged on this branch — correctly flagged as a discovered dependency gap rather than silently invented. NEEDS_FIX — unauthorized test deletions. The brief's delete list is narrow: "every controller test that exercises them [the tier] — the KF, trust, cooldown and fitted_model blocks," and the report's own broader rationale extends only to "every earlier contour-following 'auto mode' test that asserted exact model-derived wattage." Several deleted tests satisfy neither: they exercise surviving, tier-independent production code (unchanged by this diff) and assert nothing about the model/contour. - `out_of_range_config_floors_never_panic_auto` (base controller.rs:3791) — a **crash-regression test** ("Reviewer-confirmed crash pre-fix: gpu_floor_mhz > 3090 made the PI's f64::clamp panic... Controller::new sanitizes"). This guards a previously-fixed panic in Auto entry sanitization, has nothing to do with KF/trust/cooldown/contour, and its production code path is untouched by this diff. Deleting the only regression test for a real historical crash, with no mention in the report, is a genuine safety-net loss. - `fan_slope_estimate_needs_full_valid_span` (base :3228) — the only dedicated unit test of `fan_slope_rpm_s`, which this diff explicitly **keeps** as production code (still called at controller.rs:1084, still documented as feeding the allocator's velocity gate, "the exact mechanism of the field relay" per its own doc comment). Its edge cases (window shorter than span+1, NaN inside the span, flat vs. rising slope) are pure-function tests with no model/contour dependency at all, and are not covered anywhere else in the repo (confirmed: no dedicated test of this function exists in allocator.rs either). - `pi_rate_reference_tracks_hardware_on_failed_gpu_set` (base :3851), `gpu_pi_seeded_from_applied_lock_at_entry` (base :3608), `fan_invalid_freezes_allocator_but_pi_keeps_working` (base :3566, partially) — GPU PI mechanics (hardware-failure rate-limit tracking, entry seeding from an applied lock, PI continuing off `gpu_w` independent of a lost fan sensor). None of this is adaptation-tier or contour behavior; the PI/LUT code path is untouched by the diff. - `manual_and_calibration_commands_rejected_in_auto` (base :3486) — command-gating while in Auto mode (SetCpuW/SetGpuMaxClock/StartCalibration rejected, SetFanTarget allowed); asserts relative call counts, not model-derived wattage. - `set_fan_target_persists_config_on_change_only` (base :3629), `config_fast_limit_reaches_ryzenadj` (base :3666), `config_seeds_status_fan_target` (base :3699) — plain Config/manual-mode tests with no Auto/model/contour involvement whatsoever. None of these deletions are mentioned, itemized, or justified anywhere in the task-12 report's "Test-only" inventory or "What survived" sections — the report accounts for the fitted_model/lut3/calibrated() fixtures and the contour-following auto-mode block, but is silent on this second group. Repo-wide grep confirms none were relocated or renamed elsewhere. Fix: restore these tests (updating only what the LUT-only/no-contour change actually requires — e.g. `calibrated()`'s new LUT-only fixture, and any assertion that literally depended on the deleted contour producing a specific wattage sequence) rather than deleting them outright. `out_of_range_config_floors_never_panic_auto` and `fan_slope_estimate_needs_full_valid_span` in particular should port over essentially unchanged — neither touches the model or the allocator's contour input at all.; commits 1ee0ffa..4969286)
Merge: fw-fanctrl-loop-24s — rebase clean · seam-review none · gate pass
Task 12 (fw-fanctrl-loop-24s): minor (deferred): controller.rs:602 — the contour stub `let contour: &dyn Fn(f64) -> Option<f64> = &|_pc: f64| None;` plus the doc note explain the degenerate-everywhere behavior well, but a one-line inline comment at the AllocInput{contour, ...} field-init site itself (not just the stub's construction 6 lines up) would help a Task-6/19 reader who jumps straight to the call site.
Task 12 (fw-fanctrl-loop-24s): minor (deferred): Report claims 'zero warnings anywhere in controller.rs' from clippy but shows the check filtered via `grep -c controller.rs` against clippy's output rather than quoting the raw clippy invocation's exit code/summary line — a stronger form of the same evidence (e.g. `cargo clippy ... 2>&1 | tail -5`) would be more convincing, though not disputed here.
Task 12 (fw-fanctrl-loop-24s): complete (commits f243959..338c000, review clean)
Merge: fw-fanctrl-loop-mjv — rebase conflict: 1 files · seam-review none · gate fail → blocker
Task 15 (fw-fanctrl-loop-mjv): pending retry — RESOLVE: The `src/control/controller.rs` conflict is large in line count but semantically trivial: the epic has ALREADY done almost all of the controller.rs work your branch did. Do not attempt a line-by-line reconciliation of the conflict markers. Instead, resolve by taking the epic's version of `src/control/controller.rs` verbatim (`git checkout --ours src/control/controller.rs` during the rebase, i.e. the `epic-fw-fanctrl-loop-6ma-integration` side) and then applying ONE small edit to it. Verified facts behind this: 1. Both of the tests your branch deleted — `kf_decisions_reach_telemetry_with_offset` and `model_snapshot_decision_every_60s_in_auto` — are already gone from the epic tip (cf53a2d): `git grep` finds neither. Task 12 (`fw-fanctrl-loop-24s`, "Remove the adaptation tier") deleted them along with ~3000 lines of adaptation-tier code. The giant ~1700-line conflict hunk (merged-file lines 2943-4656) is only git failing to anchor two heavily divergent regions; your branch's sole net contribution inside it is those two test deletions, which the epic already made. Taking the epic side there loses nothing of yours. Replace your two explanatory tombstone comments with nothing — the epic's own removal commit already carries the rationale. 2. Your closure-signature change (`|cause, alloc|`, dropping the `model` parameter) is already in the epic verbatim at `apply_effects` — the epic's `let decision = |cause: &'static str, alloc: Option<(f64, f64, f64, f64)>| Record::Decision { ... }`. 3. The `model_snapshot` binding no longer exists anywhere in the epic's controller.rs, so your `if model_snapshot.is_some() { t.log(&decision("auto:model_snapshot", None)); }` block CANNOT be carried forward — it would not compile. Drop it (take the epic side of that second conflict hunk). The "auto:model_snapshot" cause label is gone entirely on the epic side; that is correct and expected, and `fw-fanctrl-loop-eyi`'s deletion sweep owns any leftovers. THE ONE EDIT to apply on top of the epic's file, in the `decision` closure inside `apply_effects` (epic controller.rs ~lines 1685-1692): delete the three-line comment "The adaptation tier (Kalman trim/gain, model snapshot) is gone — these fields are no longer ever populated, and stay None until a later task retires them from `Record::Decision` itself." together with the six fields it introduces: trim_rpm: None, gain: None, model_a: None, model_b: None, model_e: None, model_c: None, and replace them with your task's three fields, keeping your comments: t_star: status.t_star_c, budget_w: status.budget_w, freeze: None, Keep the epic's flatter single-expression closure formatting (`| ... | Record::Decision { ... };`) rather than your branch's braced `{ Record::Decision { ... } }` form — the field set is what matters, not the brace style. `ControlStatus` on the epic side carries `t_star_c: Option<f64>` and `budget_w: f64` (confirmed at epic controller.rs lines 326/343) but has NO `freeze` field, so `freeze: None` is correct and matches the design's "not yet wired" state — the `Option<&'static str>` `freeze` on `Effect::AutoAllocated` is a different, unrelated field; do not wire it in here. Note `Record::Decision.freeze` is `Option<String>` in your telemetry.rs, so `None` needs no coercion. Nothing else in the epic constructs `Record::Decision` outside `src/telemetry.rs` (whose tests your branch rewrote and which merged cleanly), so no other call site needs updating. `src/main.rs`, `src/telemetry.rs` and `src/ui/view.rs` all merge conflict-free — a `git merge-tree --write-tree --merge-base=f243959 cf53a2d 599cbf8` confirms `src/control/controller.rs` is the ONLY conflicted path. This resolution is exactly what design §3.5 requires ("`decision` lines add `mode`, `t_star`, `budget_w`, `freeze`; `trim_rpm`, `gain`, `model_*` are removed") and what §4 lists for deletion ("telemetry `trim_rpm/gain/model_*`", "the adaptation tier and its tests in `controller.rs`"). Note also that `src/control/controller.rs` is NOT in Task 15's declared filesTouched (`src/ui/view.rs`, `src/telemetry.rs`, `src/model.rs`) — the controller.rs edit is only the unavoidable call-site fixup for the wire-format change, so keep it minimal: the field swap above and nothing more. Then run the gate: `cargo test` (plus `cargo clippy` per the task's step 6).
Merge: fw-fanctrl-loop-51b — rebase clean · seam-review none · gate pass
Task 17 (fw-fanctrl-loop-51b): minor (deferred): src/test_support/mod.rs's doc comment for the `plant` module still reads "Empty stub here so this module compiles" (outside this task's filesTouched, so not this task's fault, but now stale and worth a follow-up touch-up).
Task 17 (fw-fanctrl-loop-51b): minor (deferred): ThermalPlant::tick() calls EcReading::read(&self.dir).expect(...), which would panic if a future caller (e.g. Task 22) scripts ambient_base_c/ambient_c/charger_c at or below 0.0 with gpu_ec unset, since all EC-positive-reading channels would be filtered out leaving nothing for EcReading::read to return Some for. Not exercised by any test here and not disallowed by the brief, but worth a defensive note for Task 22 authors.
Task 17 (fw-fanctrl-loop-51b): complete (commits cf53a2d..5c791f9, review clean)
Merge: fw-fanctrl-loop-mjv — rebase clean · seam-review cleared · gate pass
Task 15 (fw-fanctrl-loop-mjv): minor (deferred): src/control/controller.rs:288 — flag_severity() (pre-existing dead code from an earlier task, #[allow(dead_code)], no production call site) classifies StatusFlag::NvmeHot as Severity::Info, but design §3.5 states NVME HOT is warning severity and view.rs's new render_priority/flag_span (this task) correctly render it as warning/yellow — the implementer left an explicit doc comment (src/ui/view.rs:189-197) flagging the divergence rather than fixing it, which is reasonable since controller.rs is outside this task's filesTouched, but it leaves a latent trap for whoever eventually wires flag_severity into production code.
Task 15 (fw-fanctrl-loop-mjv): complete (commits 60b054e..1031c49, review clean)
Detector: round 1 — 6 ready · topped-up 11 · cap 4 · peak in-flight 4 · top-up queries 15/40 · hot-file deferrals: none (expected serialisation point src/control/controller.rs did not bind)
Sweep: 9f112ed — 0 passed, 0 failed, 83 errors, 0 skipped; failing: src/actuators/mod.rs:33:5, src/actuators/gpu.rs:32:7, src/actuators/gpu.rs:36:7, src/actuators/gpu.rs:40:7, src/actuators/gpu.rs:50:12, src/actuators/gpu.rs:57:12, src/calib/fopdt.rs:46:11, src/calib/fopdt.rs:48:11, src/calib/fopdt.rs:52:7, src/calib/fopdt.rs:55:7, src/calib/fopdt.rs:57:7, src/calib/fopdt.rs:61:7, src/calib/fopdt.rs:62:7, src/calib/fopdt.rs:69:12, src/calib/fopdt.rs:90:8, src/calib/fopdt.rs:118:4, src/calib/fopdt.rs:165:4, src/calib/fopdt.rs:223:8, src/calib/fopdt.rs:236:4, src/control/allocator.rs:307:12; command: cargo clippy --all-targets -- -D warnings — not in this measurement (escalated or pending retry, never merged): fw-fanctrl-loop-zct, fw-fanctrl-loop-dsh
Metrics: merges 0 · merge-failed 0 · rebase-conflicts 0 · seam-reviews 0 (fixed 0) · gate-fails 0
Metrics: fix-loop round 1: 0 addressed / 0 entered · round 2: 0 addressed / 0 entered · round 3: 0 addressed / 0 entered · round 4: 0 addressed / 0 entered · round 5: 0 addressed / 0 entered
Metrics: fix-loop breaker-tripped: 0
Metrics: ledger-check M≠completed: 0 vs 15 · append-failed 0 · append-retried 0
Launch: args {"epicId":"fw-fanctrl-loop-6ma","integrationBranch":"epic-fw-fanctrl-loop-6ma-integration","integrationWorktree":"/var/home/alepar/AleCode/bazerame-fans/.claude/worktrees/fw-fanctrl-loop/.worktrees/epic-fw-fanctrl-loop-6ma-integration","config":{"concurrency":4,"hotFileCap":3,"topUpQueryCap":40,"edgeAuditCap":3,"gate":"cargo test","sweep":"cargo clippy --all-targets -- -D warnings","testPaths":["src/**/*.rs","tests/**"],"models":{"planner":"opus","implementer":"sonnet","reviewer":"sonnet","mechanical":"sonnet","triage":"opus","finalReview":"opus","fixEscalation":"opus"}},"dryRun":false}
Merge: fw-fanctrl-loop-dsh — rebase clean · seam-review cleared · gate pass
Task 13 (fw-fanctrl-loop-dsh): minor (deferred): controller.rs's two model-field test-site fixes (full_calibration_persists_state_and_keeps_lut, persisted_state_seeds_controller_lut) and calib/runner.rs's SaveState construction fix sit outside the brief's stated filesTouched and its literal 'nothing else in that file' restriction on controller.rs; both are compile-forced by the schema field removal, minimal, and were pre-flagged as an expected gap by blocker bead fw-fanctrl-loop-bwt's suggested resolution -- not a defect, but worth the controller noting the boundary was crossed.
Task 13 (fw-fanctrl-loop-dsh): minor (deferred): The report's stated justification for dropping the model.a assertion in full_calibration_persists_state_and_keeps_lut ('the LUT assertion right below it already covers what the test's docstring claims') is imprecise -- the LUT check doesn't cover fit-accuracy, it's simply that the field no longer exists to assert; fit accuracy remains independently covered by calib/runner.rs's own tests. No coverage lost, just loose wording in the report.
Task 13 (fw-fanctrl-loop-dsh): minor (deferred): state.rs's missing_file_gives_default has four per-field asserts (lut/duty_rpm_table/warm_start/loop_gains) that are logically implied by the preceding assert_eq!(state, PersistedState::default()) -- already self-flagged by the implementer's own report under assertion discipline, kept for readability matching pre-existing style; no action needed.
Task 6 (fw-fanctrl-loop-zct): fix round 1/5 (1 addressed, 0 open — Two verified, in-scope defects in this diff (both confirmed against the actual compiled Rust, not just read): 1. (Important — violates the bead's verbatim acceptance criterion "slew clamp bounds per-tick change") In `Allocator::step` (src/control/allocator.rs), the "Fix round 1" reordering — quantize the rate-clamped value, THEN re-apply the floor last, unquantized — can push a single step's commanded delta up to ~0.25 W (half a grid step) beyond `UP_RATE_W`/`DOWN_RATE_W` whenever the previous commanded point sits off the 0.5 W grid. This is not a rare corner case: `gpu_floor_w` is always an LUT-interpolated wattage (controller.rs ~1180, `lut.watts_for_clock(...)`), so it is essentially always non-grid-aligned, and `cpu_floor_w` is a raw config `f64` that `Config::sanitized()` (src/config.rs:204-215) only clamps to `[0, cpu_max_w]`, never grid-aligns. Concrete repro (compiled and run against the exact quantize/clamp/floor arithmetic): with `prev = cpu_floor_w = 15.751` (a legal config value) and a large budget, step 2 computes `raw.clamp(15.751-8.0, 15.751+2.0) = 17.751`, `quantize(17.751) = 18.0` (nearest 0.5), then `.max(15.751)` is a no-op since 18.0 already exceeds the floor — so the commanded delta is `18.0 - 15.751 = 2.249 W`, i.e. 0.249 W over `UP_RATE_W = 2.0`. The two new regression tests added for "Fix round 1" (`a_non_grid_aligned_cpu_floor_is_met_exactly_through_step` / `..._gpu_...`) only assert the single floor-pinned step, not the very next (climbing) step where this manifests, so it went uncaught. Suggested fix: re-clamp the quantized value back into `[prev - DOWN_RATE_W, prev + UP_RATE_W]` before the final `.max(floor)`, and add a regression test that steps twice from a non-grid floor and asserts the second delta stays within the rate bound. 2. (Important — violates the Global Constraint "cargo clippy -D warnings clean for the code you touched") This task's own call-site edit in src/control/controller.rs removed the local `fan_slope` binding (`fan_slope_rpm_s(fan_window)`) that fed the now-deleted `AllocInput::fan_slope_rpm_s`, but left the free function `fan_slope_rpm_s` (controller.rs:516) and the const `FAN_SLOPE_SPAN_S` (controller.rs:78) in place with no remaining production caller. `cargo build --bins` on HEAD (271d8ff) confirms two new warnings that did not exist before this task: `warning: function 'fan_slope_rpm_s' is never used` and `warning: constant 'FAN_SLOPE_SPAN_S' is never used`. The report's own clippy verification explicitly scoped its check to `src/control/allocator.rs` only ("zero findings against src/control/allocator.rs") and never checked controller.rs, so this was missed — despite the report correctly performing the exact same cleanup for the analogous `FAN_SMOOTH_N` const in the same edit. Note `fan_slope_rpm_s` still has its own dedicated unit tests (controller.rs:3192-3207) that would need removing/relocating too if the function is deleted; worth a quick check with whichever task next owns controller.rs (fw-fanctrl-loop-j6s) before deleting outright, in case it's about to be reused. Everything else in the diff checks out: `split_budget`'s floor/proportional-split/cap-reassignment/unspent-surplus logic is correct on inspection and by focused `cargo test control::allocator::` (26/26 pass, confirmed independently); the AllocInput field-shape choice (`gpu_floor_w` over the design doc's stray `lut`) is a documented, pre-resolved ambiguity (cited roast doc line matches); `cargo build --bins` is clean of errors at HEAD (confirmed independently); the massive deletion of the old contour/deadband/velocity-gate/overshoot machinery and its tests matches the brief's explicit "Deleted here" list; `demand()` is untouched as required; `src/control/mod.rs` correctly left untouched.; commits 1ee0ffa..64a5f50)
Task 13 (fw-fanctrl-loop-dsh): complete (commits 9f112ed..d3a5e42, review clean)
Merge: fw-fanctrl-loop-nez — rebase clean · seam-review none · gate pass
Task 25 (fw-fanctrl-loop-nez): complete (commits fbe1f14..673002c, review clean)
Merge: fw-fanctrl-loop-zct — rebase clean · seam-review cleared · gate pass
Task 6 (fw-fanctrl-loop-zct): minor (deferred): The 'Seam fix' commit (271d8ff) goes beyond the brief's literal 'confined to the allocator.step call site only. Nothing else in that file' restriction on controller.rs — it removes the two dangling overshoot_settle_* call sites, deletes two tests plus their shared fixture, and strips a telemetry-cause assertion. This was necessary to keep the integration branch compiling (well-documented in the report) and is a reasonable call, but it preempts part of fw-fanctrl-loop-24s's stated scope and could cause a merge/rebase conflict when that task lands — worth a heads-up to whoever runs it next.
Task 6 (fw-fanctrl-loop-zct): minor (deferred): src/control/controller.rs still contains ~24 known-failing control::controller tests (e.g. flat_fan_window_lets_raises_proceed, rising_fan_window_gates_allocator_raises) that assert on the old contour allocator's velocity-gate/taper behavior via ctl.on_sample(...) and now fail under the placeholder budget_w = floor sum (which yields a constant, non-climbing allocation). This is explicitly disclosed and squarely deferred to fw-fanctrl-loop-24s per the report/brief, not a defect of this task, but flagging so the reviewer of that follow-on task knows these specific tests are the ones to look at first.
Task 6 (fw-fanctrl-loop-zct): complete (commits d458fd5..3ece172, review clean)
Merge: fw-fanctrl-loop-0nv — rebase clean · seam-review cleared · gate pass
Task 18 (fw-fanctrl-loop-0nv): minor (deferred): src/calib/runner.rs test `burner_starts_before_the_settle_hold_and_stops_after_the_step`: leaves dead cruft `let entry_effects_idx = 0; ... let _ = entry_effects_idx;` that does nothing — should be deleted.
Task 18 (fw-fanctrl-loop-0nv): minor (deferred): The exact MIN_STEP_DELTA_W (1.0 W) boundary is not asserted anywhere (unloaded_step test uses delta_w=0.0, well clear of the threshold); self-disclosed as unmeasured in the report's own ASSERTION DISCIPLINE section.
Task 18 (fw-fanctrl-loop-0nv): complete (commits 7bd9ddd..78a5f34, review clean)
Merge: fw-fanctrl-loop-7ij — rebase clean · seam-review none · gate pass
Task 23 (fw-fanctrl-loop-7ij): complete (commits e2cbf2f..39a1950, review clean)
Merge: fw-fanctrl-loop-eyi — rebase clean · seam-review none · gate pass
Task 21 (fw-fanctrl-loop-eyi): minor (deferred): Cargo.toml still declares `nalgebra = "0.33"` (line 15), but this task deleted kalman.rs and thermal_model.rs, the only two files in the repo that imported nalgebra (confirmed: `git grep nalgebra` at base e2cbf2f finds only those two files; after this task, `grep -rl nalgebra --include='*.rs' .` returns nothing). The deletion sweep left a now-fully-unused crate dependency behind. Not one of the bead's eleven named banned terms, so it doesn't fail the acceptance criteria, and it doesn't break build/test/clippy — polish-only.
Task 21 (fw-fanctrl-loop-eyi): complete (commits d82caa7..fd42af4, review clean)
Detector: round 1 — 3 ready · topped-up 4 · cap 4 · peak in-flight 3 · top-up queries 6/40 · hot-file deferrals: none (expected serialisation point src/control/controller.rs did not bind)
Sweep: MEASUREMENT INVALID: `cargo clippy --all-targets -- -D warnings` did not complete a lint pass — the crate fails to *compile* before clippy can check it, due to 46 `dead_code` lints escalated to hard errors by `-D warnings` (unused consts/fns/structs/enums/fields across `src/fanctrl/table.rs`, `src/sensors/ec.rs`, `src/types.rs`, and others), terminating with "could not compile `bazerame-fans` (bin \"bazerame-fans\") due to 46 previous errors" (exit code 101). This is a compilation/collection failure, not a completed lint run, so no valid passed/failed/errors/skipped breakdown exists to report. Tip SHA at measurement time: `247c2d1ad3c115cb967a22058719bc6a2214a232` (note: this is 41 hex characters, one longer than a normal SHA-1 — `git rev-parse HEAD` output was captured verbatim from the tool call and is reproduced as-is; worth double-checking with the orchestrator/repo directly if a clean 40-char SHA is needed). Full raw output saved at: `/tmp/claude-1000/-var-home-alepar-AleCode-bazerame-fans--claude-worktrees-fw-fanctrl-loop/6e6f1052-b05b-421f-abbd-edfbee8e089d/scratchpad/clippy_out2.txt` — not in this measurement (escalated or pending retry, never merged): fw-fanctrl-loop-j6s
Metrics: merges 20 · merge-failed 2 · rebase-conflicts 1 · seam-reviews 8 (fixed 1) · gate-fails 2
Metrics: fix-loop round 1: 2 addressed / 2 entered · round 2: 0 addressed / 0 entered · round 3: 0 addressed / 0 entered · round 4: 0 addressed / 0 entered · round 5: 0 addressed / 0 entered
Metrics: fix-loop breaker-tripped: 0
Metrics: ledger-check M≠completed: 20 vs 21 · append-failed 0 · append-retried 0
Launch: args {"epicId":"fw-fanctrl-loop-6ma","integrationBranch":"epic-fw-fanctrl-loop-6ma-integration","integrationWorktree":"/var/home/alepar/AleCode/bazerame-fans/.claude/worktrees/fw-fanctrl-loop/.worktrees/epic-fw-fanctrl-loop-6ma-integration","config":{"concurrency":4,"hotFileCap":3,"topUpQueryCap":40,"edgeAuditCap":3,"gate":"cargo test","sweep":"cargo clippy --all-targets -- -D warnings","testPaths":["src/**/*.rs","tests/**"],"models":{"planner":"opus","implementer":"sonnet","reviewer":"sonnet","mechanical":"sonnet","triage":"opus","finalReview":"opus","fixEscalation":"opus"}},"dryRun":false}
Task 11 (fw-fanctrl-loop-9it): fix round 1/5 (1 addressed, 0 open — src/control/budget.rs's own module docs (lines 24-25) and Budget::set_demand_state's doc comment (lines 248-250) — pre-existing, unchanged by this diff — explicitly state: "The concrete predicate is a placeholder; `fw-fanctrl-loop-9it` (a measurement spike) owns the real rule and rewrites this section and its callers" and "Placeholder — owner: `fw-fanctrl-loop-9it`. That spike measures the real rule against every scenario ... and rewrites this function (and §2.4) accordingly." This diff never touches budget.rs and never calls set_demand_state anywhere — grep confirms zero occurrences of "set_demand_state" in the whole diff (task-11-review-initial.diff) and in task-11-report.md. The decided predicate (demand_limited_axis, which needs draw, cap, floor AND a per-axis DEMAND_MARGIN_W) lives only inside the throwaway #[cfg(test)] harness and is structurally incompatible with set_demand_state's existing production signature (`&[(f64,f64)]` draw/cap pairs + error_sign, no floor, no margin — its body is a raw `draw >= cap` pin check, exactly the naive form the spike's floor-guard fix (scenario 5) was built to replace). The design's own plan/spec table (fwloop.4 and fwloop.12 rows in this same doc, further down, unchanged by this diff) treats `Budget::set_demand_state` as the literal seam fwloop.12/j6s will call with "the per-axis smoothed draws, their caps and the error sign" to get the decided rule — but that seam cannot express the decided predicate as currently typed, and nothing in this diff flags that mismatch. The report's "Concerns for the coordinator: None outstanding for this task" overlooked this even though the implementer was demonstrably working in budget.rs's neighborhood (the report discusses Budget::step / Freeze::DemandLimited internals in detail, e.g. the raw_du > 0.0 gate). Net effect: §2.4 now reads as "implementable," but the one concrete seam other code (fwloop.12) is documented to call to implement it is left both stale (falsely still claiming ownership by this exact bead) and incapable of carrying the decided rule (wrong signature). Fix, within this task's own scope: update the two stale doc comments (budget.rs:24-25, budget.rs:248-250) so they no longer claim fw-fanctrl-loop-9it "rewrites this section and its callers" — redirect that ownership to fw-fanctrl-loop-j6s (Controller loop integration) and note that wiring §2.4's decided predicate into set_demand_state requires widening its signature to carry each axis's floor and per-axis DEMAND_MARGIN_W (not just draw/cap pairs), which it cannot express today. If budget.rs truly is out of this task's scope (per the brief's filesTouched), the report should still have surfaced this gap explicitly instead of declaring none outstanding, so the coordinator can decide whether it needs its own follow-up bead before fwloop.12/j6s starts.; commits 247c2d1..dce5870)
Merge: fw-fanctrl-loop-9it — rebase clean · seam-review none · gate pass
Task 11 (fw-fanctrl-loop-9it): minor (deferred): src/control/spike_antiwindup.rs run_scenario: `error_sign = e_c.signum()` — Rust's f64::signum returns 1.0 for exactly +0.0, so an exact-zero error is silently treated as the 'deepening' direction rather than neutral; harness-only and unlikely to hit exactly (floating point), but undocumented and worth a one-line comment or an explicit `> 0.0` vs `>= 0.0` note so a future reader doesn't have to rediscover the Rust signum(0.0) quirk.
Task 11 (fw-fanctrl-loop-9it): minor (deferred): The harness drives allocator::split_budget directly (the documented 'pure function: no rate limiting, no grid quantisation' variant) rather than the production Allocator::step wrapper that adds slew-rate clamping (UP_RATE_W/DOWN_RATE_W) and grid quantization every real tick; this is a reasonable throwaway-harness simplification but is never stated as a limitation in the report or in §2.4's sweep writeup, and scenario 7's chatter comparison (81 vs 29 transitions) is measured against caps that can jump instantly rather than the rate-limited caps production will actually see.
Task 11 (fw-fanctrl-loop-9it): complete (commits 247c2d1..dce5870, review clean)
Task 19 (fw-fanctrl-loop-j6s): fix round 1/5 (1 addressed, 0 open — Acceptance-criteria coverage gap, self-disclosed by the implementer but not fixed: the bead's acceptance criteria say (verbatim) the anti-windup scenarios `fwloop.24` decided must be 'replayed at controller level against the rule it chose — at minimum: an idle tick ... ; a seeded-high budget with a lighter load still integrates down; a CPU-only tick with the dGPU unpowered keeps u off its lower bound; and a GPU HOT episode behaves as the spike specified.' Only the idle-tick scenario is actually replayed at controller level (`demand_limited_anti_windup_eventually_holds_an_idle_budget_off_the_ceiling`, src/control/controller.rs ~5078); the seeded-high/lighter-load, CPU-only/unpowered-dGPU, and GPU-HOT-episode scenarios are not, per the report's own 'Acceptance-criteria coverage' section. The GPU-HOT case is the most material of these three: it exercises code (`guard_state.gpu_hot` -> `gpu_share_override` -> `gpu_max_w` fed into `split_budget`/`last_gpu_cap_w`, controller.rs ~1628-1635) that the idle-tick test never touches, so it is not merely 'the same wiring with different data.' Separately, the acceptance criteria's actuator-verdict item ('a CPU and a GPU Mismatch each freeze, flag and reassert on the same tick ... three consecutive mismatches release to stock with the flag held ... a later Verified re-engages without a step') has zero controller-level (end-to-end, through the real write/read-back path) coverage — only `VerdictState`'s own state machine is unit-tested directly via `v.observe(...)` calls (controller.rs ~4640-4715), never through `run_budget_and_allocate`'s or `run_gpu_pi`'s actual write -> re-read -> observe -> apply_verdict_outcome chain with a scripted FakeRunner `ryzenadj --info` table. I verified this is buildable, not blocked: `src/actuators/cpu.rs`'s own private test module already has exactly this scripting pattern (`info_output`/`info_table_text`, cpu.rs ~222-260, used by `stale_read_back_yields_mismatch_naming_the_field` etc.) producing a genuine `WriteVerdict::Mismatch` from `set_sustained_mw` through `FakeRunner`; it just isn't exposed as a shared `test_support` helper controller.rs tests can reach, so the report's 'could not find or build a scripting helper' is an accurate description of what exists today but not evidence the gap is infeasible to close within a reasonable follow-up. Given these are explicit, named, 'at minimum' bead acceptance criteria (not generic 'coverage could be broader'), and the mismatch chain is the single largest, self-flagged gap in the report, this needs at least: (1) one controller-level test driving a CPU Mismatch through the real write path to release-then-recover (reusing/promoting cpu.rs's info-table scripting into a shared test_support helper), and (2) a GPU-HOT-episode replay at controller level confirming the demand-limited predicate does not spuriously fire during the guard's ratchet. The other two named anti-windup scenarios (seeded-high/lighter-load, CPU-only/unpowered-dGPU) are lower marginal value since they exercise the same wiring path the idle-tick test already covers with different data — listed as minors below rather than folded into this blocking finding.; commits 7bd9ddd..8d7b987)
Merge: fw-fanctrl-loop-j6s — rebase clean · seam-review cleared · gate pass
Task 19 (fw-fanctrl-loop-j6s): minor (deferred): Named anti-windup replay scenarios 'seeded-high budget with a lighter load still integrates down' and 'CPU-only tick with the dGPU unpowered keeps u off its lower bound' (bead acceptance criteria) are not separately replayed at controller level, only at the Budget/spike_antiwindup.rs level — lower risk than the GPU-HOT gap since they exercise the same controller-wiring path the covered idle-tick test already validates, but still a literal acceptance-criteria gap worth closing alongside the NEEDS_FIX item.
Task 19 (fw-fanctrl-loop-j6s): minor (deferred): The 'same-name curve edit calls resync_error and produces no kick' acceptance item has no dedicated controller-level test isolating the 'no kick' claim numerically — covered only by inference from `reengaging_from_released_reseeds_the_budget_without_a_step` plus Arbiter's own unmodified test suite; reasonable per the report's own tradeoff note, but flagged for visibility.
Task 19 (fw-fanctrl-loop-j6s): minor (deferred): controller.rs's demand-limited halt judges this tick's freeze against LAST tick's post-guard-override caps (`last_cpu_cap_w`/`last_gpu_cap_w`, controller.rs ~1674-1681), not literally 'this tick's own cap' as §2.4's prose reads most naturally ('the value split_budget actually produced ... for this tick'). The report discloses and justifies this as an unavoidable one-tick lag given the circular dependency between the freeze decision and `budget.step`'s own output; it does not violate any of §2.4's fixed invariants (directional-only, per-axis, no back-calc toward draw, visible), so it reads as a defensible implementation-sequencing choice rather than an invented rule, but the reviewer should be aware it is a real (if minor) divergence from the letter of the decided predicate.
Task 19 (fw-fanctrl-loop-j6s): complete (commits 42a0d0b..8d7b987, review clean)
Task 20 (fw-fanctrl-loop-438): fix round 1/5 (1 addressed, 0 open — Brief step 4 of "Implementation steps (TDD)" explicitly mandates "one case each" for the no-reseed rule: a strategy change, a snapped-duty change, and an AC unplug. Only the strategy-change case was implemented (`a_strategy_change_re_keys_without_reseeding_the_budget`, src/control/controller.rs ~line 5522). No dedicated test exists for a snapped-duty change or an AC unplug re-keying without reseeding. This is self-acknowledged by the implementer in the report's "Quality bar not fully reached" section: "I did not write dedicated tests for the AC-unplug and snapped-duty-change variants of the no-reseed rule ... a reviewer may reasonably want the other two spelled out explicitly." Code inspection confirms the underlying mechanism is a single shared `budget_seeded` gate keyed by `WarmStart::key(strategy, target_duty, on_ac)` that generalizes correctly across all three triggers, so this is a test-coverage gap, not a behavioral bug — but it is a literal, enumerated acceptance-criterion test that is missing. Fix: add two more tests mirroring `a_strategy_change_re_keys_without_reseeding_the_budget`'s two-parallel-sessions pattern (src/control/controller.rs, same test module, near line 5522): (1) a case where `rekey`'s LAST tick flips `on_ac` (true<->false) instead of strategy, with a different warm-start value pre-seeded under the new on_ac-keyed entry; (2) a case where `rekey`'s LAST tick's snapped `target_duty` changes (e.g. via a fan-target-RPM change that snaps to a different duty), with a different warm-start value pre-seeded under the new duty-keyed entry. Both must assert `base.status().budget_w == rekey.status().budget_w` after the re-key tick, proving that tick's delta-u is the ordinary PI increment, not a reseed.; commits dbfeecd..b89adaf)
Merge: fw-fanctrl-loop-438 — rebase clean · seam-review none · gate pass
Task 20 (fw-fanctrl-loop-438): minor (deferred): src/control/controller.rs ~5701: `calib_budget_w_stays_at_default_through_the_lut_sweep_before_any_setbudget` only asserts `status.budget_w == 0.0`, which stays 0.0 through the LUT sweep regardless of whether `Freeze::Calibrating` is actually applied to `calib_budget` — no `SetBudget` effect is ever emitted during `LutSweep` (confirmed: only src/calib/step.rs emits it), so the assertion can't distinguish 'frozen' from 'freeze code entirely absent'. The freeze itself is correctly wired (unconditional `calib_budget.step(LoopError::Temp{e_c:0.0}, Some(BudgetFreeze::Calibrating))` every `on_calib_sample`, verified by reading the code), but this test provides no protection against a regression of that specific wiring.
Task 20 (fw-fanctrl-loop-438): minor (deferred): src/control/controller.rs:607 and :1716-1717: doc comments still describe auto re-engagement from `Released` as re-seeding 'from the floors' — this task changed that path to seed from the warm-start map, falling back to floors only on a miss. The comment at the `run_budget_and_allocate` call site itself was updated to reflect this; these two other comments describing the same seeding behavior elsewhere in the file were not.
Task 20 (fw-fanctrl-loop-438): minor (deferred): Minor (new, in this fix diff): the two new no-reseed tests (an_on_ac_change_re_keys_without_reseeding_the_budget, a_snapped_duty_change_re_keys_without_reseeding_the_budget) exercise their key-change tick while both base and rekey are already under Freeze::DemandLimited, so that tick's raw_du (positive for both) is masked to du=0 on both sides — the assertion passes on a 0-vs-0 delta, not a genuine nonzero-but-equal 'ordinary PI increment' as the design/docstrings imply. This does not weaken the tests as regression guards for the actual finding (mutation-verified: temporarily forcing the seed-every-tick bug back in makes budget_w jump to the clamped hi=154 bound and all three no-reseed tests, including the pre-existing strategy-change one, fail immediately), but the in-code comments slightly overstate what that specific tick demonstrates.
Task 20 (fw-fanctrl-loop-438): complete (commits dbfeecd..b89adaf, review clean)
Task 22 (fw-fanctrl-loop-cm7): fix round 1/5 (1 addressed, 0 open — Task 22 (fw-fanctrl-loop-cm7), src/control/sim_tests.rs: the brief's "Faults" run-list section (task-22-brief.md, "Faults:" bullet) enumerates ~12 scenarios; only 3 are implemented (NVMe-hot at src/control/sim_tests.rs:2201, a steep-curve operating point at :2258, a resumed-edge test at :2284 that the report itself flags as weak — it would pass even without the resume clearing anything, since 100 pre-resume ticks is already short of the 40-sample steady-window regardless). Missing, with no test at all: the `high` unreachable case, a three-strike release followed by `Verified` re-engagement, a mismatch suppressed across an `on_ac` edge, a single confirmed read-back mismatch freeze, reconciliation A→B→A with reseed, and a scored view skipped for replica slewing. The task's own acceptance criteria (verbatim from the bead, task-22-brief.md) is "all listed runs pass deterministically" — this is a real, self-disclosed gap (report status is DONE_WITH_CONCERNS specifically because of it), not a misunderstanding. Two of the twelve (`GPU HOT` at 90°C, and by extension the mismatch-freeze race under FakeRunner's push-queue ordering) are legitimately blocked — GPU HOT by a verified real product defect (fw-fanctrl-loop-a78: the hard watchdog GPU_TRIP_C=87°C fires before the soft GpuHot guard's 90°C threshold is ever reachable) — and should follow the same `#[ignore = "..."]` + filed-bead pattern already used at sim_tests.rs:1457-1459 and :1912 for the TargetUnreachable gap (fw-fanctrl-loop-a5j, also verified real: `Controller::mirror_decision` at src/control/controller.rs:1667-1675 omits `StatusFlag::TargetUnreachable` from the five flags it syncs). Please add the remaining fault-matrix scenarios (or, where genuinely blocked by a verified defect, an `#[ignore]`d regression test with a filed bead, matching the existing precedent) so the run list is actually complete, rather than leaving six-plus items with zero test coverage.; commits 91b32e5..a66b892)
Merge: fw-fanctrl-loop-cm7 — rebase clean · seam-review none · gate pass
Task 22 (fw-fanctrl-loop-cm7): minor (deferred): src/control/sim_tests.rs: clippy `-D warnings` (the Global Constraints quality gate) is not clean for this new file — 7 warnings (4x needless_range_loop at :253/:274/:298/:313, type_complexity at :398, ptr_arg at :610, too_many_arguments at :825) fail `cargo clippy --bin bazerame-fans --tests -- -D warnings`; report ran plain `cargo clippy` without `-D warnings` and called it clean. (Note: the crate-wide `-D warnings` gate is already broken pre-task on unrelated dead-code lints in budget.rs/client.rs/curve.rs/ec.rs, so this isn't a regression relative to the integration branch, but the new file doesn't meet the stated bar either.)
Task 22 (fw-fanctrl-loop-cm7): minor (deferred): src/control/sim_tests.rs:791/797/803/809: the assertion message 'active:false must fall back to RpmLoop' is misleading — these baseline RpmLoop runs are forced via the load_step_forced_rpmloop_script argmax-pinning trick (documented at :674-687), not via Command::SetAuto/active:false; the dedicated active:false scenario is the separate run_active_false_authority tests.
Task 22 (fw-fanctrl-loop-cm7): minor (deferred): src/control/sim_tests.rs:2284-2328 (a_resumed_edge_mid_run_...): self-disclosed by the report as a weak test — it asserts warm_start is empty after a short pre-resume run + immediate resume + immediate save, but that outcome is identical whether or not the resume actually cleared anything (100 ticks is already short of the 40-sample steady window). Does not isolate the resume-specific clearing behavior it's named for.
Task 22 (fw-fanctrl-loop-cm7): minor (deferred): TraceRow gained a cpu_limit_w field (populated at all 3 TraceRow-construction sites) needed to distinguish a released CPU actuator from an engaged one in the new mismatch tests.
Task 22 (fw-fanctrl-loop-cm7): minor (deferred): a_high_unreachable_target_pins_at_the_upper_bound_and_raises_target_unreachable_high (new this round) bundles the currently-passing 'u pins at hi' assertion with the a5j-blocked TargetUnreachable-flag assertion under one #[ignore], unlike this same file's own established split pattern for equivalent bound-hold scenarios (active_false_below_flat_band_parks_at_floor_without_hunting / ...raises_target_unreachable_low_within_60s; a_sub_floor_target_holds_the_floor_without_relay / ...raises_target_unreachable_low) — so the working 'pins at hi' behavior isn't verified by the default (non-ignored) test run.
Task 22 (fw-fanctrl-loop-cm7): complete (commits 91b32e5..a66b892, review clean)
Merge: fw-fanctrl-loop-nsc — rebase clean · seam-review none · gate pass
Task 24 (fw-fanctrl-loop-nsc): minor (deferred): src/integration_tests.rs (wiring_sweep module, e.g. `every_calib_context_field_originates_from_live_data` ~L1795, `every_telemetry_field_is_populated` ~L1690, `every_effect_variant_is_applied` ~L1750, `every_status_flag_is_raised_and_rendered` ~L1640): these tests' actual `assert!`/`assert_eq!` calls compare a just-constructed literal to itself or check that a string-literal comment is non-empty -- self-referential/tautological (ASSERTION DISCIPLINE). The genuine verification is the compile-time exhaustive destructure/match (a new field/variant fails the build), which the brief explicitly asked for; the 'where it's read/raised/applied' claims are documented only in inline comments, never checked by execution. Not blocking -- matches the brief's explicit ask for compile-time rot-resistance over runtime coverage -- but worth trimming or relabeling so a reader doesn't mistake the assertions themselves for the verification.
Task 24 (fw-fanctrl-loop-nsc): minor (deferred): src/fanctrl/client.rs:272 -- `parse_print_all` now parses the `print all` JSON body twice per poll tick: once directly into `PrintAllResponse` (for strategy/ma_interval), and a second full `serde_json::from_str` inside `resolve_curve(json, &parsed.strategy)` for the curve. Correctly removes the duplicated lookup logic (the stated goal), but introduces a real (if cheap at typical poll rates) redundant re-parse the report does not mention.

Task 24 (fw-fanctrl-loop-nsc): complete (commits 9acb9bb..6beab0b, review clean)
Detector: round 1 — 1 ready · topped-up 4 · cap 4 · peak in-flight 1 · top-up queries 5/40 · hot-file deferrals: none (expected serialisation point src/control/controller.rs did not bind)
Sweep: 07b31dd — 0 passed, 0 failed, 0 errors, 0 skipped; failing: none; command: cargo clippy --all-targets -- -D warnings Note: `cargo clippy` is a lint/build check, not a test runner — it produces a pass/fail build result (clean, exit code 0, no warnings or errors), not per-test counts. Full output confirms a clean build with zero lint diagnostics.
Metrics: merges 25 · merge-failed 2 · rebase-conflicts 1 · seam-reviews 9 (fixed 1) · gate-fails 2
Metrics: fix-loop round 1: 6 addressed / 6 entered · round 2: 0 addressed / 0 entered · round 3: 0 addressed / 0 entered · round 4: 0 addressed / 0 entered · round 5: 0 addressed / 0 entered
Metrics: fix-loop breaker-tripped: 0
Metrics: ledger-check ok · append-failed 0 · append-retried 1
