---
super-roast verdict: Blocking (19 confirmed)
mode: design        iteration: 3 of 3
profile (assumed): Single-operator personal tooling with hardware side-effects: a Rust daemon on one Framework 16 laptop that writes CPU power limits via ryzenadj (sudo) and dGPU clock locks via NVML, and reads the EC through cros_ec/ectool. No network surface, no other users, no external data. Blast radius is the operator's own machine: a wrong cap or a stuck emergency release degrades performance or acoustics and, at worst, lets the hardware's own thermal protection (card slowdown 89 C / shutdown 92 C; EC trip points) take over. Rollback is a git revert and a daemon restart. Thermal-safety inversions, silent loss of control, control-loop instability (windup, chatter, starvation) and anything that defeats the hardware backstops are the material class; resilience/observability polish is low-value here.
inputs: /var/home/alepar/AleCode/fw-fan-quiet/.claude/worktrees/fw-fanctrl-loop/.worktrees/super-auto-per-device-temperature-loops/docs/superpowers/runs/2026-09-11-per-device-temperature-loops/2026-09-11-per-device-temperature-loops-design.md + /var/home/alepar/AleCode/fw-fan-quiet/.claude/worktrees/fw-fanctrl-loop/.worktrees/super-auto-per-device-temperature-loops/docs/superpowers/runs/2026-09-11-per-device-temperature-loops/task-tree-settled.md (epic fw-fanctrl-loop-eb9, 18 beads)
delta vs prior: 14 new confirmed (2 Blocking) · 0 carried (0 Blocking) · 22 resolved · 5 regressed (2 Blocking)
coverage: triage ok · 9 scouts ran (0 dead) · dedupe ok · 42 raw → 29 deduped → 25 panel + 4 spot (0 promoted) · judge completion 100% · remainder-capped: 0
independence: same-family (Claude) — seat-differentiated panel
seat-agreement: panels 25 · rr 0.72 · rg 0.84 · fg 0.64 · unanimous 0.60 · ground-loo 0.83 (n=18) · reproduce 20/5/0 · refute 13/12/0 · ground 22/3/0

Prior-report reconciliation (iteration 2, 27 confirmed): **regressed** — the tracking lock-up (iteration-2 Blocking #1; rev 3 replaced the in-band hold with hot-only tracking, which now ratchets the thermal candidate down through the shadow's draw signal — first Blocking entry below), sim 4's unsatisfiable bar (iteration-2 Blocking #3; the third rewrite still cannot fit the dead-zone crossing inside 3 λ), `DrawUnavailable` (the early return is gone but its anti-windup membership was not revisited), `Uncontrollable` vs the device loop (the park input became `ThermalMode::Bypass`, but the shadow's rise gate still contradicts "draw plus headroom"), and `gpu_shadow_enabled = false` / thermal-only paths (the ~950 MHz zero-gain plateau is back with no sim). **resolved** — the remaining 22 (relay-free parking, since parking and the band are deleted; directional anti-windup; the jump rule, by deletion; `P_limit`, by deleting the pinned test; body-vs-amendment contradictions, rewritten in place; the Auto-entry state (Held); Uncontrollable park input; the err<0 override; stuck-high-at-start (the accepted safe-direction disposition); per-label gate before argmax; GroupUnavailable exemption for absent groups; eb9.6 `Option<Gains>`; gain precedence + `gains_source`; ActuatorMismatch in the hold set; the inverted Held clamp; Released vs calibration; Held λ_eff bar; the 0.4 °C/W heat equation; the unit bar wording; the CPU shadow rise slew; the CPU hot-guard debounce; guard-precedence over the Mismatch freeze; live-`ma_interval` defaults). **carried** — none. Several fixes opened new gaps at the same seams (the per-label gate vs fw-fanctrl's unfiltered reconciliation; the re-scoped `verify_lock`; the `cpu_hot_c` sanitiser range; the `Clamp(Max)` tie rule) — these are counted as new below and their lineage is noted inline.

## Confirmed findings

### Blocking

- [Blocking] §2.3 step 3 bullet 2 + step 4 (hot-only tracking) — `thermal := min(thermal, cap)` on every tick with err < 0 reduces to `thermal := min(thermal, shadow)`, and the shadow falls unconditionally on `err`, so a draw dip that begins while the group is still above T* (ordinary, given θ_eff 50/90 s) or an idle device whose group is above T* for reasons it does not control (cross-coupling, stuck label, T* near ambient) drags the thermal candidate down by tens of watts / hundreds of MHz to a value at which the device was never thermally limited; recovery is at the PI's integral rate (~18 min / 20 W CPU, ~34 min / 850 MHz GPU at 3 °C; 9–27 min from the GPU floor), contradicting the same paragraph's "no state is discarded" and "the device was hot at that cap under that load", and `DeviceUnreachable` cannot fire because the tie rule reports `Shadow`, not `Clamp(Floor)`. (regressed from iteration-2 Blocking #1 — third incarnation of the tracking lock-up)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓) — unanimous Blocking
  evidence: design.md §2.3 step 3 "falling at `shadow_fall_rate` toward the target whenever the target is below it" (no `err` gate, unlike the rise) + step 4 "on every tick with `err < 0`, `thermal := min(thermal, cap)`" with `cap = min(thermal, shadow)`; all three seats reproduced Kc/Ti·err = 0.0063·err W/s / 0.14·err MHz/s from the Gains table (the same arithmetic the spec uses for its own "about 12 min"); ground independently found that thermal = shadow = floor ties to `Selected = Shadow`, so §2.4's `DeviceUnreachable` (gated on `Clamp(Floor)`) is bypassed; §4 sims 1–3 constant loads, sim 4 single rise, sim 10 no-relay only, sim 11 no un-stick recovery bar.
  fix-shape hint: track only on the hand-over tick (or only when the thermal candidate itself was binding), never continuously while the shadow is the min; gate the shadow's fall on err ≥ 0 as the rise is gated, or exclude a draw-driven shadow from the tracking source; add a dip-while-hot leg to sim 10 and a post-unstick recovery bar to sim 11.

- [Blocking] §2.2 per-group boxcars ("seeded, reseeded and invalidated together exactly as §2.2 states (auto entry, `view_changed`, resume, `EC MISMATCH`)"); eb9.2 — The carried §2.2's only seed source is fw-fanctrl's single argmax `movingAverageTemperature`, which has no per-group analogue, so the value `cpu_group_ma`/`gpu_group_ma` hold at Auto entry, resume and mismatch-clear is unspecified (seeding from the argmax MA gives the non-argmax group a false err on tick 1 and triggers hot-only tracking on a device that was never hot); and the same sentence adds `view_changed` to the reseed list, which the carried §2.2 explicitly forbids ("The buffer is **not** cleared on strategy change"), at a field rate of ~1 sample in 30. (new)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓) — unanimous Blocking
  evidence: docs/superpowers/specs/2026-09-07-fw-fanctrl-loop-design.md:268 (seed from `view.ma_temperature`) and :282-284 ("cleared only when the replica is re-seeded after `EC MISMATCH` clears"); fwloop.12 :960 distinguishes `set_interval` on view change from `reseed_ma`; design.md:120-124 and task-tree-settled.md:17 (eb9.2) repeat the `view_changed` trigger verbatim with acceptance limited to "unit tests for seeding, interval change, None handling". Refute concedes the seed-value half is softened by eb9.3's absent-group handling but the `view_changed` contradiction stands and would also clear the argmax replica used for reconciliation.
  fix-shape hint: state the per-group seed explicitly (e.g. seed each group boxcar from its instantaneous group max at entry/resume, or leave it `None` until filled and say the absent-group rule applies) and delete `view_changed` from the reseed list (view change = `set_interval` only), in both §2.2 and eb9.2.

- [Blocking] §4 sim 4 cold-start bar ("settles within ±1 °C of T* within 3 λ") vs §2.3 "The GPU dead zone"; eb9.11 — With λ_GPU = 270 s (3 λ = 810 s) and the spec's own per-tick integral step 0.7·|e| MHz, the 300 MHz zero-gain crossing alone costs ≈ 535 s even at the 4 °C overshoot bar (714 s at 3 °C, the spec's "about 12 min"), plus θ_eff = 90 s before the crossing is observed, leaving ~185 s of the budget for a sub-knee descent on a λ = 270 s loop; eb9.11 ships the clause as a named assertion that cannot pass on the default gains. (regressed from iteration-2 Blocking #3 — third rewrite of the sim-4 bar)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓) — unanimous Blocking
  evidence: design.md:531 "within 3 λ (the dead-zone crossing of §2.3 is part of the budget)"; :239-244 "300/(0.7·|err|) ticks — about 12 min at 3 °C"; :254 λ = 270; eb9.11 has no dependency on eb9.8 (fitted gains cannot rescue the sim); two seats re-measured run-1789139478 (median 1942 MHz at 81 W with util ≥ 90 %) confirming the reported clock pins at the knee. The spec's own P-term correction is unquantified and cannot absorb a ~2× shortfall.
  fix-shape hint: either restate the bar as "within 3 λ after the lock bites" (or ≥ 5 λ from the step) and keep the ≤ 4 °C overshoot clause, or shorten the crossing structurally (seed the thermal candidate at the knee, not `max`, when the card is power-limited; or a fast-descent path while err < 0 above the knee) — then re-run sim 4 on the ChainedPlant before eb9.11 is written.

- [Blocking] §2.5 step 5 (`verify_lock` re-scoped to a strike streak that survives lock changes) vs §2.5 step 3 (`DOWN_RATE_MHZ = 105` per sample) — No pairing rule between the SM-clock sample and the command it is scored against and no settling allowance: the sample is read at the top of the tick (one command stale) and 105 > `VERIFY_CLOCK_SLACK_MHZ` 30, so a compliant card scores a violation on every sample of a monotone descent; `VERIFY_STRIKES = 3` then `MISMATCH_RELEASE_STRIKES = 3` release the clock lock (`gpu.release()`) ~6 s into a GPU HOT episode — a safety inversion sim 9a does not test. (new — lineage: the iteration-1 verify_lock finding was resolved in iteration 2 by this re-scope, which is incomplete)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓) — unanimous Blocking
  evidence: design.md:369-372 and :349-350; src/control/controller.rs:2099-2163 (`set_max_clock` then score `s.gpu_sm_mhz` from the pre-actuation Sample; per-value rebuild at :2144-2147 is what the design deletes), src/actuators/gpu.rs:36/:40, controller.rs:97; refute replayed run-1789067819 and found reported SM clock above commanded+30 for up to 204 consecutive samples at util > 90 % during real descents. Refute also notes the finding's NVIDIA-forum quote is not on the cited page — not load-bearing; the repo's own telemetry carries the premise. External flag: no seat returned UNVERIFIED.
  fix-shape hint: score each sample against the command applied one tick earlier (or the max of the last two commands), scale slack with |Δcommand| during a ratchet, and suppress strikes while the lock is descending; add a compliant-card-during-ratchet false-positive test to eb9.7 and a verifier-outcome bar to sim 9a.

### Should-fix

- [Should-fix] §2.3 step 1 tick inputs vs the write-cadence paragraph ("the PI and shadow maths are `t_mono`-driven") — The tick contract carries no dt although every rate is per second, eb9.3 writes acceptance in ticks, and nothing bounds or reseeds the loops on the first post-resume sample, where `Instant` is CLOCK_BOOTTIME-backed and dt equals the suspend duration; the spec must say which (fixed per-tick step, dt clamped to one period, or reseed on `Sample.resumed`). (new)
  verdict: confirmed (reproduce ✓ / refute ✗ (dissent: REJECT) / ground ✓)
  evidence: design.md:128 input list; :212 "t_mono-driven, so a backlog lags the hardware, not the arithmetic"; src/sensors/sampler.rs:107-112 and :75-80 (`RESUME_GAP_S`); grep for "resume" in §2.3/§2.5 finds only the boxcar reseed and the reassert write exception. Severity held at Should-fix rather than the seats' Blocking on the refute seat's evidence: the thermal PI is sample-count gated (`PI_PERIOD_S = 5`, "runs every 5th sample"), CPU draw is `None` on the post-resume RAPL reset so that shadow holds (eb9.5), and the resume reassert re-writes the held cap — so "one tick saturates both candidates to `max`" is not established; the unstated dt contract and the unbounded GPU-shadow dt are what remain.
  fix-shape hint: add `dt_s` (clamped to, say, 2× the sample period) to the tick input or state that all rates are per-tick at 1 Hz; reseed/`resync_error` both loops on `Sample.resumed`; one unit vector with t_mono 100 → 7300 at err = +20 °C.

- [Should-fix] §2.5 step 3 ("the device is at or above T* when the guard clears") vs §2.3 step 4 — The GPU HOT guard keys on the NVML die while tracking pulls the thermal candidate down only when the EC group is above T*; the ratchet reaches the 1000 MHz floor in ~14 s, inside the group's 90 s dead time, so the group may never cross T*, nothing is tracked, and the applied cap follows `max` straight back to the pre-episode clock (shadow rises at 300 MHz/s, faster than the half-rate ratchet) — the re-trip cycle the roast-d1 gate was added to prevent; sim 9a scripts the die independently of the group so it cannot exercise this branch. (new — the complementary branch of the known asymmetric-recovery case)
  verdict: confirmed (reproduce ✓ / refute ✗ (dissent: REJECT) / ground ✓)
  evidence: design.md:22-23, :67-69, :251-254, :360-362 (the parenthetical is asserted, not derived), :507-509, :547-551; src/config.rs gpu_floor_mhz 1000. Refute's dissent — `gpu_temp@40` tracks the die within ~1 °C on the rise and T*_ceiling = 86 sits 2 °C under the 88 °C guard — is a real counter-consideration for Curve at a high T*, but does not cover a low fan-target T* or the group-vs-die divergence on the ratchet's timescale; the spike (replay run-1789067819 die vs boxcar'd group) settles it cheaply.
  fix-shape hint: on guard entry set the thermal candidate to `min(thermal, applied)` (the guard is thermal evidence), or feed the die as a second error source for the GPU; derive die = GPU node + offset in eb9.10 and re-run sim 9a with the excursion caused by load.

- [Should-fix] §2.3 step 4 ("the device was hot at that cap under that load") vs §2.4 Curve re-derivation / Held RPM PI; §4 sim 10 — A purely setpoint-driven hot spell (T* re-derived downward on a curve/fan-target change, or walked down by Held's RPM PI) ratchets the thermal candidate toward `floor` although the load never changed, and when T* returns the cap recovers only at the PI's integral rate (seats recompute ≈ 52 min CPU floor→max at 2 °C, ≈ 56 min GPU at 3 °C — the finding's 21/28 min understated it); `resync_error` deliberately removes the proportional kick, and sim 10's T* step leg carries only the no-relay rule, which a monotone crawl passes. (new)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓) — unanimous
  evidence: design.md §2.3 step 4, §2.4 Curve "with `resync_error` on both loops (no proportional kick)", Held "0.5 °C per PI tick"; sim 9a has a 3 λ / 10 % recovery bar for the guard case while the T* step leg has none (task-tree-settled.md:111, eb9.11/eb9.14); src/config.rs:99-102.
  fix-shape hint: on a T* step in the rising direction, resync the thermal candidate to `min(max, shadow)` (bumpless up) rather than leaving it at the tracked-down value, or give the T*-step leg of sim 10 the same 3 λ recovery bar as sim 9a.

- [Should-fix] §2.8 `t_star_last_good: Option<f64>` as the `Held` entry seed (§2.4 "Auto entry starts in `Held`") — Persisted unkeyed and untimestamped while its neighbours `cpu_gains`/`gpu_gains` (keyed by strategy:ma_interval) and `warm_start` (strategy, duty, on_ac) are qualified against the conditions they were measured under; a restart under another strategy/fan target seeds both loops from a foreign curve, the "so err = 0 on both loops and nothing moves" justification holds only for the else-branch, and in the no-curve case (the field's stated purpose) the RPM PI corrects it at λ_eff up to ~96 min; §4 has no restart-from-stale-seed leg. (new)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓) — unanimous
  evidence: design.md:267-268, :292-294, :301-308, :387-388 ("a device already above T* at entry is tracked on that first tick"), :445-449 (`validated()` = finite, positive, in range); sim 6 :552-557 covers only mid-session curve loss; eb9.17 reloads the field without a freshness check.
  fix-shape hint: key the field by (strategy, fan_target) with a timestamp and fall back to the controllable-group max when the key does not match or the value is older than N hours — or drop the field and always seed from the group max (the spike on the two field runs decides which).

- [Should-fix] §2.4 Uncontrollable "No cap step on either transition" vs §2.3 step 2 (Bypass) and step 4; eb9.5 acceptance "no cap step either way" — For any device whose thermal candidate is below its shadow (the normal post-hot-episode state by step 4's hand-over rule) the cap steps UP to the shadow on Bypass entry (thermal reports `max`) and back DOWN to the stale frozen PI state on exit (the shadow has meanwhile risen; tracking is a one-sided `min` and disabled while err ≥ 0), so eb9.5 encodes a false acceptance clause. (new — note: the iteration-2 rejection of the Curve↔Uncontrollable flapping finding cited this clause; the flapping rejection itself stands, but its "no cap step" premise no longer does)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓) — unanimous
  evidence: design.md:161-162, :172-183, :284-291; task-tree-settled.md:43-44; ground grep confirms the only `resync` in the doc is Curve's points-change path, none for Bypass entry/exit; 12 committed transitions in 7.6 h (roast-design-2.md:169).
  fix-shape hint: on Bypass entry seed the shadow to the applied cap (no upward step) and on exit resync the thermal candidate to `min(max, shadow)` (no downward step); rewrite eb9.5's clause to match.

- [Should-fix] §2.1 stateless plausibility gate extended to `argmax`/`all`/`max_c` vs §2.2 reconciliation against fw-fanctrl's unfiltered `movingAverageTemperature`; §4 sim 11 — fw-fanctrl filters only `x > 0` (both seats fetched `FrameworkToolHardwareController.py` and `FanController.py`), so a persistent > 110 °C label enters its average and not ours, latches `EC MISMATCH` within 3 scored views (`MISMATCH_ABS_DIFF_C = 1.0`, `MISMATCH_STRIKES = 3`) and drops the T* source from `Curve` to `Held` for as long as the reading persists — while sim 11 asserts "no cap effect". (new — arises from the iteration-2 per-label-gate fix)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓) — unanimous
  evidence: design.md:104-107, :120-121, :276, :293, :565-566; task-tree-settled.md:10 (eb9.1 "dropped from its group max AND from argmax/all"), :17 (eb9.2 "reconciliation unchanged"); src/control/mode.rs:74/78; git diff vs 2430e31 confirms rev 3 widened the gate's scope; upstream https://github.com/TamtamHero/fw-fanctrl — `temps = sorted([... if x > 0])`, `[x for x in self.temp_history if x > 0]`.
  fix-shape hint: keep an unfiltered `max_c`/argmax for reconciliation (gate only the group maxima and the T* feasibility inputs), or skip reconciliation scoring on views where `EcImplausible` fired; add the leg to sim 11 with an EC MISMATCH assertion.

- [Should-fix] §2.4 Held directional anti-windup set includes `DrawUnavailable` ("a frozen or unavailable one cannot move") vs §2.3 step 1 ("A `None` draw does **not** stop thermal regulation … after the dwell the shadow is set to `max`") — A thermal-only device's cap is a live function of T*, so the membership rationale is false for the rev-3 rule; with the other device `Shadow`-bound (normal light load) one lost NVML clock reading freezes the RPM PI upward for the rest of the outage, and eb9.16 tests only fire cases. (regressed from iteration-2 Should-fix on `DrawUnavailable` — the early return was fixed, the hold set was not revisited)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓) — unanimous
  evidence: design.md:148-153, :311-316, :496 (Hold stays `DrawUnavailable` post-dwell); task-tree-settled.md:170-171 (eb9.16).
  fix-shape hint: remove `DrawUnavailable` from the upward set (or add a post-dwell `ThermalOnly` hold that is in neither set) and add a must-not-fire test to eb9.16.

- [Should-fix] §2.4 Uncontrollable ("the shadows own the caps, so devices run at their draw plus headroom") vs §2.3 step 3 rise gate — Bypass exempts only the thermal candidate; the shadow still obeys "rising only while `err ≥ 0`" against the frozen T*, so a group above the frozen T* holds (or falls) and cannot follow a rising load for the episode (11–16 % of loaded samples), with no other actuator authority in the state; eb9.4 never mentions Bypass and sim 8 only requires the value be reached. (regressed from iteration-2 Should-fix "Uncontrollable vs tick inputs" — the park input became Bypass, the contradiction with "draw plus headroom" remains)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓) — unanimous (refute notes "ratchet" overstates: the shadow plateaus rather than descends unless draw dips)
  evidence: design.md:161-162, :167-172, :283-291, :495; task-tree-settled.md:31-34, :150.
  fix-shape hint: under Bypass compute the shadow's rise gate with err treated as ≥ 0 (or skip the gate), and add a Bypass + hot-group unit case to eb9.4.

- [Should-fix] §2.3 "The GPU dead zone" bound (≤ 300 MHz of zero-gain travel) vs `DRAW_UNAVAILABLE_DWELL_S` expiry (shadow set to `max`) and `gpu_shadow_enabled = false` — Both paths make `cap = min(thermal, max)` with thermal saturated at 3090 for a never-hot device, restoring the full ~947 MHz plateau (≈ 38 min at 3 °C by the spec's own scaling), bounded only by the 88 °C guard; sim 8 requires only that `DrawUnavailable` be reached and no sim runs the off path. (regressed from iteration-2 Should-fix on `gpu_shadow_enabled` — scope narrowed: ground concedes the A/B half is the user's parked hardware check, the automatic dwell path is the live gap)
  verdict: confirmed (reproduce ✓ / refute ✗ (dissent: REJECT) / ground ✓)
  evidence: design.md:150-153, :158-161, :221, :234-244 ("**not** the ~950 MHz to `gpu_max_mhz`"); task-tree-settled.md:34 (unit-only), :103, :111-112, :150. Refute's dissent (the HOT guard is the stated universal bound and sim 9 exercises it) is why this is Should-fix, not Blocking.
  fix-shape hint: when the shadow is forced to `max` (dwell or toggle), also cap the thermal candidate's seed/ceiling at the power-limit knee + headroom for a power-limited card, or state the widened window and add a thermal-only sim leg with the sim-4 overshoot bar.

- [Should-fix] §2.3 step 4 tie rule ("on a tie `Selected = Shadow`") + step 7 `Clamp(Bound::Max)` — Both candidates clamp to the same `[floor, max]`, so thermal = max with thermal < shadow is impossible and the only tie routes to `Shadow`: `Hold::Clamp(Max)` is unreachable, the informational `DeviceUnreachable` leg never fires, the anti-windup's headline member is inert, and eb9.14's "every Hold/Selected value reached behaviourally" smoke cannot pass. `Clamp(Floor)` is reachable, so the asymmetry is Max-specific. (new — arises from the iteration-1 `Bound` payload fix)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓) — unanimous
  evidence: design.md:133, :156, :171, :172-181, :195-197, :313, :329-330, :544-546; task-tree-settled.md:150-151. Refute also checked `gpu_shadow_enabled = false` (shadow held exactly at `max` → still a tie → `Shadow`) and the ratchet ordering (single `max` per tick).
  fix-shape hint: break ties toward the candidate that is at a clamp bound (or define `Clamp(Max)` as "thermal at max and shadow ≥ max"), and drop the informational `DeviceUnreachable` leg if it is not wanted; regenerate eb9.14's checklist.

- [Should-fix] §2.3 Gains — GPU τ = 35 s carries no derivation (it is the CPU plant's value) while K and θ_eff are cited; Kc = τ/(K(λ+θ_eff)) scales linearly with it, defaults ship uncalibrated ("Auto no longer requires calibration"), and §4's GPU node reuses the same τ so sim 10's ±50 % perturbation is centred on the assumption. (new — the τ factor of the formula whose K factor was fixed in iteration 1)
  verdict: confirmed (reproduce ✓ / refute ✗ (dissent: REJECT) / ground ✓)
  evidence: design.md:251-254, :377/:431, :508, :555; the only τ = 35 s lineage is 2026-09-07 spec :478/:850 (CPU die → `cpu@4c`). Refute's dissent — the IMC λ ≥ 3θ margin, the 88 °C guard and eb9.8's fit are the stated safeguards — is real and keeps this at Should-fix; the spike (fit_fopdt on the existing gpu-burn captures against the GPU group) needs no hardware run.
  fix-shape hint: fit τ_GPU from the September captures and put the number (with its source) in §2.3 and eb9.10; if the fit lands outside 17.5–52.5 s, re-derive the default Kc.

- [Should-fix] §2.1 "no jump rule … fails in the safe direction" vs §2.4 Uncontrollable — The safe-direction argument is scoped to the device groups; a plausible-but-stuck uncontrollable label (100–109 °C, above what the guarded groups can reach) satisfies the entry debounce trivially and can never be overtaken, so `Uncontrollable`/Bypass latches for the session and T*-regulation is silently off on both devices with only the die/Tctl guards and hardware trips remaining. (new — reconsidered against the iteration-2 rejection of "Uncontrollable parks both candidates unconditionally": that rejection's escape hatch, "a hot group becomes the argmax and exits the state", rested on the field data and on the then-present jump rule; rev 3 deleted `EC_MAX_JUMP_C`, which is the materially new spec text)
  verdict: confirmed (reproduce ✓ / refute ✗ (dissent: REJECT) / ground ✓)
  evidence: design.md:104-111, :283-291, :352-358; sim 11 pins a GPU-group label only, sim 8 requires only that the state be reached; roast-design-2.md:163. Refute's dissent (guards are an independent layer and Bypass is already an accepted normal state) bounds the consequence to loss of proactive regulation, not runaway — hence Should-fix.
  fix-shape hint: give `Uncontrollable` a dwell-based flag/backstop (e.g. after N min with an unchanged argmax value raise `ArgmaxStuck` and fall to Held with T* = controllable max), or apply a per-label no-change detector to the uncontrollable labels only; extend sim 11 with a stuck ambient/charger leg.

- [Should-fix] §2.1 fixed eight-label list with `is_controllable() = group().is_some()` vs §2.4 Uncontrollable — Any label outside the list (renamed/added by an EC firmware or kernel update) is classified uncontrollable and, when it holds the argmax, drives the controller into Bypass with the same `ArgmaxUncontrollable` flag as a genuine ambient argmax; the affected group's max silently shrinks with no `EcImplausible`-style signal; the code this replaces classified by prefix precisely to tolerate this, and no startup label check or test exists. (new)
  verdict: confirmed (reproduce ✓ / refute ✗ (dissent: REJECT) / ground ✓)
  evidence: design.md:51-58, :112-113, :283-291; src/sensors/ec.rs (`starts_with("apu"/"cpu"/"gpu_")`, `MAX_TEMP_INDEX` comment "not silently truncated"); eb9.1's acceptance covers dGPU-off, 150 °C and 105 °C only. Refute's dissent (single-machine scope, Bypass is a validated safe state) is noted; kept at Should-fix because the outcome is silent loss of T* regulation for the session — the profile's material class — and the fix is a startup check.
  fix-shape hint: keep a prefix fallback for group membership (exact list first, prefix second) and log/flag `EcUnknownLabel` at startup when any expected label is missing or an unexpected one appears.

- [Should-fix] §2.4 Held `T*_floor = min(max(plausible uncontrollable sensors) + 5 °C, T*_ceiling)`; §2.7; eb9.16 — No value is defined when the plausible uncontrollable set is empty, and both uncontrollable labels sit on the same F75303 @4d chip, so one chip fault or one `−150` sentinel pair empties the set while `cpu@4c`/`gpu_temp@40` keep the groups valid and `ec_valid` true; the loop stays in Held with a clamp bound and §2.7's high-unreachable comparison that need a number every tick. (new)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓) — unanimous
  evidence: design.md:54-61, :102, :105, :307-309, :326-332; 2026-09-07 spec :285 (`ec_valid` false only when every reading is dropped); eb9.16 (task-tree-settled.md:170-171) tests only the inverted-interval case. (Refute corrects the finding's "§2.8 sanitiser" citation — that section is Guards — not load-bearing.)
  fix-shape hint: define the empty case (floor = T*_ceiling, or floor = last good floor with `TargetUnreachable(high)` raised) and add the one unit vector from the spike to eb9.16.

- [Should-fix] §2.8 `cpu_hot_c` sanitised to `[gpu_hot_c − 2 + 3, CPU_TRIP_C − 1]` vs §2.4 `T*_ceiling = min(cpu_hot_c − 2, gpu_hot_c − 2)` — The stated rationale (ceiling ≥ 80 °C) needs only `cpu_hot_c ≥ 82`; the written floor `gpu_hot_c + 1` forces `cpu_hot_c ∈ [86, 94]` (≥ 89 at defaults), couples one device's protection setpoint to the other's, and makes the CPU term of the `min` unconditionally dead, so eb9.16's test of it exercises nothing. (new — introduced by the iteration-2 inverted-clamp fix)
  verdict: confirmed (reproduce ✓ / refute ✗-survived / ground ✓) — unanimous
  evidence: design.md:279, :452-454; task-tree-settled.md:53, :170; src/config.rs:32/:37 (`GPU_HOT_C_FLOOR` 85, `GPU_HOT_C_CEIL = GPU_TRIP_C − GPU_HYSTERESIS_C` = 89), watchdog.rs:17/:25, guards.rs:48.
  fix-shape hint: sanitise `cpu_hot_c` to `[82, CPU_TRIP_C − 1]` (independent of `gpu_hot_c`) and either keep the `min` live or state that the ceiling is `gpu_hot_c − 2`.

## Not verified (beyond panel cap)
- none

## Beyond remainder cap (count only)
- none

## Rejected (with reason)
- §4 plant makes the GPU die / CPU Tctl scripted inputs so sim 9's bars are decided by the script, not the guard (suggested Should-fix) — reproduce and refute REJECT: §2.1 deliberately separates the two signal domains (regulate on the EC group, guard on the die), scripted excursions sit in the same fault-injection API as stuck labels and EC-invalid views, eb9.15's adversarial flap-at-the-threshold unit test is a stronger no-limit-cycle check than one plant trajectory, and the physical "does the ratchet cool the die" question is explicitly parked to the 30-min hardware check (Decision 8). Ground's CONFIRM (the spec's "closed-loop GPU HOT episode sim" wording overstates what sim 9 shows) stands as a minority view — a one-line wording edit; the divergent die-vs-group branch is the confirmed Should-fix above.
- §4 sim 4's ≤ 4 °C overshoot bar is satisfiable or not purely by the unspecified load level (suggested Should-fix) — unanimous REJECT: sim 4's own text fixes the regime ("the dead-zone crossing of §2.3 is part of the budget", i.e. a knee-crossing heavy step) and §2.3 states the bar "is set from" the dead-zone arithmetic; a mild step that never crosses the knee is not the scenario named; the bar is a delta from T*, so T* need not be pinned; the finding's "pins at ~2143 MHz" misreads the knee as the operating clock (the spec's worked case is ~2520 MHz). A numeric `load_level` next to "heavy" would not hurt.
- eb9.3's hot-only-tracking acceptance is unsatisfiable inside eb9.3 because the stub shadow is `max` (suggested Should-fix) — reproduce and refute REJECT: eb9.3's own `seed(thermal_at, shadow_at)` API sets the shadow to any fixed value ("an externally lower cap (stub shadow)"), and its acceptance "after seed(Some(x), _) the first tick applies min(x, shadow)" proves the shadow is settable, so every clause is reachable. Ground's CONFIRM notes a real transcription slip — the design's bar reads "below the shadow", eb9.3's reads "below the cap" — worth restoring the design's wording in eb9.3.
- Dynamic Boost reallocates the GPU's enforced power limit from the CPU cap, making the CPU actuator an input to the GPU plant (suggested Should-fix, external) — unanimous REJECT on grounded evidence: docs/research/05-fw-fanctrl-loop.md §5 (2026-09-09, this machine) records "`nvidia-powerd` is inactive"; Tom's Hardware's Framework 16 RTX 5070 review states the module does not support Dynamic Boost; nvidia-powerd is documented broken on AMD (amd_pstate) laptops (open-gpu-kernel-modules #392, NVIDIA forum 319852); the base spec's Facts measured `enforced.power.limit` 100 W. Re-litigate only if the target hardware changes.
- `gpu_group_c == None` is read as "dGPU unpowered" from session history alone, so an idle-down or a powered label dropout is treated as a fault and answered with a release-to-`max` write (suggested Should-fix) — reproduce and refute REJECT: `None` requires every group label to fail the absolute gate (an idling powered dGPU reads plausible low temperatures — the design's own 2026-09-09/11 facts show the labels as load-following), the 60 s dwell filters transients, eb9.14 deliberately tests both legs as intended behaviour, and the release is backstopped by the die-keyed HOT guard. Ground's CONFIRM rests on the stale 2026-09-08 `sensors/ec.rs` header ("they never report on this machine"), which the design's later facts supersede — that header comment should be updated.
- No escalation between the CPU soft guard (90 °C, 2 W/sample) and the latched `CPU_TRIP_C` 95 trip, whose stock restore raises the fast limit (suggested Should-fix) — refute and ground REJECT: the hard trip is debounced by the same `TRIP_STREAK = 3`, so a single 10 °C+ sample trips neither layer; the fixed 53 W fast limit and the stock-restore direction are carried "unchanged" from the already-roasted base spec (§2.7, seed) and watchdog.rs states the hand-off-to-firmware rationale; the finding never shows a sustained excursion outrunning the ratchet. Reproduce's CONFIRM correctly notes that iteration 2's "consider also lowering the fast limit during an episode" hint was not adopted and sim 9b has no Tctl-below-trip bar — an FYI for the fix pass, not a confirmed gap. (The finding's "13.5 °C" figure is from iteration 2's field replay, not the spec text.)

## Unverified nits (spot-checked)
- [Nit] `ThermalMode::Bypass` is machinery rev 3 no longer needs since a cool thermal candidate integrates to `max` on its own (spot refute: REJECT — "integrates upward at its own pace" is a slow ramp; a recently-hot device entering Uncontrollable has thermal < shadow and would keep binding the cap without Bypass, defeating the state's "draw plus headroom" guarantee; the iteration-2 spot rejection of "Uncontrollable is redundant with Held" is unaffected).
- [Nit] `Config` gain overrides `cpu_gains`/`gpu_gains` are a third gain source with no stated use case (spot refute: REJECT — the precedence rule + `gains_source` is the iteration-2 fix applied verbatim; an operator escape hatch for a fit that goes wrong parallels the other operator-settable bounds; at most a one-line "why this key exists").
- [Nit] The `GroupLost` path is built for a per-group dropout the spec never cites an observation of (spot refute: REJECT — it mirrors the accepted `DrawUnavailable` dwell shape one paragraph earlier, the design itself distinguishes absent-from-tick-1 from lost, and the whole-EC watchdog cannot see a single group going `None` while the replica stays valid).
- [Nit] `default_gains(ma_interval_s)` adopts machinery iteration 2 judged already covered by the ±50 % θ runs (spot refute: REJECT — iteration 2's own fix-shape hint offered exactly this branch as one of two acceptable resolutions; it rides the existing gains-resolution call site and can only improve default fidelity).

## Escalations (need human)
- none
---
