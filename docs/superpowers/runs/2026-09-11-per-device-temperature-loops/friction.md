- [2026-09-11 roast-design/extension-4] Saved continuation assumed Claude Workflow and Claude cache paths; current Codex session has subagents but no Workflow — used the documented manual pipeline, rendered local prompts, mapped role tiers to available OpenAI models, and will persist coverage and stage outputs.
- [2026-09-11 roast-design/extension-4] All nine late-round scouts completed with zero raw findings. Reporter Step 4 nevertheless requires low coverage for zero raw findings on a non-trivial artifact, while late-round scout instructions expressly permit no findings — preserve that qualifier and distinguish the policy trigger from an agent failure; no judge panels can be dispatched without inventing candidates.
- [2026-09-11 super-code/preflight] The sandboxed baseline `cargo test` denied Unix-domain socket creation with `Operation not permitted`; rerunning the identical command with the approved test escalation passed 699 tests (2 ignored). Treat this as an execution-environment restriction, not a product failure, and use the approved cargo-test escalation for integration gates that exercise socket tests.
- [2026-09-12 super-code/fw-fanctrl-loop-eb9.7] The first controller implementation passed 834 tests but adversarial review found 14 lifecycle/safety defects and weakened simulator assertions. The repair required a second production lane plus a simulation lane, exposed a once-per-40-second refinement regression, and expanded the task to narrow clock/floor seams. The final fix wave restored the physical bars and passed 857 tests before scoped re-review.
- [2026-09-12 super-code/fw-fanctrl-loop-eb9.7] Broad `rustfmt` in the initial implementation and an accidental helper formatting pass created review-hostile churn in hot legacy files. The helper churn was restored before functional edits; the controller's edition-2024 formatting remained entangled with the initial commit and is disclosed in the task report for whitespace-ignored review.
- [2026-09-12 super-code/fw-fanctrl-loop-eb9.12] The terminal deletion sweep initially removed hundreds of still-live safety, persistence, calibration and rev4 lifecycle tests together with retired scalar coverage. Five adversarial rounds restored and adapted the live cases, replaced synthetic telemetry checks with the real Auto path, removed stale active-source guidance and broad warning suppressions, and strengthened two causal tests that could pass with broken accumulation or re-entry behavior.
- [2026-09-12 super-code/fw-fanctrl-loop-eb9.17] The final integration audit found that the controller clipped elapsed time before `TStarSource`, making its unmarked-wall-gap branch unreachable. The first repair exposed additional continuity counters that could consume the gap tick or survive it: Curve eligibility and quarantine recovery. Production-level 7,200-second regressions now require zero timer credit on the gap tick and fresh post-gap dwell/recovery samples.
- [2026-09-12 super-code/fw-fanctrl-loop-eb9.17] Existing restart and wiring tests looked comprehensive but used a hand-authored warm pair and prose/default inventories. Review required the live steady recorder to create the persisted pair, fitted/default controllers to diverge after PI, and config/status registries to be mechanically coupled to production consumption, raise, serialization and rendering paths.
