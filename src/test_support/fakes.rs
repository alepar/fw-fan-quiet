//! `FakeFanctrl` (a scripted `FanctrlSource` implementation that records
//! every command it receives and replays scripted `print all`/`print speed`
//! views or failures) belongs to Task 7 (fwloop.2). Empty stub here — this
//! task only needs the module to exist so `test_support::mod` compiles.
