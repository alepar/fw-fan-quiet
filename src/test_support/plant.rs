//! `FanctrlEmulator` / `ThermalPlant` / `FanPlant` / `ChainedPlant` — the
//! fw-fanctrl replica and chained physical plant driving the closed-loop
//! acceptance sims — belong to Task 17 (fwloop.16). Empty stub here — this
//! task only needs the module to exist so `test_support::mod` compiles.
